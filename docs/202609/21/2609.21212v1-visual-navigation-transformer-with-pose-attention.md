---
title: Visual Navigation Transformer with Pose Attention
title_zh: 具有位姿注意力的视觉导航Transformer
authors: "Beiming Li, Jaime Romero, Jonathan Diller, Vijay Kumar, Alejandro Ribeiro"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21212v1"
tags: ["query:world-uav"]
score: 6.0
evidence: 以位姿索引深度关键帧的视觉导航Transformer策略
tldr: "现有学习式导航策略把观测当作时序历史，位置编码绑定时间顺序，难以复用过往经验；复用经验的系统又多依赖显式地图或拓扑图。本文提出 VNT-PA，用相机位姿索引深度关键帧作为空间上下文，注意力依赖位姿差而非时间顺序。在 HM3D 点目标导航上达到 93.3% 成功率与 90.4% SPL，训练更快，定位噪声下退化更平缓。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.21212v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1813, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.21212v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1828, \"height\": 984, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.21212v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 842, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.21212v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 895, \"height\": 511, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.21212v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21212v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 802, \"height\": 320, \"label\": \"Table\"}]"
motivation: 学习式导航策略以时序历史编码观测，位置编码绑定时间，难以复用同一环境早先遍历的经验，复用者又需显式地图并规划。
method: 提出 VNT-PA，以相机位姿为位置编码，将深度关键帧组成位姿索引集合，注意力按位姿差计算，仅凭当前位姿与目标位置查询空间上下文预测动作。
result: "在 HM3D 验证场景点目标导航中达 93.3% 成功率、90.4% SPL，优于时序编码与位姿作输入特征的基线，训练效率更高，定位噪声下退化更平缓。"
conclusion: 表明带位姿标记的经验可直接作为学习规划器的环境表示，让注意力依赖位姿差而非时间顺序，可加速训练并提升长程导航，且测试时可融合不同轨迹帧。
---

## 摘要
学习到的导航策略通常将观测作为按时间排序的历史来使用，并用位置编码将每个观测与其被看到的时间绑定，这使得难以复用来自环境早期遍历的经验。确实复用此类经验的系统通常会构建显式表示，例如地图或拓扑图，并在其上规划。我们提出VNT-PA（Visual Navigation Transformer with Pose Attention，具有位姿注意力的视觉导航Transformer），一种Transformer规划器，其上下文是由相机位姿索引的一组深度关键帧。以相机位姿作为位置编码，注意力取决于关键帧之间的位姿差异，而不是它们的时间顺序。VNT-PA通过模仿在真值场景网格上运行的最短路径规划器进行训练，仅用其当前位姿和目标位置查询空间上下文来预测动作。在HM3D验证场景中的点目标导航任务上，VNT-PA达到93.3%的成功率和90.4%的按路径长度加权的成功率（SPL），在导航性能和训练效率上均优于将相同上下文编码为时间序列或将位姿视为输入特征的基线。由于空间上下文是按位姿索引的集合，来自不同轨迹的帧可以在测试时融合。该规划器在定位噪声下也比在显式地图上规划的传统基线退化得更平缓。这些结果表明，带有位姿标记的经验可以直接作为学习型规划器的环境表示，并且让注意力取决于位姿差异而非时间顺序，可以加速训练并改善长时程导航。

## Abstract
Learned navigation policies typically consume observations as a temporally ordered history, with positional encodings tying each observation to when it was seen, making it difficult to reuse experience from earlier traversals of an environment. Systems that do reuse such experience usually construct an explicit representation, such as a map or a topological graph, and plan on it. We propose VNT-PA (Visual Navigation Transformer with Pose Attention), a transformer planner whose context is a set of depth keyframes indexed by camera pose. With camera poses as positional encoding, attention depends on the pose differences between keyframes rather than on their temporal order. VNT-PA is trained to imitate a shortest-path planner operating on the ground-truth scene mesh, predicting actions by querying the spatial context with only its current pose and the goal position. On point-goal navigation in HM3D validation scenes, VNT-PA reaches 93.3% success and 90.4% success weighted by path length (SPL), outperforming baselines that encode the same context as a temporal sequence or treat pose as an input feature, in both navigation performance and training efficiency. Because the spatial context is a pose-indexed set, frames from different trajectories can be fused at test time. The planner also degrades more gracefully under localization noise than a conventional baseline which plans on explicit maps. These results show that pose-stamped experience can serve directly as the environment representation for a learned planner, and that making attention depend on pose differences, rather than on temporal order, speeds up training and improves long-horizon navigation.