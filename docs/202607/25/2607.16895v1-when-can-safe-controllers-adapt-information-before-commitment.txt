Title: When Can Safe Controllers Adapt? Information before Commitment

URL Source: https://arxiv.org/pdf/2607.16895v1

Published Time: Tue, 21 Jul 2026 00:44:25 GMT

Number of Pages: 45

Markdown Content:
# When Can Safe Controllers Adapt? 

# Information before Commitment 

## Venkatesh Saligrama ∗

Abstract 

Safe adaptive control is online adaptation under a safety guarantee on the learning trajectory itself. The controller may use any causal, history-dependent rule and act differently across environments as data arrive. Only its safety guarantee is uniform: the same rule must satisfy it under every initially plausible model. Performance is measured against a safe oracle that knows the realized model. Many finite-time analyses assume persistent excitation of the uniformly safe closed loop, so the data distinguish every pair of models requiring different control decisions. Under that assumption, feasibility is already settled; only the rate remains. We ask instead: Do the safety constraints permit such an informative experiment at all? 

While an alternative remains plausible, the controller must preserve a safe continuation under it. We call the first action that forecloses such a continuation commitment . Chance safety allows commitment only on an event rare under the alternative, and the evidence must arrive beforehand: the observa-tion generated by the committing action is too late. We define precommitment information as the KL divergence between learner-visible laws stopped before commitment. Our main result is a causal reduction. The commitment rule determines three system-dependent quantities: (a) the probability that safety permits commitment under the alternative, (b) the target-side cost of remaining noncommittal, (c) the information available when the decision is made. Bounded precommitment information therefore leaves a fixed fraction of the oracle gap unavoidable. If the gap is Ω( T ), every uniformly safe policy has linear regret. We establish the obstruction in a constrained linear system with quadratic regulation cost. With multiple alternatives, finite information limits how small a certified candidate list can be. We also prove recovery when safe experiments are repeatable, persistently informative, and return to a common continuation state, and derive semidefinite upper certificates for deterministic linear-Gaussian systems. 

# 1 Introduction 

The safe adaptive control problem is regret minimization over uniformly safe policies. A causal policy must satisfy the safety specification under every model not yet ruled out, and its performance is measured against the safe oracle that knows the true model. The difficulty is exploration. The policy can learn only through actions that remain safe under every plausible model, yet it must reach the oracle’s model-specific behavior from inside that set. Many finite-time analyses impose conditions under which this common safe regime remains informative. Excitation can be injected, or process noise and safety-induced state variation keep the closed loop informative [1, 2, 3, 4, 5]. Safety then limits where the system may go, but not the decision-relevant information it can acquire. Under such conditions, familiar adaptive-control rates can be recovered. We ask what can be said when the constraints also limit the experiment. This is not merely a slower estimation problem. An action that distinguishes models may consume safety margin under a model that is still plausible. The admissible experiment set is then endogenous: it depends on the margin already spent and on the models not yet falsified. The model-specific action sought by adaptation is unavailable until 

> ∗Department of Electrical and Computer Engineering, Boston University, MA 02215, Email: srv@bu.edu

1arXiv:2607.16895v1 [cs.LG] 18 Jul 2026 the evidence supporting it has been collected. This creates an ordering constraint: evidence must precede commitment, while the same margin that commitment consumes may also limit the available evidence. The full controlled trajectory is therefore the wrong statistical experiment: it can contain evidence generated by the very action that already required the model-specific decision. The question is no longer only how fast uncertainty shrinks, but whether enough evidence can be collected before the first action that forecloses a safe continuation under an alternative. We call the evidence available before that action precommitment in-formation . Bounded precommitment information certifies an unavoidable oracle gap. Persistent information generated by repeatable safety-preserving experiments supports recovery. 

Guarantee or exclude. With uncertainty unresolved, a uniformly safe controller has two options. It can guarantee: remain in the regime that is feasible under every plausible model and accept the value this assures. In our lower-bound examples, this regime already attains the max–min value. Or it can exclude: once the evidence permits, take an action as if an alternative were no longer in play. The exclusion is statistical, not literal. The safety guarantee remains uniform over the initial model class; chance safety permits an action that is unsafe under an alternative only on an event whose probability under that alternative is at most ηT .Making commitment likely under the target while keeping it this rare under the alternative requires order log(1 /η T ) nats of evidence, and that evidence must be available before the committing action is chosen. In the lower-bound examples, adaptation beyond the max–min value is precisely such an exclusion. The max–min criterion does not distinguish the two options once the worst-case value is fixed. Oracle regret does. 

Oracle regret. Let the unknown environment be θ ∈ Θ0. Over a horizon T , let Π T

> safe

(Θ 0) be the causal policies whose probability of any safety violation is at most ηT under every θ ∈ Θ0. Let J⋆T (θ) be the value of the safe oracle that knows θ, and define 

RT (π; θ) := J⋆T (θ) − JT (π; θ).

We study 

RT (Θ 0) := inf 

> π∈ΠT
> safe (Θ 0)

sup 

> θ∈Θ0

J⋆T (θ) − JT (π; θ), (1) and ask when it is o(T ). More generally, if the oracle advantage itself is GT = o(T ), we ask whether the learner can close all but o(GT ) of that gap. 

Commitment. Fix a target environment θ and an incompatible alternative θ′. A commitment rule specifies, at each time t, a predictable set Dθ|θ′ 

> t

of history–action pairs. The induced commitment time and event are 

τ π := inf t ≤ T : ( Ht−1, U t) ∈ D θ|θ′ 

> t

, Aπ := {τ π ≤ T }.

In deterministic constrained systems, commitment is the first action after which no hard-safe continuation remains under θ′. Noncommitment is therefore preservation of recursive feasibility under the alternative. Because the commitment decision is made from ( Ht−1, U t), the observation produced by Uτ π arrives too late to justify that action. The commitment rule is the fundamental object. It simultaneously determines how often safety permits commitment under the alternative, what noncommitment costs under the target, and how much information is available when the decision is made. Safety determines how often commitment is allowed under the alternative. Value determines the regret cost of not committing under the target. Information is measured on the learner-visible record stopped before commitment. The controller need not identify the full model; it needs only to resolve alternatives that require different safe decisions. This connects to sequential design and controlled sensing [6, 7, 8], with the additional restriction that safety controls which experiments are admissible and whether their observations arrive before commitment. 

Precommitment lower bound. Let 

αT (θ, θ ′) := sup 

> μ∈ΠT
> safe ({θ,θ ′})

Pμθ′ (Aμ)2be the largest commitment probability allowed under the alternative. If commitment forces a violation under 

θ′, then αT (θ, θ ′) ≤ ηT . For q ∈ [0 , 1], define the noncommitment regret profile 

GT (θ, θ ′; q) := inf  

> μ∈ΠT
> safe ({θ,θ ′})
> Pμθ(( Aμ)c)≥q

RT (μ; θ).

It is the least target regret among uniformly safe policies that do not commit with probability at least q.Let Ipre (T ; θ, θ ′) be the largest KL divergence between the stopped learner-visible laws under θ and θ′.Since Aπ is determined by the stopped record, data processing links precommitment information to the two commitment probabilities. Theorem 1 gives an exact binary-KL bound. A simpler consequence is 

RT (π; θ) ≥ G T θ, θ ′;

 12 e−Ipre (T ;θ,θ ′) − αT (θ, θ ′)



> +

!

. (2) If a system-specific calculation gives 

GT (θ, θ ′; q) ≥ [GT q − ζT ]+,

then 

RT (π; θ) ≥



GT

 12 e−Ipre (T ;θ,θ ′) − αT (θ, θ ′)



− ζT



> +

. (3) Thus bounded precommitment information leaves a nonvanishing fraction of the oracle gap. If GT = Ω( T ), every uniformly safe policy has linear oracle regret. At the standing risk level ηT = T −2, the explicit bound remains positive until the stopped KL reaches 2 log T − log 2. Driving the target noncommitment probability to zero therefore requires evidence of order 2 log T , which a bounded profile cannot supply. 

Finite-headroom example. A simple example (see Figure 1) shows why waiting need not help. Over 

T rounds, a controller chooses commands 1 Ut ∈ [0 , U ]. The command earns reward but consumes thermal headroom: 

dt+1 = dt − θu 2 

> t

, dt ≥ 0.

After applying ut, the controller observes 

yt = θu t + ξt, ξt ∼ N (0 , σ 2).        

> command
> Ut
> Performance
> reward increases
> Information
> kl t∝U2
> t
> Safety margin
> dt+1 =dt−θU 2
> t
> reward
> signal
> consumption

Controller Reward 

Healthy oracle U T 

Worn-safe max–min baseline min {U T, pT d 0/θ high }

Uniformly safe learner may exceed the baseline only after evidence 

Evidence required for error of order ηT = T −2: order log(1 /η T ) = 2 log T

Evidence available before leaving the worn budget: θhigh d0/(2 σ2) — independent of the horizon 

Figure 1: Finite headroom. A command affects reward, information, and thermal headroom. The evidence needed to leave the worn-safe regime grows with the horizon, while the evidence available before leaving it is bounded. 

> 1

The restriction to nonnegative commands is expositional, not structural. One may instead take Ut ∈ [−U, U ], reward 

rt = |Ut|, and the same thermal dynamics and sensor; both safety expenditure and Gaussian information depend on U 2 

> t

, so the sign is immaterial. A signed aeroelastic realization replaces these by a noisy flutter signal Yt = θU 2 

> t

+ ξt and a fatigue budget 

dt+1 = dt − θ2U 4 

> t

. Then both accumulated fatigue and pairwise information depend on P 

> t

U 4 

> t

, and the same matched-order argument applies. 

3Take rt = ut and compare a healthy device θlow = 0 with a worn device θhigh > 0. The healthy oracle uses ut = U in every round. Let τ π be the first time the learner leaves the worn-device budget. Before commitment, safety under the worn model gives 

X 

> t<τ π

u2 

> t

≤ d0

θhigh 

.

The stopped Gaussian KL therefore satisfies 

Ipre (T ; θlow , θ high ) ≤ (θhigh − θlow )2

2σ2

d0

θhigh 

= θhigh d0

2σ2 , (4) which is independent of T . The required evidence grows as 2 log T , but the available evidence does not grow at all. The healthy oracle gap is Θ( T ), so the lower bound gives linear regret. 

Recovery. An unbounded information profile alone does not prove recovery. One safe policy must generate the information and then enter the selected operating regime without losing feasibility. We give a sufficient condition based on a repeatable safety-preserving experiment. In the braking example, maximal braking is both safe and informative, and a confidence-based controller achieves O(√T log T ) regret. 

Contributions. 

1. Commitment and precommitment information. We define a predictable commitment rule for con-trolled systems. The same rule determines the safety allowance under the alternative, the noncommitment regret profile under the target, and the stopped information. 2. Causal reduction. We reduce safe oracle attainability to three quantities induced by the same com-mitment rule: the alternative-side commitment allowance, the target-side noncommitment regret profile, and the information in the learner-visible record available at the decision. This gives the exact binary-KL bound in Theorem 1. We extend the result to continuous classes and use a list-Fano bound to quantify partial specialization among multiple alternatives. 3. Examples and recovery. We prove linear regret in a constrained linear system with quadratic regulation cost and an unknown margin-consuming input direction. An unknown-disturbance-location problem yields a certified safe candidate list. A separate alignment condition gives oracle recovery at rate O(√T log T )when safe experiments are repeatable and informative. 4. Computation. For deterministic linear-Gaussian systems, we formulate finite-horizon precommitment information as a QCQP and derive SDP upper certificates. Numerical studies show how replenishment, sensing, and unknown-location structure change the attainable level of adaptation. 

# 2 Setup 

We define the controlled interaction, uniform safety, the parameter-aware oracle, and commitment. 

## 2.1 General controlled interaction 

Fix a horizon T and an uncertainty set Θ 0 ⊆ Θ. A state-space representation, when useful, is 

Xt+1 = fθ,t (Xt, U t, W t+1 ), Zt = hθ,t (Xt, U t, V t), t = 1 , . . . , T, (5) where Xt is the physical state, Ut is the chosen control or sensing action, Zt is the learner-visible datum, and ( Wt, V t) are noises. Uppercase denotes random variables. The learner sees 

Ht−1 := ( Z1, U 1, . . . , Z t−1, U t−1)4before choosing Ut ∼ πt(· | Ht−1). Equivalently, under θ, the next learner-visible datum has conditional law 

Zt | (Ht−1, U t) ∼ Qθ,t (· | Ht−1, U t). (6) The kernels Qθ,t are induced by the dynamics and observation model. The lower bound uses only these controlled observation laws; it does not require linearity, Gaussian noise, or Markov representation in (5). Let Safe θ,T be the event that all state, input, resource, and operational constraints hold through horizon 

T under θ. For state constraints, it includes the post-action state XT +1 generated by UT . Let P πθ,T denote the full physical trajectory law, including latent safety variables, and let P πθ,T denote the learner-visible marginal. Safety probabilities are taken under P πθ,T ; KL divergences are taken on learner-visible records. 

Definition 1 (Uniform chance safety) . For a risk level ηT ∈ [0 , 1), a policy is chance-ηT safe under θ if 

P πθ,T (Safe cθ,T ) ≤ ηT .

It is uniformly chance-safe on Θ 0 if the same causal policy satisfies this inequality for every initially plausible environment: ΠT

> safe

(Θ 0) := \

> θ∈Θ0

n

π : P πθ,T (Safe cθ,T ) ≤ ηT

o

. (7) Hard safety is the special case ηT = 0. In the lower-bound examples, ηT → 0, often ηT = T −2. When reward is bounded, the contribution of violation paths to expected value then vanishes. 

Remark 1 (Why safety is uniform) . The policy must be certified before the true environment is known. Uniformity therefore applies to the safety guarantee, not to the realized behavior: a causal policy may use observations and act differently under different models. Every data-dependent decision inside the policy, including a decision to commit, remains covered by the guarantee in (7). 

Remark 2 (Uniform safety and max–min control) . The class Π T

> safe

(Θ 0) contains general adaptive policies; a max–min policy may also be adaptive. The distinction in this paper is the performance criterion: safety is required uniformly over Θ 0, whereas performance is measured by regret to the oracle for the environment that is actually realized. Let RT denote realized reward through horizon T . We assume that its expectation and the oracle value below are finite. The main theorem imposes no pathwise reward ceiling. A finite gross bound is used only in Lemma 2 to evaluate the noncommitment regret profile in deterministic examples. The value of a policy 

π in environment θ is 

JT (π; θ) := Eπθ [RT ]. (8) The parameter-aware safe oracle knows θ and is constrained only by safety under that environment: 

J⋆T (θ) := sup 

> π∈ΠT
> safe ({θ})

JT (π; θ). (9) Regret is 

RT (π; θ) := J⋆T (θ) − JT (π; θ). (10) Because Π T

> safe

(Θ 0) ⊆ ΠT

> safe

({θ}) for every θ ∈ Θ0, the regret of a uniformly safe policy is nonnegative. 

Definition 2 (Safe oracle attainability) . Let 

RT (Θ 0) := inf 

> π∈ΠT
> safe (Θ 0)

sup 

> θ∈Θ0

RT (π; θ).

For a positive comparison scale aT , the oracle is safely attainable at scale aT if RT (Θ 0) = o(aT ). Average-regret learnability corresponds to aT = T .The scale matters: a lower bound of order √T can rule out closure of a Θ( √T ) oracle advantage without ruling out o(T ) average regret. 52.2 Commitment and information before commitment 

Fix an ordered pair ( θ, θ ′) ∈ Θ20. The first environment is the target: leaving the common regime may be valuable there. The second is an incompatible alternative: the same decision is not safely recoverable there. These roles are relative to the decision under study; they do not impose a global ordering on the model class. In the deterministic constrained systems used below, let Uviab  

> t

(h; ϑ) denote the actions available after history h for which at least one continuation remains hard-safe under ϑ through the horizon. 

Definition 3 (Pairwise commitment) . For the ordered pair ( θ, θ ′), define 

Dθ|θ′ 

> t

:= (h, u ) : u / ∈ U viab  

> t

(h; θ′) . (11) For a policy π, let 

τ π := inf t ≤ T : ( Ht−1, U t) ∈ D θ|θ′ 

> t

, (12) with τ π = T + 1 if the set is empty, and define 

Aπ := {τ π ≤ T }. (13) Thus commitment is the first action after which no hard-safe continuation remains under the incom-patible alternative. Because membership in Dθ|θ′ 

> t

is determined by ( Ht−1, U t), the commitment decision is made before the observation Zt generated by that action is available. For a general stochastic model, the same stopping-time construction may be used with an application-specific predictable set Dθ|θ′ 

> t

; its safety consequence is then quantified by bounding the commitment allowance of Definition 5 below. No general chance-constrained viability operator is needed for the lower bound. 

Finite-headroom commitment. Recall from Section 1 the controller chooses ut ∈ [0 , U ], receives reward 

rt = ut, and the safety margin evolves as dt+1 = dt − θu 2 

> t

, d1 = d0 > 0. For an ordered pair 0 ≤ θb < θ d,write Bd := Bθd = d0/θ d. Let 

Et−1(h) := 

> t−1

X

> s=1

u2

> s

.

Since future commands can be set to zero, for ϑ > 0, 

Uviab  

> t

(h; ϑ) = 

"

0, min 

(

U, 

s d0

ϑ − Et−1(h)



> +

)# 

. (14) For the ordered pair ( θb, θ d), commitment is therefore the first exit from the dangerous-device budget: 

Dθb|θd 

> t

= (h, u ) : Et−1(h) + u2 > B d . (15) 

# 3 Fundamental Pre-Commitment Limits 

The lower bound uses three quantities: commitment probability under the alternative, the regret cost of noncommitment under the target, and the information available before commitment. We define each quantity directly. 

## 3.1 Binary target versus incompatible alternative 

The committing action is chosen before its observation is seen, so the record on which information is measured should contain the committing action but not its outcome. We therefore use a stopping time. Let Ft := 

σ(Ht−1, U t) be the σ-algebra generated by the learner-visible history and the time-t action but not the time-t

6observation Zt. Because whether ( Ht−1, U t) ∈ D θ|θ′ 

> t

is decided by Ut before Zt is seen, the commitment set is Ft-measurable, and hence τ π is an {F t}-stopping time. Let Fτ π be the associated stopped σ-algebra. The stopped record 

SπT := 



τ π ∧ (T + 1) , H (τ π −1) ∧T , eU π 

, eU π := 

(

Uτ π , τ π ≤ T, 

†, τ π = T + 1 , (16) generates Fτ π ; the placeholder † marks the absence of a committing action. Let P π, pre  

> θ,T

denote the law of SπT

under θ, i.e. the restriction of the physical law to Fτ π .The commitment event Aπ = {τ π ≤ T } is determined by the stopped record. The physical law and the stopped law therefore assign it the same probability, written P πθ (Aπ ). We apply change of measure to the stopped laws and evaluate safety under the full physical laws. For later use, define the one-step conditional divergence kl θ,θ ′ 

> t

:= DKL (Qθ,t (· | Ht−1, U t) ∥ Qθ′,t (· | Ht−1, U t)) . (17) 

Definition 4 (Pairwise pre-commitment information) . The maximum information that can be collected safely before commitment is 

Ipre (T ; θ, θ ′) := sup 

> π∈ΠT
> safe ({θ,θ ′})

DKL 



P π, pre  

> θ,T

P π, pre 

> θ′,T



. (18) Its horizon-uniform capacity is 

βpre (θ, θ ′) := sup  

> T≥1

Ipre (T ; θ, θ ′). (19) 

Lemma 1 (Stopped controlled-KL identity) . Assume Qθ,t (· | h, u ) ≪ Qθ′,t (· | h, u ) for every pre-commitment history-action pair. Then, for every causal policy, 

DKL 



P π, pre  

> θ,T

P π, pre 

> θ′,T



= Eπθ

" TX

> t=1

1{t < τ π }kl θ,θ ′

> t

#

. (20) 

Proof. Since τ π is an {F t}-stopping time with Ft = σ(Ht−1, U t), the event {t < τ π } is Ft-measurable, hence determined by ( Ht−1, U t) before Zt is seen. Form the likelihood ratio of the stopped record under θ against 

θ′. The policy kernels πt(· | Ht−1) are common to both environments and cancel, including the kernel that generates the committing action Uτ π ; only the observation kernels Qθ,t versus Qθ′,t survive, and the stopping time contributes no additional likelihood factor. If τ π = m ≤ T , the stopped record contains observations only through time m − 1, so the log-likelihood ratio is the sum of one-step observation contributions for t < m ; if no commitment occurs, the sum runs through t = 1 , . . . , T . In both cases the terms present are exactly those on {t < τ π }. Conditioning the t-th term on ( Ht−1, U t) gives kl θ,θ ′ 

> t

by definition (17), and taking expectations under θ yields (20). 

Finite-headroom information. After choosing ut, the controller observes yt = θu t + ξt, where ξt ∼N (0 , σ 2). The one-step divergence is kl θb,θ d 

> t

= DKL 

 N (θbut, σ 2) N (θdut, σ 2) = (θd − θb)2u2

> t

2σ2 . (21) Before commitment, P  

> t<τ π

u2 

> t

≤ Bd = d0/θ d. Hence Lemma 1 gives 

Ipre (T ; θb, θ d) ≤ βth := (θd − θb)2d0

2σ2θd

. (22) This bound does not depend on T .7Definition 5 (Commitment allowance) . For an ordered pair ( θ, θ ′) with a nonempty uniformly safe class, define 

αT (θ, θ ′) := sup 

> π∈ΠT
> safe ({θ,θ ′})

P πθ′ (Aπ ). (23) We write αT when the pair is clear. Any upper bound ¯ αT ≥ αT can replace it in the bounds below. In particular, if 

Aπ ⊆ Safe cθ′,T P πθ′,T -a.s. for every uniformly safe policy, then αT ≤ ηT .

Finite-headroom device. Crossing the θd-safe budget gives 

dτ π +1 = d0 − θd

X 

> s≤τπ

u2 

> s

< 0under θd. Hence commitment implies a safety violation and αT (θb, θ d) ≤ ηT .

## 3.2 The noncommitment regret profile 

The stopped information bound gives a lower bound on the probability of not committing under the target. The next definition records the exact regret cost of that probability. 

Definition 6 (Noncommitment regret profile) . For an ordered pair ( θ, θ ′) and q ∈ [0 , 1], define 

GT (θ, θ ′; q) := inf   

> μ∈ΠT
> safe ({θ,θ ′})
> Pμθ(( Aμ)c)≥q

RT (μ; θ), (24) with the convention inf ∅ = + ∞. Equivalently, whenever the constraint set is nonempty, 

GT (θ, θ ′; q) = J⋆T (θ) − sup   

> μ∈ΠT
> safe ({θ,θ ′})
> Pμθ(( Aμ)c)≥q

JT (μ; θ). (25) The function q 7 → G T (θ, θ ′; q) is nonnegative and nondecreasing. This definition imposes no pathwise reward ceiling. In a stochastic-reward problem, the profile may be bounded directly in expectation. The following lemma gives a convenient pathwise bound for the determin-istic examples. 

Lemma 2 (Pathwise bound on the profile) . Suppose there are constants 

0 ≤ VT (θ, θ ′) ≤ BT (θ) ≤ Rmax ,T < ∞

such that every μ ∈ ΠT

> safe

({θ, θ ′}) satisfies, P μθ,T -almost surely, 

RT ≤



VT (θ, θ ′), on (Aμ)c ∩ Safe θ,T ,BT (θ), on Aμ ∩ Safe θ,T ,Rmax ,T , always. Then, for every q ∈ [0 , 1] ,

GT (θ, θ ′; q) ≥

h

J⋆T (θ) − BT (θ) +  BT (θ) − VT (θ, θ ′)q

−  Rmax ,T − VT (θ, θ ′)ηT

i

> +

. (26) 8Proof. Fix a feasible policy μ in (24), and write 

p := P μθ (( Aμ)c), f := P μθ,T (Safe cθ,T ).

Let x := P μθ,T (( Aμ)c ∩ Safe θ,T ). Then x ≥ p − f . The pathwise bounds give 

JT (μ; θ) ≤ VT x + BT (1 − f − x) + Rmax ,T f

≤ BT − (BT − VT )p + ( Rmax ,T − VT )f

≤ BT − (BT − VT )q + ( Rmax ,T − VT )ηT ,

where the arguments ( θ, θ ′) have been suppressed. Subtracting from J⋆T (θ) and taking the infimum over feasible policies gives (26). A useful simpler bound is 

GT (θ, θ ′; q) ≥ [GT (θ, θ ′)q − ζT (θ, θ ′)] + , q ∈ [0 , 1] . (27) The quantities GT and ζT are derived from the system; they are not assumptions of the main theorem. 

Corollary 1 (Pathwise verification in deterministic systems) . Suppose J⋆T, hard (θ) is achieved by a hard-safe policy and bounds the reward of every hard-safe trajectory. Suppose also that every safe noncommitting trajectory earns at most VT, com (θ, θ ′) and that 0 ≤ R T ≤ Rmax ,T . Then 

GT (θ, θ ′; q) ≥

h

J⋆T (θ) − J⋆T, hard (θ) + GT (θ, θ ′)q

−  Rmax ,T − VT, com (θ, θ ′)ηT

i

> +

, (28) 

where 

GT (θ, θ ′) := J⋆T, hard (θ) − VT, com (θ, θ ′). (29) 

Since every hard-safe policy is chance-safe, J⋆T (θ) ≥ J⋆T, hard (θ). Thus (27) holds with this GT and ζT =

Rmax ,T ηT .

These conditions hold in the deterministic examples in this paper. For stochastic rewards, the main theorem still applies; one bounds GT (θ, θ ′; q) directly in expectation. 

Remark 3 (Intuition) . The pathwise bounds involve four values, ordered as 

VT, com (θ, θ ′) ≤ J⋆T, hard (θ) ≤ J⋆T (θ) ≤ Rmax ,T .Rmax ,T is the physical ceiling: the most any trajectory can earn, safe or not. J⋆T (θ) is the benchmark: the best chance-ηT -safe value with θ known. J⋆T, hard (θ) is the best value with θ known and no violation tolerance; it enters for a pathwise reason: on Safe θ,T every realized trajectory is hard-safe, so in a deterministic-reward system its reward cannot exceed J⋆T, hard (θ), whereas the chance-safe value is an expectation and bounds no single path. Using J⋆T, hard ≤ J⋆T only makes the regret bound conservative. VT, com (θ, θ ′) is the recoverable value: the most a trajectory can earn under θ while retaining a hard-safe continuation under θ′, that is, while noncommitting. The gap GT = J⋆T, hard − VT, com is the portion of the target’s value reachable only by committing. The linear profile bound (27) reads: forcing noncommitment probability q forfeits at least a q-fraction of this gap. On the finite-headroom device, Rmax ,T = U T (full command every round), 

J⋆T, hard (θb) = min {U T, √T B b} (spend the true budget), VT, com = min {U T, √T B d} (spend only the worn budget); GT is the reward available only beyond the worn budget. 9Finite-headroom device. For the target θb and alternative θd,

J⋆T, hard (ϑ) = min {U T, pT B ϑ}, B0 = + ∞,

and every safe noncommitting trajectory earns at most 

VT, com (θb, θ d) = min {U T, pT B d}.

Thus 

GT (θb, θ d) = min {U T, pT B b} − min {U T, pT B d}. (30) Since Rmax ,T = U T , Corollary 1 gives 

GT (θb, θ d; q) ≥ [GT (θb, θ d)q − U T η T ]+ . (31) 

## 3.3 The binary limit 

For p, q ∈ [0 , 1], let 

dbin (p∥q) := p log pq + (1 − p) log 1 − p

1 − q ,

with the usual extended-value conventions. For α ∈ [0 , 1) and D ∈ [0 , ∞], define 

ψα(D) := inf {p ∈ [0 , 1 − α] : dbin (p∥1 − α) ≤ D} . (32) Set ψ1(D) := 0 and ψα(∞) := 0. The function ψα is nonincreasing. It is the smallest target noncommitment probability compatible with divergence D and alternative commitment probability at most α.

Theorem 1 (Binary pre-commitment limit) . Fix an ordered pair (θ, θ ′) whose uniformly safe class is nonempty. For π ∈ ΠT

> safe

({θ, θ ′}), define 

DπT := DKL 



P π, pre  

> θ,T

P π, pre 

> θ′,T



, aπT := P πθ′ (Aπ ).

Then 

RT (π; θ) ≥ G T

 θ, θ ′; ψaπT (DπT ) . (33) 

Consequently, 

RT (π; θ) ≥ G T

 θ, θ ′; ψαT (θ,θ ′)(Ipre (T ; θ, θ ′))  . (34) 

Since 

ψα(D) ≥

 12 e−D − α



> +

,

this implies the explicit bound 

RT (π; θ) ≥ G T θ, θ ′;

 12 e−Ipre (T ;θ,θ ′) − αT (θ, θ ′)



> +

!

. (35) 

If the linear profile bound (27) has been verified, then 

RT (π; θ) ≥



GT (θ, θ ′)

 12 e−Ipre (T ;θ,θ ′) − αT (θ, θ ′)



− ζT (θ, θ ′)



> +

. (36) 

If, in addition, βpre (θ, θ ′) < ∞, αT (θ, θ ′) → 0, and ζT (θ, θ ′) = o(GT (θ, θ ′)) , then 

lim inf   

> T→∞

inf 

> π∈ΠT
> safe ({θ,θ ′})

sup ϑ∈{ θ,θ ′} RT (π; ϑ)

GT (θ, θ ′) ≥ 12 e−βpre (θ,θ ′). (37) 10 Interpretation. Equation (34) is the exact causal reduction; Equation (35) is a convenient explicit relax-ation, and Equation (36) follows after inserting the linear noncommitment-profile bound. Safety controls the commitment probability under the alternative, the stopped experiment controls how different the target com-mitment probability can be, and the noncommitment profile converts the remaining target-side probability into regret. 

Proof. Let 

p := P πθ (( Aπ )c), q := P πθ′ (( Aπ )c) = 1 − aπT .

Data processing for the event ( Aπ )c gives 

dbin (p∥q) ≤ DπT . (38) If p ≤ q, the definition of ψ gives p ≥ ψaπT (DπT ); if p > q , the same inequality is immediate. The policy π is therefore feasible in the definition of the profile at this value, which proves (33). For the uniform bound, Definition 5 gives q ≥ 1−αT (θ, θ ′). If p ≤ 1−αT , then dbin (p∥1−αT ) ≤ dbin (p∥q); if p > 1 − αT , the same lower bound is immediate. Hence 

p ≥ ψαT (θ,θ ′)(DπT ).

Since 

DπT ≤ Ipre (T ; θ, θ ′)and ψα is nonincreasing, (34) follows. Bretagnolle–Huber gives 

ψα(D) ≥

 12 e−D − α



> +

.

This yields (35). Substituting (27) gives (36). The asymptotic statement follows from the horizon-uniform information bound and the stated limits. The explicit term in (35) is positive when 

Ipre (T ; θ, θ ′) < log 12αT (θ, θ ′) .

For αT = T −2, the threshold is 2 log T − log 2. 

Finite-headroom device. Using αT (θb, θ d) ≤ ηT , (22), and (31), Theorem 1 gives 

RT (π; θb) ≥



GT (θb, θ d)

 12 e−βth − ηT



− U T η T



> +

. (39) With ηT = T −2, fixed 0 < θ b < θ d, and an inactive per-step cap, GT (θb, θ d) = Θ( √T ). At θb = 0, 

GT (0 , θ d) = U T − O(√T ) = Θ( T ), so the same information bound gives linear regret. 

Corollary 2 (Information required for a target regret) . Suppose the linear profile bound (27) holds. If a uniformly safe policy satisfies RT (π; θ) ≤ rT , then 

Ipre (T ; θ, θ ′) ≥

log 12



αT (θ, θ ′) + rT +ζT (θ,θ ′) 

> GT(θ,θ ′)



> +

. (40) 

Thus rT = o(GT ) requires unbounded pre-commitment information whenever αT → 0 and ζT = o(GT ).Proof. Apply (36) and rearrange. 11 Remark 4 (Other risk constraints) . The theorem uses safety only through the commitment allowance αT .Any risk constraint that bounds P πθ′ (Aπ ) can be used. For example, if a nonnegative loss satisfies LT ≥

mT 1{Aπ } under θ′ and CVaR β (LT ) ≤ cT , then 

P πθ′ (Aπ ) ≤ cT

mT

.

Specifications that budget cumulative violations rather than bounding the violation probability under every plausible model need not imply a small commitment allowance and therefore fall outside the premise of the theorem; in the safe linear bandit setting, [9] show that such a budget purchases exactly the boundary exploration that uniform chance safety forbids, and near-optimal learning is then possible. 

Remark 5 (Generality) . The theorem uses only the two stopped laws and data processing. It does not require Markov dynamics, Gaussian noise, linearity, full-state observation, or absolute continuity. Absolute continuity is needed only for the sum identity in Lemma 1. An application must define a predictable commitment rule and bound αT , Ipre , and GT .

## 3.4 Continuous classes: optimizing over the alternative 

The binary theorem applies to every incompatible pair in Θ 0. If Θ 0 is continuous, the pairwise bound can be maximized over the alternative. 

Corollary 3 (Worst-case alternative and minimax bound) . Let Θ0 be arbitrary. For a target θ ∈ Θ0, let 

A(θ) ⊆ Θ0 contain alternatives with predictable commitment rules. Each alternative may use its own rule. Then every π ∈ ΠT

> safe

(Θ 0) satisfies 

RT (π; θ) ≥ sup 

> θ′∈A (θ)

GT

 θ, θ ′; ψαT (θ,θ ′)(Ipre (T ; θ, θ ′))  , (41) 

and 

RT (Θ 0) ≥ sup 

> θ∈Θ0

sup 

> θ′∈A (θ)

GT

 θ, θ ′; ψαT (θ,θ ′)(Ipre (T ; θ, θ ′))  . (42) 

Proof. Fix θ′ ∈ A (θ). A policy safe on Θ 0 is safe on {θ, θ ′}, so Theorem 1 applies. Taking the supremum over alternatives gives (41). Taking the supremum over targets and then the infimum over uniformly safe policies gives (42). If each pair satisfies the linear profile bound (27), Corollary 3 gives the explicit lower bound 

RT (π; θ) ≥ sup 

> θ′∈A (θ)



GT (θ, θ ′)

 12 e−Ipre (T ;θ,θ ′) − αT (θ, θ ′)



− ζT (θ, θ ′)



> +

. (43) The optimization balances value and distinguishability. Nearby alternatives are harder to distinguish but may have a smaller value gap. 

The finite-headroom device on a continuum. Let Θ 0 = [0 , ¯θ], with Bϑ = d0/ϑ for ϑ > 0 and 

B0 = + ∞. For a target θ and alternative θ′ ∈ (θ, ¯θ], commitment is the first exit from the θ′-budget. Then 

αT (θ, θ ′) ≤ ηT , ζT (θ, θ ′) = U T η T ,GT (θ, θ ′) = min {U T, pT B θ } − min {U T, pT B θ′ },

and 

Ipre (T ; θ, θ ′) ≤ β(θ, θ ′) := (θ′ − θ)2d0

2σ2θ′ .

12 Ignoring the vanishing ηT terms, define 

ST (θ, θ ′) := 12 e−β(θ,θ ′)GT (θ, θ ′).

Interior target. Let θ > 0 and suppose the per-step cap is inactive. Then 

ST (θ, θ ′) = 12

pT d 0 fθ (θ′), fθ (θ′) := e−β(θ,θ ′) θ−1/2 − θ′− 1/2.

The function fθ does not depend on T , so the optimized lower bound is Θ( √T ). 

Boundary target. For θ = 0, 

GT (0 , θ ′) = U T − pT d 0/θ ′, β(0 , θ ′) = θ′d0

2σ2 .

Optimizing ST (0 , θ ′) gives 

θ′⋆ =

 σ4

U 2d0

1/3

T −1/3(1 + o(1)) , β⋆ = 12

 d0

σU 

2/3

T −1/3(1 + o(1)) , (44) and sup 

> θ′∈(0 ,¯θ]

ST (0 , θ ′) = U T 

2 − 34

 d20Uσ2

1/3

T 2/3 + o(T 2/3). (45) Thus 

RT ([0 , ¯θ]) ≥ U T 

2 (1 − o(1)) . (46) The optimizing alternative approaches the boundary target at rate T −1/3.

## 3.5 One policy, many alternatives: safe lists 

Remark 6 (One policy against many alternatives) . The binary theorem is pairwise. Its information profile may use a different policy for each alternative. If the learner must choose among M model-specific regimes, one policy must gather information about all M possibilities. The relevant quantity is 

I(M )pre (T ; Θ M ) := sup  

> π∈ΠT
> safe (Θ M)

Iπ (J; SπT ), J ∼ Unif[ M ]. (47) When partial identification already improves control, the target is a certified candidate list rather than exact classification. 

Proposition 1 (Information required for a safe candidate list) . Let J ∼ Unif[ M ], let S be the information available when a nonempty list C(S) ⊆ [M ] is selected, and fix L ∈ { 1, . . . , M − 1}. Define 

εL := P  J / ∈ C(S) or |C(S)| > L . (48) 

Then 

I(J; S) ≥ (1 − εL) log ML − h2(εL), (49) 

where h2(p) := −p log p − (1 − p) log(1 − p). Consequently, if 

P (J / ∈ C(S)) ≤ η, P (|C(S)| > L ) ≤ δ, η + δ ≤ 12 ,

then 

I(J; S) ≥ (1 − η − δ) log ML − h2(η + δ). (50) 13 The proof is in the appendix. The case L = M gives no reduction, L = 1 gives exact specialization, and 1 < L < M gives partial specialization. 

Remark 7 (Scalar continuous candidate sets) . Let J ∼ Unif[ θmin , θ max ], with D = θmax − θmin . Suppose the learner returns a set C(S) such that 

P  J / ∈ C(S) or diam C(S) > ℓ  ≤ ε ≤ 12 .

Then 

I(J; S) ≥ (1 − ε) log Dℓ − h2(ε), ℓ ≥ D exp 



− I(J; S) + h2(ε)1 − ε



. (51) This is a converse for a scalar parameter on an interval. Achieving the bound still requires a safe sensing policy. 

## 3.6 How to use the limits in an application 

For a new system: 1. define a predictable commitment event; 2. bound the commitment allowance αT under the incompatible alternative; 3. compute or lower-bound the noncommitment regret profile GT ; and 4. bound the stopped KL divergence, or the stopped mutual information in a multi-alternative problem. The theorem then gives a regret lower bound. A positive result requires a single uniformly safe policy that gathers the needed information and moves safely to the selected regime. 

# 4 Mechanisms and Examples 

Section 3.6 gives the application procedure for the lower bound. Here we study two mechanisms that bound the information available before commitment: finite expenditure and a finite sensing window. We apply them to a constrained linear-regulation problem and an unknown-location problem. The first yields linear regret; the second yields safe partial adaptation through a certified candidate list. 

## 4.1 Finite expenditure before commitment 

Lemma 3 (Finite pre-commitment expenditure) . Fix an ordered pair (θ, θ ′) and its commitment time τ π .Suppose there is a nonnegative predictable quantity St = S(Ht−1, U t), constants κ < ∞ and q ≥ 1, and a number BT < ∞ such that every π ∈ ΠT

> safe

({θ, θ ′}) satisfies, pathwise, 

> T

X

> t=1

1{t < τ π }St ≤ BT , (52) 

and, whenever t < τ π ,

kl θ,θ ′ 

> t

≤ κS qt . (53) 

Then 

Ipre (T ; θ, θ ′) ≤ κB qT . (54) 

In particular, if sup T BT < ∞, then βpre (θ, θ ′) < ∞.

The proof is in Appendix A.1. Since P  

> t<τ π

Sqt ≤ (P  

> t<τ π

St)q for q ≥ 1, the pathwise expenditure bound directly bounds the stopped information. For q = 1, information is at most proportional to expenditure. For 

q > 1, splitting a finite budget among many small actions cannot produce persistent information. 14 Remark 8 (One-sided versus Signed Actions) . The conversion 

X

> t

ut ≤ B =⇒ X

> t

u2 

> t

≤ min {B2, U B }

requires ut ≥ 0. With signed inputs and only a constraint on the signed sum, cancellations can keep P 

> t

ut

bounded while P 

> t

u2 

> t

grows. This caveat is specific to the linear one-sided margin of Variant I. In Variant II, safety directly bounds the signed expenditure P

> t

(a⊤ut)2, so reversing the active loading consumes the same quadratic headroom rather than restoring it. The information ceiling therefore persists under fully signed requests and controls. 

## 4.2 Constrained linear regulation under two safety geometries 

We use a common two-channel output geometry to exhibit two finite-expenditure mechanisms in linear– quadratic regulation. Variant I uses nonnegative reserve deployment and a linear security margin; its information ceiling follows from an order mismatch between linear expenditure and quadratic Gaussian information. Variant II permits genuinely signed regulation and uses a quadratic I2t-type margin. The second variant shows that the obstruction is not an artifact of one-sided actuation. 

Shared observation geometry. Let 

a ∈ { a0, a 1}, a0 = e2, a1 = e1, (55) and write ut = ( pt, q t). The regulated state xt is observed before the action. After applying ut, the controller observes 

yt = a⊤ut + ξt, ξt

> iid

∼ N (0 , σ 2). (56) The active direction and the physical margin are not directly observed. The initial margin and its update law are known, so the controller can propagate the candidate margin associated with each direction from its action history. In both variants the target is a0: the p-channel is then invisible to the active security constraint, whereas under a1 that same channel spends the margin. In each variant the controller minimizes a quadratic cost and is compared with the parameter-aware chance-safe oracle. Stable dynamics and bounded inputs give a uniform stage-cost ceiling. Subtracting the stage cost from this ceiling converts the example to the bounded-reward convention without changing regret, with an O(T η T ) value-profile remainder. 

Variant I: one-sided reserve deployment. Let 

ut = ( pt, q t) ∈ [0 , U ]2, xt+1 = ρx t + w − pt, x1 = ¯ x := w

1 − ρ , (57) where 0 < ρ < 1 and 0 < w ≤ U , and define 

C(1)  

> T

(π; a) := EπaTX

> t=1

x2 

> t+1

+ λ(p2 

> t

+ q2 

> t

) . (58) Let C(1) ,⋆ T (a) be the parameter-aware chance-safe oracle cost and write 

R(1)  

> T

(π; a) := C(1)  

> T

(π; a) − C(1) ,⋆ T (a).

The hidden security margin evolves as 

dt+1 = dt − a⊤ut, d1 = d0 > 0, dt ≥ 0. (59) 15 Under a0, the feasible policy pt = w, qt = 0 gives 

C(1) ,⋆ T (a0) ≤ λw 2T + O(1) . (60) Define commitment relative to a1 by 

τ π 

> 1

:= inf 

(

t ≤ T :

> t

X

> s=1

ps > d 0

)

, Aπ 

> 1

:= {τ π 

> 1

≤ T }. (61) Commitment violates safety under a1, so Pπa1 (Aπ 

> 1

) ≤ ηT . On noncommitment, P 

> t

pt ≤ d0. Since 

xt+1 = ¯ x −

> t

X

> s=1

ρt−sps,

the deviations from ¯ x sum to at most d0/(1 − ρ), and every noncommitting trajectory satisfies 

> T

X

> t=1

x2 

> t+1

≥ ¯x2T − 2¯ xd 0

1 − ρ .

Thus the common-regime gap can be chosen so that 

G(1)  

> T

≥ (¯ x2 − λw 2)T − O(1) , (62) and hence G(1)  

> T

= Θ( T ) whenever λw 2 < ¯x2. The input box also gives 

|xt| ≤ max 



¯x, U − w

1 − ρ



,

which verifies the bounded stage-cost claim above. Under a0 and a1, the residual means are qt and pt, respectively, so kl t(a0, a 1) = (qt − pt)2

2σ2 . (63) Before commitment, P pt ≤ d0 pathwise. On the target-safe event, P qt ≤ d0; on its complement, which has probability at most ηT , only the input box remains. Splitting the stopped-KL expectation over these events gives 

I(1) pre (T ; a0, a 1) ≤ min {4d20, 2U d 0}

2σ2 + T U 2ηT

2σ2 . (64) For ηT ≤ T −2, the profile is uniformly bounded, and Theorem 1 gives 

R(1)  

> T

(π; a0) = Ω( T ).

Continuous cross-loading. The same one-sided model also yields a useful continuous-class calculation. Replace the binary loading class by 

aγ = ( γ, 1) ⊤, γ ∈ [0 , ¯γ], (65) with target γ = 0, so that 

dt+1 = dt − qt − γp t, yt = qt + γp t + ξt. (66) For an alternative γ > 0, let commitment be the first time P

> s≤t

(qs + γp s) > d 0. This is a safety violation under γ, so the commitment allowance is at most ηT . Strictly before commitment, 

X

> t

pt ≤ d0

γ .

16 The q-channel cancels from the likelihood ratio, and the same budget also bounds the target value on noncommitment. Therefore 

Ipre (T ; 0 , γ ) ≤ γU d 0

2σ2 , G(1)  

> T

(γ) ≥ cT − Bγ − O(1) , (67) where 

c := ¯ x2 − λw 2 > 0, B := 2¯ xd 0

1 − ρ .

Choose the horizon-dependent alternative γT = T −1/2. Then Ipre (T ; 0 , γ T ) = O(T −1/2) and G(1)  

> T

(γT ) ≥

cT − O(√T ). For ηT = T −2,

ψηT (Ipre (T ; 0 , γ T )) = 1 − o(1) ,

so Corollary 3 gives 

R(1)  

> T

(π; 0) ≥ cT − o(T ). (68) Thus even a cross-loading that vanishes as T −1/2 can block asymptotically the entire linear regulation advantage. 

Variant II: signed regulation with quadratic headroom. Let st ∈ {− 1, +1 } be an exogenous up/down regulation request, observed before the action, included in the pre-action history, and indepen-dent of a. Take 

ut = ( pt, q t) ∈ [−U, U ]2, xt+1 = ρx t + ws t − pt − qt, x1 = 0 , (69) where 0 ≤ ρ < 1 and 0 < w ≤ U , with cost 

C(2)  

> T

(π; a) := EπaTX

> t=1

x2 

> t+1

+ λ(p2 

> t

+ q2 

> t

) . (70) Let C(2) ,⋆ T (a) be the parameter-aware chance-safe oracle cost and write 

R(2)  

> T

(π; a) := C(2)  

> T

(π; a) − C(2) ,⋆ T (a).

The hidden margin now evolves as 

dt+1 = dt − (a⊤ut)2, d1 = d0 > 0, dt ≥ 0. (71) Under a0, the feasible policy pt = ws t, qt = 0 keeps xt = 0 and certifies C(2) ,⋆ T (a0) ≤ λw 2T . Define 

τ π 

> 2

:= inf 

(

t ≤ T :

> t

X

> s=1

p2 

> s

> d 0

)

. (72) Commitment violates safety under a1, so Pπa1 (τ π 

> 2

≤ T ) ≤ ηT .On a target-safe, noncommitting trajectory, 

∥p1: T + q1: T ∥2 ≤ 2pd0.

With 

et := ws t − pt − qt = xt+1 − ρx t,

we obtain 

∥e1: T ∥2 ≥  w√T − 2pd0



> +

, ∥e1: T ∥2 ≤ (1 + ρ)∥x2: T +1 ∥2.

17 Therefore the common-regime gap satisfies 

G(2)  

> T

≥

 w√T − 2√d0

2+

(1 + ρ)2 − λw 2T



> +

. (73) If λ < (1 + ρ)−2, then G(2)  

> T

= Θ( T ). Moreover, 

|xt| ≤ w + 2 U

1 − ρ ,

so the stage cost is uniformly bounded here as well. The one-step divergence remains kl t(a0, a 1) = (qt − pt)2

2σ2 .

Before τ π 

> 2

, P p2 

> t

≤ d0 pathwise. On the target-safe event, P q2 

> t

≤ d0; on its complement, the input box gives P(qt − pt)2 ≤ 4U 2T . Hence 

I(2) pre (T ; a0, a 1) ≤ 2d0

σ2 + 2U 2T η T

σ2 . (74) 

Corollary 4 (Linear regret with an unknown active constraint direction) . For i ∈ { 1, 2}, let ΠT,i  

> safe

denote the policies that are uniformly chance-ηT safe for {a0, a 1} in Variant i. Suppose that all system parameters are fixed independently of T and that ηT ≤ T −2.For Variant I, assume g1 := ¯ x 2 − λw 2 > 0, β1 := min {4d20, 2U d 0} 

> 2σ2

. For Variant II, assume g2 := w2 (1 + 

ρ)−2 − λ > 0, β2 := 2d0 

> σ2

. Then, for i ∈ { 1, 2},

lim inf   

> T→∞

inf 

> π∈ΠT ,i
> safe

R(i) 

> T

(π; a0)

T ≥ gi

2 e−βi > 0.

In particular, there are constants ci > 0 and Ti < ∞, depending only on the fixed system parameters, such that every π ∈ ΠT,i  

> safe

satisfies 

R(i) 

> T

(π; a0) ≥ ciT, T ≥ Ti.

Thus both safety geometries force linear oracle regret under uniform chance safety. 

The proof appears in Appendix. 

Role of signed actuation. Variant I isolates the one-sided order mismatch and supplies the local contin-uous cross-loading calculation. Variant II shows that neither the information ceiling nor the linear-regret conclusion depends on one-sided actuation: the request and both reserve injections may change sign, while the active loading spends quadratic energy. Sign reversal can undo a linear signed expenditure, but it cannot undo an even expenditure: reversing the active loading pays the quadratic headroom a second time. In both variants, full observation of the regulated state is insufficient because the active security channel is visible only through the input-dependent residual. A persistent direct measurement of the margin would change the stopped information profile and can remove the obstruction. 

## 4.3 Finite sensing bandwidth before an evolving deadline 

The linear-regulation example limits information because informative control spends a hidden safety margin. A different obstruction occurs when the state evolves toward a boundary while the learner must search across many possible sensing locations. 18 Lemma 4 (Finite pre-commitment sensing window) . Consider a labeled problem with J ∼ Unif[ M ]. Suppose that, under every policy, at most NT label-dependent observations can occur before specialization or an absorbing fallback, and each such observation satisfies 

Iπ (J; Zt | Ht−1, U t, t < τ π ) ≤ cobs . (75) 

Then 

I(M )pre (T ; Θ M ) ≤ NT cobs . (76) 

For the one-location Gaussian sensor 

Zt = μ1{At = J} + ξt, ξt ∼ N (0 , σ 2), (77) 

where At ∈ [M ] is chosen adaptively, one may take 

cobs = min 



log 2 , μ2

2σ2



. (78) The proof is in Appendix A.3. This is the one-policy search effect identified in Remark 6: one policy must search over all labels, while each round can inspect only one location. The same bandit-feedback search structure governs quickest changepoint detection with adaptive sensing [10], where the delay–false-alarm tradeoff arises. 

## 4.4 Unknown disturbance location: safe lists and partial adaptation 

There are M components and one unknown affected location J ∈ [M ]. Fix an integer 1 ≤ N < T . During diagnostic operation, 

Xi,t +1 = Xi,t + ∆ 1{i = J}, Xi, 1 = 0 , i ∈ [M ], (79) and safety requires Xi,t ≤ H for all i, t . Write 

H = N ∆. (80) Thus full diagnostic operation is safe for N rounds; on the next round the affected component would cross the boundary unless mitigation is selected. At each diagnostic round the learner chooses one sensor At ∈ [M ]and observes 

Zt = μ1{At = J} + ξt, ξt

> iid

∼ N (0 , σ 2). (81) Diagnostic operation earns unit reward per round. At the deadline the learner selects a nonempty certified list CN ⊆ [M ]. The post-diagnostic controller divides both throughput and mitigation across the retained candidates: 

q(CN ) = 1

|C N | , ui(CN ) = ∆

|C N | 1{i ∈ C N }. (82) After the list is chosen, 

Xi,t +1 = Xi,t + ∆ q(CN )1{i = J} − ui(CN ). (83) If J ∈ C N , the affected state is arrested. If J / ∈ C N , it violates the constraint on the next round; we treat this as an absorbing zero-reward failure. Thus safety of the list policy is equivalent to retaining the true location. Proposition 1 and Lemma 4 give the following list-size bound. If a chance-safe policy returns a list CN

with P(J / ∈ C N ) ≤ η and P(|C N | > L ) ≤ δ, then necessarily 

N c sens ≥ (1 − η − δ) log ML − h2(η + δ), csens := min 



log 2 , μ2

2σ2



. (84) 19 Equivalently, for a deterministic size cap and failure probability at most η ≤ 1/2, 

L ≥ M exp 



− N c sens + h2(η)1 − η



. (85) Finite pre-commitment information can therefore reduce the uncertainty set even when it is insufficient for exact localization. One uniformly safe elimination rule is the following. For candidate j, let Λj,t := X

> s≤t:As=j

log ϕ0,σ (Zs)

ϕμ,σ (Zs) , (86) where ϕm,σ is the density of N (m, σ 2), and set 

Ct := 



j : Λ j,t < log 1

ηT



. (87) Under the true location, exp(Λ J,t ) is a likelihood-ratio martingale, so Ville’s inequality gives 

PJ

 J / ∈ C t for some t ≤ N  ≤ ηT . (88) If the set in (87) is empty, the learner returns [ M ]. Otherwise, it can inspect, for example, the least-sampled surviving location. This rule is not claimed to be optimal; it only shows that partial certification can be uniformly safe. Under the reward convention above, the robust, list-adaptive, and oracle values are 

Jrob  

> T

= N + T − NM , (89) 

Jlist  

> T

= N + ( T − N )E

 1{J ∈ C N }|C N |



, (90) 

Joracle  

> T

= T. (91) The list policy improves on robust control when 

E

 1{J ∈ C N }|C N |



> 1

M ,

and it reaches the oracle only when CN = {J} almost surely. Whenever the first inequality is strict and exact localization does not hold, 

Jrob  

> T

< J list  

> T

< J oracle  

> T

. (92) Section 7.3 evaluates this rule over a range of sensing windows. 

# 5 Safe Oracle Recovery from Repeatable Experiments 

Theorem 1 gives the lower-bound certificate, and Section 4 verifies it in several systems. We now give a sufficient condition for recovery. If a safety-preserving experiment can be repeated and remains informative, a confidence-based controller can learn without leaving the safe set. The result uses three properties: 1. a conservative experiment is safe and returns the system to a common continuation state; 2. repeated experiments accumulate information about the unknown parameter; and 3. the loss in oracle value is controlled by the parameter error. This is not a converse to the lower bound. It identifies one setting in which pre-commitment information is persistent and can be used safely. 20 Example Pre-commitment informa-tion Oracle gap Consequence 

Unknown active constraint direction (linear regulation) bounded by finite margin ex-penditure. Θ( T ) linear regret under uniform safety Unknown disturbance loca-tion at most N c sens before mitiga-tion list dependent certified robust, partial, or exact adaptation Repeatable safe experiment can grow linearly model dependent oracle recovery when the transition remains safe 

Table 1: The effect of bounded information depends on the oracle gap. In a multi-alternative problem, finite information can instead determine the size of a certified safe list. 

## 5.1 Repeatable safe experiments 

Let θ ∈ [θ, θ ], 0 < θ < θ, be a fixed unknown scalar parameter. Time is divided into episodes. Let Gt−1

denote the σ-field generated by the first t − 1 episodes. The learner chooses a Gt−1-measurable conservative design parameter ϑt and executes an experiment κϑt . The experiment returns reward rt and an observation 

yt = θa t + ξt, ξt

> iid

∼ N (0 , σ 2), (93) where at is Gt−1-measurable and ξt is independent of Gt−1. Define the accumulated design information 

Vt := 

> t

X

> s=1

a2

> s

σ2 , V0 := 0 . (94) Let v⋆(θ) ∈ [0 , r max ] be the oracle value per episode. For ϑ ≤ θ, let vbnd (θ, ϑ ) be the expected value of 

κϑ under the true parameter θ.

Assumption 1 (Repeatable safe experiments) . There is a nondecreasing function ω : [0 , ∞) → [0 , r max ]with ω(0) = 0 such that: (P1) Fixed parameter. The experiments do not change θ, and the same parameter governs all episodes. (P2) Safe repeatability. For every θ ≤ ϑ ≤ θ ≤ θ, executing κϑ is safe under θ and, possibly after a fixed reset, returns the system to a common state from which the next experiment can be run safely. (P3) Value modulus. Whenever ϑ ≤ θ,0 ≤ v⋆(θ) − vbnd (θ, ϑ ) ≤ ω(θ − ϑ). (95) (P4) Bounded reward. Almost surely, 0 ≤ rt ≤ rmax , and 

Eθ [rt | G t−1] = vbnd (θ, ϑ t) whenever ϑt ≤ θ. (96) (P5) Informative initialization. When κθ is used in the first episode, a1̸ = 0 almost surely. Hence Vt > 0almost surely for every t ≥ 1. Thus conservative operation is itself a repeatable experiment. As the design parameter approaches the truth, its expected value approaches the oracle value. 21 5.2 Lower-confidence policy 

For t ≥ 1, define the weighted least-squares estimate 

bθt := 

Pts=1 asYs/σ 2

Vt

. (97) For a mixture scale ν > 0 and confidence level δ ∈ (0 , 1), let 

b(v, δ ; ν) := 



2( v + ν) log 

 √1+ v/ν δ



v2



> 1/2

, v > 0. (98) 

Lemma 5 (Information-indexed confidence sequence) . Under (93) , for every δ ∈ (0 , 1) and ν > 0,

Pθ



|bθt − θ| ≤ b(Vt, δ ; ν) for every t ≥ 1



≥ 1 − δ. (99) The proof is in Appendix B.1. The radius is indexed by the accumulated information Vt and is of order plog Vt/V t for large Vt.The controller uses the lower endpoint of this confidence sequence: 

ϑ1 := θ, ϑt := 

hbθt−1 − b(Vt−1, δ ; ν)

iθθ

, t ≥ 2, (100) where [x]θθ := min {θ, max {θ, x }} .

At episode t, it runs κϑt , observes ( rt, Y t), and updates (97). 

## 5.3 Safe recovery theorem 

The parameter-aware oracle receives v⋆(θ) per episode. In this episodic specialization, its expected regret is 

RT (π; θ) := 

> T

X

> t=1

(v⋆(θ) − Eπθ [rt]) . (101) 

Theorem 2 (Safe recovery from repeatable information) . Suppose Assumption 1 holds. Then the policy 

(100) satisfies, for every θ ∈ [θ, θ ],

P πθ,T (Safe cθ,T ) ≤ δ, (102) 

and 

RT (π; θ) ≤ rmax +

> T

X

> t=2

Eπθ [ω(2 b(Vt−1, δ ; ν))] + rmax (T − 1) δ. (103) 

Consequently, for ηT ∈ (0 , 1) , setting δ = ηT gives π ∈ ΠT

> safe

([ θ, θ ]) .

The proof is in Appendix B.2. Whenever |bθt−1 − θ| ≤ b(Vt−1, δ ; ν), the projection in (100) gives ϑt ≤ θ

and θ − ϑt ≤ 2b(Vt−1, δ ; ν). Thus (P2) gives safety and (P3) bounds the loss in that episode. Lemma 5 makes these inequalities simultaneous with probability at least 1 − δ; bounded reward controls the remaining event. 22 5.4 Rates from information growth 

Theorem 2 converts a lower bound on Vt into a regret rate. 

Corollary 5 (Polynomial information growth) . Suppose Assumption 1 holds and, uniformly over θ ∈ [θ, θ ],there are constants c−, c + > 0 and α ∈ (0 , 1] such that 

c−tα ≤ Vt ≤ c+t almost surely for every t ≥ 1. (104) 

Assume also that ω(r) ≤ Lr q for some L < ∞ and q > 0. Run the policy with δ = δT = T −2 and fixed 

ν > 0. Then 

sup 

> θ∈[θ,θ ]

RT (π; θ) = 



O LT 1−αq/ 2(log T )q/ 2 , αq < 2,O L(log T )1+ q/ 2 , αq = 2 ,O L(log T )q/ 2 , αq > 2.

(105) 

The hidden constants may depend on c−, c+, α, ν, q, and rmax , but not on T or θ.

The proof is in Appendix B.3. For example, if Vt ≍ t and ω(r) ≤ ℓr , then 

RT (π; θ) = O



ℓpT log T



. (106) If Vt ≍ t and ω(r) ≤ Lr 2, then 

RT (π; θ) = O L(log T )2 . (107) The information-growth rate and the value modulus jointly determine regret. 

## 5.5 Alignment as a sufficient condition 

Definition 7 (Safety–information alignment) . The family {κϑ}ϑ∈[θ,θ ] is aligned if the same experiment that satisfies the safety and repeatability condition (P2) also produces the observation (93). It is uniformly (ρ, ¯ρ)-aligned if every execution satisfies 0 < ρ ≤ a2

> t

σ2 ≤ ¯ρ < ∞ almost surely. (108) Under alignment, no separate excitation phase is needed: the maneuver used to preserve safety also supplies information. 

Corollary 6 (Uniform alignment) . Suppose Assumption 1 holds, the experiment family is uniformly (ρ, ¯ρ)-aligned, and ω(r) ≤ ℓr . Then the lower-confidence policy with δ = T −2 is uniformly chance-T −2 safe and 

sup 

> θ∈[θ,θ ]

RT (π; θ) = O ℓ

s

T log Tρ

!

. (109) 

The hidden constant may depend on ¯ρ, ν, and rmax .

## 5.6 Example: recursively feasible braking 

Consider a vehicle approaching a wall at p = 0. Its fixed but unknown braking efficiency satisfies 

θ ∈ [θ, θ ].

Each episode starts at p(0) = −D. The controller selects the initial speed, which is also the episode reward. Since θ ≤ θ, the reward is bounded by rmax := √2Dθ . The controller then applies braking according to ˙p(s) = v(s), ˙v(s) = −θu (s), u(s) ∈ [0 , 1] , (110) 23 Safety requires p(s) ≤ 0 until the vehicle stops. A fixed reset then returns the vehicle to the same initial state for the next episode. For known θ, maximal braking from speed v has stopping distance v2/(2 θ). The oracle entry speed is therefore 

v⋆(θ) = √2Dθ. (111) A conservative experiment uses 

v(ϑ) = √2Dϑ, (112) applies maximal braking, and records the noisy deceleration measurement 

yt = θ + ξt, ξt

> iid

∼ N (0 , σ 2). (113) Thus at = 1 in (93). 

Lemma 6 (Braking verifies alignment) . For every θ ≤ ϑ ≤ θ ≤ θ, the experiment (112) is safe under θ,returns to the common initial state after the reset, contributes Fisher information 1/σ 2, and satisfies 

0 ≤ v⋆(θ) − v(ϑ) ≤

s

D

2θ (θ − ϑ). (114) 

Corollary 7 (Safe oracle recovery for braking) . The lower-confidence policy applied to the braking instance is uniformly chance-T −2 safe and satisfies 

sup 

> θ∈[θ,θ ]

RT (π; θ) = O σ

s

Dθ

pT log T

!

. (115) Each safe braking episode supplies one new observation, so Vt = t/σ 2. Unlike the finite-expenditure mechanism in Lemma 3, the experiment can be repeated without consuming a nonrenewable safety margin. 

5.6.1 Nominal certainty equivalence at an active safety boundary 

The braking example also separates statistical consistency from trajectory safety. Suppose that, after n safe braking episodes, the controller forms the projected sample mean bθCE  

> n

:=  1

> n

Pns=1 Ys

θθ and then uses the certainty-equivalent entry speed vCE  

> n+1

:= 

q

2DbθCE  

> n

. For any θ ∈ (θ, θ ), the resulting episode is safe if and only if bθCE  

> n

≤ θ, because (vCE 

> n+1

)2

2θ = D bθCE 

> n

θ .

Since 1

n

> n

X

> s=1

Ys − θ ∼ N 



0, σ2

n



,

and projection preserves comparison with an interior value of θ,

Pθ

 (vCE 

> n+1

)2

2θ > D 



= Pθ

bθCE  

> n

> θ 



= 12for every n.More generally, consider the confidence-adjusted plug-in rule 

bθn,m := 

"

1

n

> n

X

> s=1

Ys − mn

#θθ

, vn+1 (mn) := 

q

2Dbθn,m ,

24 where mn ≥ 0. For an interior parameter, 

Pθ

 vn+1 (mn)2

2θ > D 



= 1 − Φ

 √n m n

σ



,

where Φ is the standard Gaussian distribution function. Thus a one-episode violation probability at most 

δ < 1/2 requires 

mn ≥ σ

√n Φ−1(1 − δ).

Increasing n therefore shrinks the magnitude of the certainty-equivalent overshoot, but does not shrink its probability when mn = 0. A trajectory-wide guarantee requires a time-uniform, data-dependent margin, which is the role of the confidence sequence in the lower-confidence policy. 

## 5.7 Relation to pre-commitment information 

Definition 4 and Lemma 1 identify pre-commitment information with a stopped sum of one-step divergences. In the present Gaussian model, this sum is a multiple of VT .

Proposition 2 (Pre-commitment KL for repeatable experiments) . Fix θ̸ = θ′ and a policy that is uniformly safe for {θ, θ ′} and executes experiments of the form (93) for T episodes without entering a model-specific commitment regime. Then 

DKL 



P π, pre  

> θ,T

P π, pre 

> θ′,T



= (θ − θ′)2

2 Eπθ [VT ]. (116) 

Consequently, if such a policy satisfies VT ≥ g(T ) almost surely, then 

Ipre (T ; θ, θ ′) ≥ (θ − θ′)2

2 g(T ). (117) 

Under uniform ρ-alignment, repeating the common-safe experiment κθ remains noncommitting and therefore 

Ipre (T ; θ, θ ′) ≥ ρ

2 (θ − θ′)2T. (118) The lower bound requires bounded information for every uniformly safe policy that has not committed. Here one common-safe experiment gives linear information growth, so the bounded-information obstruction is absent. The recovery theorem also shows how to use that information through a safe confidence rule and convert estimation error into value loss. 

Remark 9 (Scope) . The result treats a scalar parameter with a known order in which smaller design val-ues are conservative. It also requires repeatable experiments and a common reset or continuation state. These are sufficient conditions, not necessary ones. Vector parameters, multiple control-equivalence classes, non-resetting trajectories, and dynamic latent environments require additional transition and exploitation arguments. 

# 6 Computing Pre-Commitment Information 

Theorem 1 requires an upper bound on the information available before commitment. In general, computing this quantity is an adaptive experiment-design problem. We now give an exact reduction for a deterministic linear–Gaussian class with quadratic common-safe constraints. This section addresses only the information calculation. The commitment event and the value lost without commitment must still be derived from the control problem. Exact formulas can also show that common-safe information grows, but growth alone does not provide the safe transition required by Section 5. For the list problem in Section 3.5, pairwise certificates give a conservative bound on the corresponding multiclass information. 25 6.1 A deterministic linear–Gaussian class 

Fix a pair θ, θ ′ ∈ Θ0 and write 

g := θ − θ′.

Let UT 

> core

be the set of complete action sequences that remain in the common pre-commitment regime through horizon T .

Assumption 2 (Deterministic linear–Gaussian certificate class) . The following hold for the pair ( θ, θ ′). (D1) Under ϑ ∈ { θ, θ ′}, the observations obey the sequential model 

yt = ϑ⊤ζt(u1: t) + ξt, ξt

> iid

∼ N (0 , σ 2), (119) where ζt is a deterministic causal function, common to the two environments, and ξt is independent of the past and of the policy’s private randomization. (D2) Membership in UT 

> core

depends only on the action sequence, not on the observation noise. (D3) Every action prefix that can occur strictly before commitment under a uniformly safe policy extends to a sequence in UT

> core

. Conversely, every sequence in UT 

> core

can be played open loop by a uniformly safe policy without commitment. This class includes the consumable-resource examples in which the regressor is a known function of the controls. It does not cover general partially observed stochastic systems, where noise or feedback changes which future experiments remain feasible. For u1: T ∈ U T

> core

, define ΦT (u1: T ) := 

> T

X

> t=1

 g⊤ζt(u1: t)2. (120) 

Proposition 3 (Open-loop information reduction) . Under Assumption 2, 

Ipre (T ; θ, θ ′) = 12σ2 sup  

> u1: T∈U T
> core

ΦT (u1: T ). (121) 

Thus feedback and randomization do not increase the maximum pre-commitment information in this class. The proof is in Appendix C.1. 

Equation (121) is a constrained experiment-design problem: choose a common-safe action sequence to maximize signal energy. The unconstrained counterpart is classical deterministic input design, where open-loop sequences with certified excitation can be constructed explicitly [11, 12]; the constraint that the sequence remain in UT 

> core

is what caps the value of the program. 

## 6.2 Quadratic information programs 

Suppose the regressors are linear in the stacked action vector 

u := u⊤ 

> 1

· · · u⊤

> T

⊤ ∈ RnT , ζt(u) = Zt,T u,

where causality means that Zt,T has zero columns for controls after time t. Define 

MT := 

> T

X

> t=1

Z⊤ 

> t,T

gg ⊤Zt,T ⪰ 0. (122) Then Φ T (u) = u⊤MT u.26 Single quadratic budget. Suppose 

UT 

> core

= u : u⊤KT u ≤ ϵT , KT ⪰ 0, ϵT > 0. (123) Define the generalized Rayleigh quotient (with value zero when MT = 0) 

λmax (MT , K T ) := sup   

> u:u⊤KTu>0

u⊤MT uu⊤KT u . (124) 

Proposition 4 (Single-budget certificate) . Assume MT , K T ⪰ 0.1. If ker KT̸ ⊆ ker MT , then the information program is unbounded: the model contains an informative direction with zero quadratic safety cost. 2. If ker KT ⊆ ker MT , then 

Ipre (T ; θ, θ ′) = ϵT

2σ2 λmax (MT , K T ). (125) 

The proof is in Appendix C.2. 

The profile is therefore the available quadratic safety resource multiplied by the largest ratio of information to resource. Bounded information follows when 

ϵT λmax (MT , K T )is uniformly bounded; linear growth follows when this product is Θ( T ). 

Corollary 8 (Stationary matched-order systems) . Suppose 

MT = IT ⊗ M0, KT = IT ⊗ K0,

and ker K0 ⊆ ker M0. Then 

Ipre (T ; θ, θ ′) = ϵT

2σ2 λmax (M0, K 0). (126) 

A bounded ϵT gives a bounded profile. If ϵT = Θ( T ) and λmax (M0, K 0) > 0, the profile grows linearly. The proof is in Appendix C.3. 

## 6.3 Multiple constraints and an SDP certificate 

Suppose the common core is contained in the quadratic outer approximation 

UT 

> core

⊆ bUT 

> core

:= u : u⊤Qj,T u ≤ bj,T , j = 1 , . . . , m T , (127) where Qj,T ⪰ 0 and bj,T ≥ 0. Then 2σ2Ipre (T ; θ, θ ′) ≤ vT := max 

> u

u⊤MT u : u⊤Qj,T u ≤ bj,T , j ∈ [mT ] . (128) The QCQP is nonconvex in general. Its semidefinite dual is 

dsdp  

> T

:= min 

> λ≥0
> mT

X

> j=1

bj,T λj

subject to 

> mT

X

> j=1

λj Qj,T ⪰ MT .

(129) Weak duality gives vT ≤ dsdp  

> T

. Thus every feasible dual point is a valid information upper bound; strong duality is not required. 27 Theorem 3 (Uniform SDP information certificate) . Suppose that for every horizon T there are multipliers 

λj,T ≥ 0 such that mTX

> j=1

λj,T Qj,T ⪰ MT (130) 

and mTX

> j=1

bj,T λj,T ≤ Λ for all T (131) 

for some finite Λ. Then 

βpre (θ, θ ′) ≤ Λ2σ2 . (132) 

Once the commitment allowance and the noncommitment regret profile have been bounded, Theorem 1 applies with information ceiling Λ/(2 σ2). The proof is in Appendix C.4. 

A finite-horizon solve bounds only Ipre (T ). A bounded-capacity certificate requires feasible multipliers whose objective values are uniformly bounded in T . Conversely, a growing SDP value does not prove that the true information profile grows: the relaxation may be loose. Even exact information growth does not by itself establish safe oracle recovery. 

Remark 10 (Affine terms and input constraints) . Affine dynamics, nonzero initial states, off-center ellipsoids, and linear input constraints can be represented by homogenizing with eu = (1 , u⊤)⊤ and imposing eu0 = 1 (or W00 = 1 after lifting). The resulting SDP remains a valid upper certificate, but the relaxation may be more conservative. 

## 6.4 Labeled problems 

Pairwise certificates also give a conservative upper bound on the mutual information used in Proposition 1. Let Θ M = {θ1, . . . , θ M } and use the same labeled stopped record SπT under every label. Suppose that every 

π ∈ ΠT

> safe

(Θ M ) satisfies 

DKL 



P π, pre   

> θj,T

P π, pre  

> θk,T



≤ βjk,T , j, k ∈ [M ].

Then 

I(M )pre (T ; Θ M ) ≤ 1

M 2

> M

X

> j=1
> M

X

> k=1

βjk,T . (133) The proof is in Appendix C.5. The bound can be loose because the pairwise programs may favor different sensing actions. For the unknown-location problem in Section 4.4, the direct list calculation is sharper. 

## 6.5 Finite-headroom example 

Recall the finite-headroom model used in the binary consequence of Theorem 1: 

dt+1 = dt − θu 2 

> t

, yt = θu t + ξt, 0 ≤ ut ≤ U. 

For the pair ( θb, θ d) with 0 ≤ θb < θ d, the common regime satisfies 

> T

X

> t=1

u2 

> t

≤ d0

θd

.

The information matrix is 

MT = ( θd − θb)2IT , KT = IT .

28 With the pointwise action cap, the largest feasible input energy is min {T U 2, d 0/θ d}. Hence 

Ipre (T ; θb, θ d) = (θd − θb)2

2σ2 min 



T U 2, d0

θd



. (134) Once T U 2 ≥ d0/θ d, the profile saturates at ( θd − θb)2d0/(2 σ2θd), which is the bound used in the finite-headroom consequence. The one-sided budget in Remark 8 is better handled directly. If 0 ≤ ut ≤ U and P 

> t

ut ≤ B, then 

X

> t

u2 

> t

≤ min {B2, U B }.

No quadratic-budget reformulation is needed. 

Scope of the computational certificate. A feasible finite-horizon dual point bounds one profile value. A uniformly bounded family of dual points certifies bounded pre-commitment information. Pairwise bounds can also be combined into a sufficient multiclass certificate. A growing SDP upper bound does not establish persistent information, and none of these calculations supplies the commitment value or the safe transition to the oracle; those remain separate control arguments. 

# 7 Numerical Illustrations 

The preceding sections give three outcomes. Bounded pre-commitment information yields an oracle-gap lower bound through Theorem 1. Repeatable information aligned with safety yields oracle recovery through Theorem 2. In the multi-alternative setting of Section 4.4, finite sensing can instead support a certified list and partial adaptation. This section illustrates these outcomes. The replenishment and sensor examples evaluate analytic infor-mation profiles. The unknown-location example uses Monte Carlo only to evaluate the resulting list sizes and values. The braking example compares the simulated lower-confidence policy with the exact robust baseline, nominal certainty equivalence, and the analytic regret upper bound from Theorem 2. Monte Carlo is used only to display policy performance; the safety guarantee for the lower-confidence policy remains analytic. Safety in all four examples is established analytically, not by simulation. The quantities Iav , Irate , and Ilevel 

are example-specific evidence quantities used in the plots; Ipre in Definition 4 remains the general profile. 

## 7.1 Replenishment: from bounded to persistent safe information 

Consider the replenishing thermal resource 

dt+1 = min {dmax , d t + b − θu 2 

> t

}, dt ≥ 0, 0 ≤ ut ≤ U, (135) with sensor 

yt = θu t + ξt, ξt

> iid

∼ N (0 , σ 2). (136) We compare the target θ0 = 0 with the incompatible alternative θ1 > 0. The θ0-oracle uses ut = U . Before excluding θ1, the learner must preserve feasibility under θ1.The largest Gaussian divergence available over a length-T common-safe experiment is 

Iav (T ; b) = θ21

2σ2 min 



U 2T, d0 + bT θ1



. (137) The first term is the action limit; the second is the initial headroom plus replenishment. Figure 2 uses 

θ0 = 0 , θ1 = 1 , σ = 1 , U = 1 , d0 = dmax = 2 .

29 In the plotted horizon range, the resource term is active and 

Iav (T ; b) = 1 + b

2 T. 

Thus b = 0 gives a bounded information budget, whereas every fixed b > 0 gives a positive information rate. A robust-then-commit policy accumulates evidence inside the common-safe regime and uses u = U only after the log-likelihood ratio in favor of θ0 over θ1 reaches 

κT = log(1 /η T ) = 2 log T, ηT = T −2. (138) Under θ1, the exponentiated likelihood ratio is a nonnegative martingale. Ville’s inequality therefore bounds the probability of false commitment by e−κT = T −2.

Figure 2: Replenishment changes the information-growth regime (Sec 7.1). Replenishing finite-headroom device with target θ0 = 0, alternative θ1 = 1, and replenishment rate b. (a) Available common-safe evidence Iav (T ; b) against the commitment threshold κT = 2 log T : bounded for b = 0, linear for every fixed b > 0. (b) Normalized gap left, min {1, κ T /I av (T ; b)}: the fraction of the horizon a robust-then-commit policy spends below the threshold, and hence the fraction of the commitment-only gap it forfeits. It is an evidence-budget ratio, not a simulated regret; it equals one when the threshold is unreachable and vanishes when the threshold requires a negligible fraction of the available evidence. (c) The Ville bound e−κT = T −2

on false commitment under θ1; the curve is analytic, not a Monte Carlo estimate. For b = 0, increasing the horizon does not increase the evidence available before commitment. For b > 0, the information profile grows linearly, so the bounded-information premise of Theorem 1 no longer applies. 

## 7.2 Sensor ablation: transient evidence versus persistent imprint 

We next keep the plant, objective, and safe diagnostic action sequence fixed and change only the sensor. Set 

b = 0 and retain the pair ( θ0, θ 1) = (0 , θ 1). The learner applies a safe prefix with 

q := 

> m

X

> t=1

u2 

> t

≤ d0/θ 1,

and then sets ut = 0. With the rate sensor 

yrate  

> t

= θu t + ξt,

no further information arrives after the prefix. For every T ≥ m,

Irate (T ) = θ21 q

2σ2 . (139) 30 With the level sensor 

ylevel  

> t

= dt + ξt,

the same prefix leaves a persistent separation ∆ d = θ1q between the two headroom levels. Independent passive measurements then give 

Ilevel (T ) = Ilevel (m) + ∆2

> d

2σ2 (T − m), T ≥ m. (140) Thus the rate sensor yields finite evidence, while the level sensor converts the same finite perturbation into a linearly growing observation record. 

Figure 3: The sensing channel changes the information profile (Section 7.2); both configurations apply the same safe diagnostic prefix of energy q, and only the sensor differs. (a) Pre-commitment information: the rate sensor yrate  

> t

= θu t + ξt records only the transient input, giving the bounded profile; the level sensor 

ylevel  

> t

= dt + ξt observes the persistent headroom separation ∆ d = θ1q, so information grows linearly. (b) 

Normalized gap left, min {1, κ T /I (T )}, as in Figure 2(b): the rate sensor never reaches the growing threshold 

κT = 2 log T , while the level sensor reaches it after logarithmically many passive measurements. The example isolates the role of observation design: the plant dynamics alone do not determine the pre-commitment information regime. 

## 7.3 Unknown disturbance location: safe lists and partial adaptation 

We use the multi-alternative model from Section 4.4. There are M possible affected locations and one true location J. During the diagnostic window, 

xi,t +1 = xi,t + ∆ 1{i = J}, xi, 1 = 0 , xi,t ≤ H, (141) with H = N ∆. Hence unit-throughput diagnostic operation is safe for N rounds. Varying N changes the available physical headroom, not only a policy parameter; the curves therefore compare different safe sensing windows. At each round, the learner inspects one location: 

yt = μ1{At = J} + ξt, ξt

> iid

∼ N (0 , σ 2). (142) At the deadline, the learner returns a nonempty certified list CN . The post-diagnostic controller divides mitigation and throughput across the retained locations. If J ∈ C N , it arrests the affected state. If J / ∈ C N ,the state violates the constraint on the next round and the continuation reward is set to zero. Therefore safety reduces to retaining the true location. 31 We use the elimination rule from Section 4.4. A candidate is deleted only when its likelihood-ratio statistic exceeds κT = log(1 /η T ). Since the lists only shrink, Ville’s inequality gives, for every true location 

J,

PJ (J / ∈ C N ) ≤ ηT . (143) At each round, the policy inspects the least-sampled surviving location. This balanced rule is used to demonstrate partial certification; it is not claimed to be an optimal sensing policy. All three controllers receive unit reward during the N diagnostic rounds. Afterward, the robust controller retains all M locations, the list controller uses CN , and the oracle retains only J. Their values are 

Jrobust  

> T

(N ) = N + T − NM , (144) 

Jlist  

> T

(N ) = N + ( T − N )E

 1{J ∈ C N }|C N |



, (145) 

Joracle  

> T

(N ) = T. (146) The fraction of the robust-to-oracle gap recovered by the list policy is ΓN := Jlist  

> T

(N ) − Jrobust  

> T

(N )

Joracle  

> T

(N ) − Jrobust  

> T

(N ) . (147) Panel (a) of Figure 4 reports 

I(J; SN ) = log M − E[H(pN )] ,

where pN is the posterior computed from the exact Gaussian likelihoods under a uniform prior. We use 

M = 8 , μ = 2 .5, σ = 1 , T = 1000 , ηT = T −2 = 10 −6.

For each value of N , all Monte Carlo quantities are averaged over 10 5 independent draws of J and the sensor noise. The standard error of every reported reward rate or probability is therefore at most 1 /(2 √10 5) <

1.6 × 10 −3.At N = 36, the reward rates are robust = 0 .1565 , safe list = 0 .3893 , oracle = 1 .

The list policy recovers 27 .6% of the robust-to-oracle gap. Its mean list size is 3 .62; 10 .6% of trials produce a singleton list and 88 .9% produce a proper partial list. No false elimination was observed in 10 5 trials. The safety claim is the analytic bound (143), not the empirical count. This example realizes the intermediate case quantified by Proposition 1: a smaller certified uncertainty set can improve value before exact localization is possible. 

## 7.4 Repeatable braking: safe information converted into performance 

The final illustration uses the braking model of Section 5.6. The safety-preserving maneuver is also informa-tive. We set θ ∈ [0 .5, 1.5] , θ⋆ = 1 , D = 1 , σ = 0 .3. Each episode begins at distance D. Given a pessimistic parameter ϑt, the learner enters with speed 

v(ϑt) = p2Dϑ t,

applies maximal braking, and observes yt = θ⋆ + ξt. Because the design coefficient is one in every episode, the accumulated information is Vt = t/σ 2.

The learner uses the lower-confidence rule in Section 5.2 with δT = T −2. On the simultaneous confidence event, ϑt ≤ θ⋆ for every t, so the stopping-distance calculation gives safety episode by episode. Corollary 7 gives 

RT = O σ

s

Dθ

pT log T

!

. (148) 32 Figure 4: Limited sensing produces safe partial adaptation. Unknown-disturbance-location model (Section 7.3) with M = 8 locations, true location J, and a diagnostic window of N safe rounds; parameters as in the text. (a) Decision information I(J; SN ) approaches log M as N grows. (b) Mean size of the certified list CN , shrinking from the robust list [ M ] toward the oracle list {J}. (c) Post-diagnostic reward rates: the list policy lies between the robust and oracle values over a broad range of N . (d) Fraction of trials returning the full list, a proper partial list, or a singleton; near the operating point N = 36 most trials produce a proper partial list rather than exact localization. No false elimination occurred in 10 5 trials; the safety claim is the analytic bound, not the empirical count. A permanently robust controller uses v(θ), so its regret at θ⋆ > θ is linear. We fix a terminal horizon Tmax = 5000, set δ = T −2max and ν = 1 /σ 2, and simulate 20 ,000 independent trajectories in Figure 5. All quantities at episode t ≤ Tmax are prefixes of this single horizon-Tmax policy. For nominal certainty equivalence, no violation through episode t = n + 1 is equivalent to the first n centered Gaussian partial sums remaining nonpositive. For continuous symmetric increments, the corresponding survival probability is 

Pθ⋆

 no CE violation through episode t =

 2nn



4n , n = t−1. =⇒ Pθ⋆

 CE violation by episode t = 1 −

 2nn



4n .

This example verifies the two ingredients used in Section 5: one safe policy produces persistent informa-tion, and the value loss decreases with the confidence radius. 33 Figure 5: Repeatable information must be converted into a safety-valid decision rule. Recursively feasible braking with θ ∈ [0 .5, 1.5], θ⋆ = 1, D = 1, σ = 0 .3, Tmax = 5000, δ = T −2max , and ν = 1 /σ 2. (a) 

Median entry speed of the lower-confidence policy over 20 ,000 independent trajectories, with the 10–90 percentile band. The policy initially coincides with the robust controller and then expands the certified safe boundary toward the oracle. (b) Cumulative probability that nominal certainty equivalence has violated the stopping-distance constraint by episode t, compared with the trajectory-wide LCB (lower confidence bound policy (see Sec. 5.2) certificate δ. (c) Exact cumulative regret of the permanently robust controller, empirical mean regret of the LCB policy, and the analytic upper bound from Theorem 2. Nominal certainty equivalence is omitted from the regret comparison because it is not uniformly safe. No LCB violations were observed in simulation; the safety claim is the analytic bound P(Safe c) ≤ δ.

# 8 Related Work 

Adaptive control and constrained LQR. Finite-time adaptive-control results obtain regret or sample-complexity bounds when deliberate excitation, process noise, or closed-loop state variation keeps the system informative [1, 13, 14, 15]. Certainty equivalence can be efficient in unconstrained LQR when a point estimate may be used directly for control [16]. A binding trajectory constraint changes this implication: an optimistic estimation error can itself make the selected action unsafe before later observations can correct the estimate. Section 7.4 gives a simple instance. Learning-based MPC can separate a robust model used for safety from a learned model used for performance, with convergence to the model-informed controller under sufficient excitation [17]. Constrained-LQR results give more specific guarantees. Dean et al. [2] combine persistent excitation with robust controller synthesis. Li et al. [3] obtain eO(T 2/3) regret for a 34 single-trajectory constrained problem. Schiffer and Janson [4] show that a binding safety constraint can itself generate useful state variation in a one-dimensional problem with nonlinear baselines. Hutchinson et al. [5] obtain eO(√T ) regret for multidimensional chance-constrained LQR. These results give conditions under which safe operation continues to supply information. In our terms, they place the problem in a persistent precommitment-information regime. We ask whether this property holds for a given dynamics– sensor–constraint geometry. Bounded precommitment information gives a certificate that it does not. 

Dual control, experiment design, and controlled sensing. Dual control accounts for the effect of an action on both current performance and future information [18, 19]. Experiment design for control selects inputs for their value to the eventual control task [20]. Sequential experimental design and controlled sensing select experiments to distinguish among hypotheses [6, 7, 8, 10]. Our setting adds a decision-time constraint. The action that uses the information must be chosen before the observation generated by that action is available. Safety may also shrink the set of admissible experiments as margin is spent. Thus full-horizon information can count evidence that arrives too late, and the relevant likelihood ratio is stopped before commitment. 

Safe learning, recursive feasibility, and returnability. Safe learning and learning-based control are surveyed by Garc´ ıa and Fern´ andez [21], Hewing et al. [22], and Brunke et al. [23]. Recursive feasibility and controlled invariance are central tools in constrained control [24, 25]. Safety filters and reachability methods preserve a safe continuation while a learning controller operates [26, 27, 28]. Safe exploration methods use recoverability, returnability, or terminal safe sets to avoid states with no safe exit [29, 30, 31, 32]. In the deterministic systems considered here, noncommitment relative to an alternative is exactly preservation of hard-safe recursive feasibility under that alternative. Commitment is the first action that removes this continuation. Our lower bound asks whether the evidence needed for that action can be collected while the continuation is still preserved. Related methods include safe Bayesian optimization and safe linear bandits [33, 34]; safety issues in power-system learning are surveyed by Dobbe et al. [35]. 

Data informativity. Data informativity asks whether a given dataset supports a controller that works for every model consistent with the data. This line builds on persistency of excitation [11] and has been developed into a general informativity theory [36]. Worst-case identification bounds what a finite data record can resolve when the model class itself is uncertain [37, 38], and deterministic input designs with certified excitation show how such records can be generated [12]. Mixed stochastic–set-membership formulations sharpen the excitation story: persistency of excitation is necessary, not only sufficient, for uniform consistency, and finite-data lower bounds hold without assuming a consistent estimator [39]. Our question is online and decision-timed: whether safe interaction can generate the data needed for a model-specific decision before that decision must be made. The distinction is between certifying a controller from available data and certifying whether the required data can be collected safely in time. 

# 9 Discussion and Limitations 

When safety changes the learning problem. If a near-oracle policy can be reached while preserving a safe continuation under every plausible model, the safety-specific obstruction is absent. Information can con-tinue to grow inside the common feasible regime, and the problem is in the setting addressed by constrained adaptive-control analyses. The lower-bound examples have a different geometry. Better target performance requires leaving the common regime, while the actions that separate the target from an alternative consume the margin that must be preserved under that alternative. The issue is then not only the rate at which uncertainty shrinks. It is whether the required evidence is available before the first action that needs it. 

Information timing versus information growth. Persistent excitation asks whether information con-tinues to grow along the closed loop. Precommitment information asks whether the relevant information arrives in time. A system may be identifiable from its complete trajectory and still fail to support safe adaptation if the observations that distinguish the relevant models appear only after commitment. Stopping the record is therefore part of the causal statement, not only a proof technique. The positive theorem treats a case in which the safe maneuver itself is informative and repeatable, so information accumulates without giving up recursive feasibility. 35 Design implications. When persistent safe information is available, it must still be converted into a safety-valid decision rule. In the braking example, nominal certainty equivalence can overestimate braking authority and violate the constraint, whereas the lower-confidence rule expands the safe operating boundary only as the data justify it. When precommitment information is bounded, however, changing the estimator cannot remove the obstruction; the controlled experiment or the required degree of specialization must change. Replenishment, a persistent sensor signal, and certified candidate lists illustrate these three possibilities. 

Limits of the present results. Bounded precommitment information is a sufficient certificate for an unavoidable oracle gap. Failure of the certificate does not prove recovery. The pairwise profile may be large because different policies are informative for different alternatives, even though no single safe policy resolves all of them. Information may also be available but unusable because the controller cannot enter the selected regime without losing feasibility. The recovery theorem proves one positive result for an ordered scalar parameter, a repeatable experiment family, and a common continuation state. Extending it to vector parameters, several control-equivalence classes, and nonresetting dynamics requires additional transition and exploitation arguments. The computational certificate omits process noise, and the general case of unbounded but sublinear precommitment information remains unresolved. The lower-bound theorem itself is more general. An application must specify a predictable commitment rule, compute or bound the allowed commitment probability under the alternative, evaluate the noncommit-ment regret profile under the target, and bound the stopped information. Pathwise value ceilings are one way to control the regret profile in deterministic systems; they are not required by the theorem. 

# Acknowledgments 

The author thanks Aditya Gangrade and Alex Olshevsky for technical discussions, and Emiliano Dall’Anese for help with practical applications. Large language models were used for editorial assistance, including identifying possible issues in exposition, consistency, and references. The author independently developed and verified all mathematical arguments and conclusions, checked the cited sources, and assumes full respon-sibility for the manuscript. 

# A Proofs for Section 4 

## A.1 Proof of Lemma 3 

Proof. Fix a uniformly safe policy and one sample path. Define 

st := 1{t < τ π }St.

Then st ≥ 0 and, by (52), TX

> t=1

st ≤ BT .

For q ≥ 1, TX

> t=1

sqt ≤

> T

X

> t=1

st

!q

≤ BqT .

Using (53), TX

> t=1

1{t < τ π }kl θ,θ ′ 

> t

≤ κ

> T

X

> t=1

sqt ≤ κB qT .

Take expectation under θ, apply Lemma 1, and then take the supremum over uniformly safe policies. This gives (54). 36 A.2 Proof of Corollary 4 

For Variant I, the noncommitment calculation above gives 

G(1)  

> T

≥ g1T − O(1) ,

while the stopped-information bound gives 

I(1) pre (T ; a0, a 1) ≤ β1 + o(1) .

For Variant II, 

G(2)  

> T

≥ g2T − O(√T ), I(2) pre (T ; a0, a 1) ≤ β2 + o(1) .

In both variants, commitment under a1 implies a safety violation, so the commitment allowance is at most 

ηT = o(1). The uniform stage-cost bounds give a noncommitment-profile remainder of order O(T η T ) = o(T ). Applying Theorem 1, dividing by T , and taking the lower limit proves lim inf   

> T→∞

inf 

> π∈ΠT ,i
> safe

R(i) 

> T

(π; a0)

T ≥ gi

2 e−βi .

The eventual linear lower bound follows because the right-hand side is strictly positive. 

## A.3 Proof of Lemma 4 

Proof. Fix a uniformly safe policy π. The policy action at time t, the decision to continue, and the eventual specialization label are functions of the previously observed record and environment-independent private randomization. They therefore add no information about J conditional on that record. The chain rule for the stopped record gives 

Iπ (J; SπT ) = 

> T

X

> t=1

Pπ (t < τ π )Iπ (J; Zt | Ht−1, U t, t < τ π ) .

Only label-dependent observations contribute. By assumption there are at most NT such observations before specialization or fallback, and each contributes at most cobs . Hence 

Iπ (J; SπT ) ≤ NT cobs .

Taking the supremum over uniformly safe policies proves (76). For the Gaussian sensor, condition on ( Ht−1, A t) and define 

Bt := 1{At = J}.

The observation depends on J only through Bt, so 

I(J; Zt | Ht−1, A t) = I(Bt; Zt | Ht−1, A t) ≤ H(Bt | Ht−1, A t) ≤ log 2 .

Let 

pt := P (J = At | Ht−1, A t), P1 := N (μ, σ 2), P0 := N (0 , σ 2).

The output mixture minimizes the weighted average relative entropy over the choice of reference distribution. Taking P0 as a reference gives 

I(Bt; Zt | Ht−1, A t) ≤ ptDKL (P1∥P0) ≤ μ2

2σ2 .

Combining the two bounds proves (78). 37 A.4 Proof of Proposition 1 

Proof. Let 

E := {J / ∈ C(S) or |C(S)| > L }, P (E) = εL.

On Ec, the true label belongs to a list of size at most L, so 

H(J | S, E c) ≤ log L. 

On E, use the bound H(J | S, E ) ≤ log M . Therefore 

H(J | S) ≤ H(1E | S) + H(J | S, 1E )

≤ h2(εL) + (1 − εL) log L + εL log M. 

Since J is uniform on [ M ], 

I(J; S) = H(J) − H(J | S) ≥ (1 − εL) log ML − h2(εL),

which proves (49). If P (J / ∈ C(S)) ≤ η and P (|C(S)| > L ) ≤ δ, then εL ≤ η + δ by the union bound. For L < M , the function 

x 7 −→ (1 − x) log( M/L ) − h2(x)is decreasing on [0 , 1/2]. Substituting εL ≤ η + δ ≤ 1/2 gives (50). 

## A.5 Safe-list guarantee and value calculation for Section 4.4 

Proof of the safe-list guarantee. Fix the true location J. At a time s with As = J, the multiplicative increment in exp(Λ J,t ) is 

ϕ0,σ (Zs)

ϕμ,σ (Zs) .

Under the true model, Zs ∼ N (μ, σ 2) conditionally on the past, so this increment has conditional expectation one. At times with As̸ = J, the process does not change. Hence {exp(Λ J,t )}t≥0 is a nonnegative martingale under the true location, even though the sampled locations are chosen adaptively. Ville’s inequality gives 

PJ



sup 

> t≤N

ΛJ,t ≥ log 1

ηT



≤ ηT .

The event on the left is exactly false elimination of the true location before the deadline. If an empty candidate set is replaced by [ M ], the failure probability can only decrease. This proves (88). 

Proof of the value formulas. Suppose first that J ∈ C N . For the affected component, 

xJ,t +1 − xJ,t = ∆

|C N | − ∆

|C N | = 0 .

Each retained unaffected component decreases by ∆ /|C N |, and each nonretained unaffected component is unchanged. Thus the post-diagnostic controller is safe whenever the list contains the true location. If J / ∈ C N ,the affected component starts the post-diagnostic phase at the boundary and increases on the next round, so a violation occurs immediately. Every policy earns unit reward in each of the N diagnostic rounds. The robust controller retains all 

M locations and earns post-diagnostic reward 1 /M per round. The list controller earns 1 /|C N | per post-diagnostic round when J ∈ C N and zero after failure. The oracle retains the singleton {J} and earns unit reward throughout. These calculations give (89)–(91). 38 B Proofs for Section 5 

## B.1 Proof of Lemma 5 

Proof. Define 

Mt := 

> t

X

> s=1

asξs

σ2 .

Then 

bθt − θ = Mt

Vt

.

Because at is predictable and ξt ∼ N (0 , σ 2) is independent of the past, for every fixed λ ∈ R,

Lt(λ) := exp 



λM t − λ2

2 Vt



is a nonnegative martingale with initial value one. Integrate Lt(λ) against the Gaussian density λ ∼ N (0 , ν −1). Completing the square gives the nonnegative martingale 

Mt =

r νVt + ν exp 

 M 2

> t

2( Vt + ν)



.

Ville’s inequality implies 

Pθ



sup 

> t≥1

Mt ≥ 1

δ



≤ δ. 

On the complementary event, simultaneously for all t ≥ 1, 

M 2 

> t

≤ 2( Vt + ν) log 

p1 + Vt/ν δ

!

.

Dividing by V 2 

> t

proves 

|bθt − θ| ≤ b(Vt, δ ; ν)for every t ≥ 1. 

## B.2 Proof of Theorem 2 

Proof. Let 

E∞ := 

n

|bθs − θ| ≤ b(Vs, δ ; ν) for every s ≥ 1

o

.

Lemma 5 gives Pθ (Ec

> ∞

) ≤ δ.On E∞, the selected design is conservative. At t = 1, ϑ1 = θ ≤ θ. For t ≥ 2, 

bθt−1 − b(Vt−1, δ ; ν) ≤ θ. 

Clipping to [ θ, θ ] therefore preserves ϑt ≤ θ. The same confidence bound also gives 0 ≤ θ − ϑt ≤ 2b(Vt−1, δ ; ν), t ≥ 2.

If the lower clipping is inactive, this follows directly from the two-sided confidence interval. If it is active, then bθt−1 − b(Vt−1, δ ; ν) < θ and bθt−1 ≥ θ − b(Vt−1, δ ; ν), which imply the same bound. By Assumption 1(P2), every episode is safe and returns to the common continuation state on E∞. Hence 

P πθ,T (Safe cθ,T ) ≤ Pθ (Ec

> ∞

) ≤ δ. 

39 For regret, the first episode contributes at most rmax . For t ≥ 2, define the predictable event 

Et−1 := 

n

|bθt−1 − θ| ≤ b(Vt−1, δ ; ν)

o

.

It is Gt−1-measurable, and Pθ (Ect−1) ≤ δ. On Et−1,

ϑt ≤ θ, 0 ≤ θ − ϑt ≤ 2b(Vt−1, δ ; ν).

Assumption 1(P3)–(P4) then gives 

 v⋆(θ) − Eθ [rt | G t−1]1Et−1 ≤ ω(2 b(Vt−1, δ ; ν)) 1Et−1 .

On Ect−1, the one-episode loss is at most rmax . Taking expectations yields 

Eπθ [v⋆(θ) − rt] ≤ Eπθ [ω(2 b(Vt−1, δ ; ν))] + rmax δ. 

Summing over t = 2 , . . . , T and adding the first-episode bound proves (103). 

## B.3 Proof of Corollary 5 

Proof. Set δT = T −2. Under (104), for every 1 ≤ t ≤ T ,

b(Vt, δ T ; ν) ≤ C0t−α/ 2plog T , 

where C0 depends only on c−, c+, α, and ν. Since ω(r) ≤ Lr q ,

ω(2 b(Vt, δ T ; ν)) ≤ C1L(log T )q/ 2t−αq/ 2.

Theorem 2 therefore gives 

RT (π; θ) ≤ rmax + C1L(log T )q/ 2 

> T−1

X

> t=1

t−αq/ 2 + rmax 

T .

The three cases in (105) follow from the standard partial-sum bounds for t−αq/ 2.

## B.4 Proof of Corollary 6 

Proof. Uniform alignment gives 

ρt ≤ Vt ≤ ¯ρt. 

Apply Corollary 5 with α = 1, c− = ρ, c+ = ¯ ρ, and q = 1. 

## B.5 Proofs for the braking example 

Proof of Lemma 6. Under maximal braking, the stopping distance from v(ϑ) = √2Dϑ is 

v(ϑ)2

2θ = D ϑθ ≤ D. 

Thus the vehicle stops before the wall whenever ϑ ≤ θ. The observation Yt = θ + ξt has coefficient at = 1 and contributes Fisher information 1 /σ 2.Also, 

v⋆(θ) − v(ϑ) = √2D θ − ϑ

√θ + √ϑ

≤

s

D

2θ (θ − ϑ).

This proves (114). 40 Proof of Corollary 7. Lemma 6 verifies Assumption 1 with 

ρ = ¯ ρ = 1

σ2 , ω(r) = 

s

D

2θ r, rmax =

p

2Dθ. 

Apply Corollary 6. 

## B.6 Proof of Proposition 2 

Proof. Conditioned on ( Gt−1, a t), the two observation laws are 

N (θa t, σ 2) and N (θ′at, σ 2).

Their conditional KL divergence is (θ − θ′)2a2

> t

2σ2 .

The policy does not commit during the T experiments, so Lemma 1 gives 

DKL 



P π, pre  

> θ,T

P π, pre 

> θ′,T



= (θ − θ′)2

2 Eπθ

" TX

> t=1

a2

> t

σ2

#

,

which is (116). Any one uniformly safe policy gives a lower bound on the profile, proving (117). Under uniform ρ-alignment, repeating κθ is safe for the full interval and gives VT ≥ ρT , which proves (118). 

# C Proofs for Section 6 

## C.1 Proof of Proposition 3 

Proof. Fix a uniformly safe causal policy. By Lemma 1 and Assumption 2(D1), 

DKL 



P π, pre  

> θ,T

P π, pre 

> θ′,T



= 12σ2 Eπθ

" X 

> t<τ π

 g⊤ζt(U1: t)2

#

.

Every realized pre-commitment action prefix extends, by (D3), to a sequence in UT

> core

. Since all summands are nonnegative, X 

> t<τ π

 g⊤ζt(U1: t)2 ≤ sup  

> u1: T∈U T
> core

ΦT (u1: T )on every path. Taking the supremum over uniformly safe policies gives the upper bound in (121). Conversely, (D3) states that every sequence in UT 

> core

can be played open loop by a uniformly safe, noncom-mitting policy. For this policy, the stopped record is the full horizon record, and the preceding identity gives KL divergence Φ T (u1: T )/(2 σ2). Taking the supremum over core sequences proves the reverse inequality. 

## C.2 Proof of Proposition 4 

Proof. Suppose first that ker KT̸ ⊆ ker MT . Then there is a vector z ∈ ker KT with z⊤MT z > 0. For every 

c > 0, the vector cz is feasible, while (cz )⊤MT (cz ) = c2z⊤MT z −→ ∞ .

Hence the information program is unbounded. 41 Now suppose ker KT ⊆ ker MT . Decompose u = ur + u0, where ur ∈ range( KT ) and u0 ∈ ker KT . Since 

MT u0 = 0, both the objective and the constraint depend only on ur . Set 

w := K1/2 

> T

ur .

Then 

ur = K†/2 

> T

w, u⊤ 

> r

KT ur = ∥w∥22,

and 

u⊤ 

> r

MT ur = w⊤K†/2 

> T

MT K†/2 

> T

w. 

Maximizing over ∥w∥22 ≤ ϵT gives sup  

> u⊤KTu≤ϵT

u⊤MT u = ϵT λmax (MT , K T ).

Divide by 2 σ2 and apply Proposition 3. 

## C.3 Proof of Corollary 8 

Proof. On the range of KT ,

K†/2 

> T

MT K†/2 

> T

= IT ⊗  K†/20 M0K†/20

.

Its largest eigenvalue is therefore λmax (M0, K 0). Substituting this equality into (125) gives (126). 

## C.4 Proof of Theorem 3 

Proof. Fix T and a vector u in the quadratic outer core. By (130), 

u⊤MT u ≤

> mT

X

> j=1

λj,T u⊤Qj,T u

≤

> mT

X

> j=1

λj,T bj,T ≤ Λ.

Hence the QCQP value in (128) is at most Λ. Proposition 3 then gives 

Ipre (T ; θ, θ ′) ≤ Λ2σ2

for every T . Taking the supremum over horizons proves (132). 

## C.5 Proof of the labeled aggregation bound 

Proof. Fix a uniformly safe policy and write 

Pj := P π, pre   

> θj,T

, P := 1

M

> M

X

> k=1

Pk.

Under the uniform prior, 

I(J; SπT ) = 1

M

> M

X

> j=1

DKL (Pj ∥P ).

42 Convexity of KL divergence in its second argument gives 

DKL (Pj ∥P ) ≤ 1

M

> M

X

> k=1

DKL (Pj ∥Pk).

Applying the assumed pairwise bounds and averaging over j yields 

I(J; SπT ) ≤ 1

M 2

> M

X

> j=1
> M

X

> k=1

βjk,T .

Finally, take the supremum over uniformly safe policies to obtain (133). 

# References 

[1] Yasin Abbasi-Yadkori and Csaba Szepesv´ ari. Regret bounds for the adaptive control of linear quadratic systems. In Proceedings of the 24th Annual Conference on Learning Theory , volume 19 of Proceedings of Machine Learning Research , pages 1–26. PMLR, 2011. [2] Sarah Dean, Stephen Tu, Nikolai Matni, and Benjamin Recht. Safely learning to control the con-strained linear quadratic regulator. In 2019 American Control Conference , pages 5582–5588. IEEE, 2019. arXiv:1809.10121. [3] Yingying Li, Subhro Das, Jeff Shamma, and Na Li. Safe adaptive learning-based control for constrained linear quadratic regulators with regret guarantees, 2021. arXiv:2111.00411. [4] Benjamin Schiffer and Lucas Janson. Foundations of safe online reinforcement learning in the linear quadratic regulator: √T -regret, 2025. arXiv:2504.18657. [5] Spencer Hutchinson, Nanfei Jiang, and Mahnoosh Alizadeh. Rate-optimal regret for the safe learning-based control of the constrained linear quadratic regulator, 2026. arXiv:2604.22158. [6] Herman Chernoff. Sequential design of experiments. The Annals of Mathematical Statistics , 30(3):755– 770, 1959. [7] Mohammad Naghshvar and Tara Javidi. Active sequential hypothesis testing. The Annals of Statistics ,41(6):2703–2738, 2013. [8] Sirin Nitinawarat, George K. Atia, and Venugopal V. Veeravalli. Controlled sensing for multihypothesis testing. IEEE Transactions on Automatic Control , 58(10):2451–2464, 2013. [9] Aditya Gangrade, Tianrui Chen, and Venkatesh Saligrama. Safe linear bandits over unknown polytopes. In Proceedings of the 37th Conference on Learning Theory , volume 247 of PMLR , pages 1755–1795, 2024. [10] Aditya Gopalan, Braghadeesh Lakshminarayanan, and Venkatesh Saligrama. Bandit quickest change-point detection. In Advances in Neural Information Processing Systems , volume 34, 2021. [11] Jan C. Willems, Paolo Rapisarda, Ivan Markovsky, and Bart L. M. De Moor. A note on persistency of excitation. Systems & Control Letters , 54(4):325–329, 2005. [12] Venkatesh Saligrama. Aperiodic sequences with uniformly decaying correlations with applications to compressed sensing and system identification. IEEE Transactions on Information Theory , 58(9):6023– 6036, 2012. [13] Sarah Dean, Horia Mania, Nikolai Matni, Benjamin Recht, and Stephen Tu. On the sample complexity of the linear quadratic regulator. Foundations of Computational Mathematics , 20:633–679, 2020. 43 [14] Alon Cohen, Tomer Koren, and Yishay Mansour. Learning linear-quadratic regulators efficiently with only √T regret. In Proceedings of the 36th International Conference on Machine Learning , volume 97 of Proceedings of Machine Learning Research , pages 1300–1309. PMLR, 2019. [15] Max Simchowitz and Dylan J. Foster. Naive exploration is optimal for online LQR. In Proceedings of the 37th International Conference on Machine Learning , volume 119 of Proceedings of Machine Learning Research , pages 8937–8948. PMLR, 2020. [16] Horia Mania, Stephen Tu, and Benjamin Recht. Certainty equivalence is efficient for linear quadratic control. In Advances in Neural Information Processing Systems , volume 32, 2019. [17] Anil Aswani, Humberto Gonzalez, S. Shankar Sastry, and Claire Tomlin. Provably safe and robust learning-based model predictive control. Automatica , 49(5):1216–1226, 2013. arXiv:1107.2487. [18] A. A. Feldbaum. Dual control theory, parts I–IV. Automation and Remote Control , 21, 1960. Parts I–IV appeared across volumes 21–22, 1960–1961. [19] Karl J. ˚ Astr¨ om and Bj¨ orn Wittenmark. Adaptive Control . Addison-Wesley, 2 edition, 1995. [20] H˚ akan Hjalmarsson. From experiment design to closed-loop control. Automatica , 41(3):393–438, 2005. [21] Javier Garc´ ıa and Fernando Fern´ andez. A comprehensive survey on safe reinforcement learning. Journal of Machine Learning Research , 16(42):1437–1480, 2015. [22] Lukas Hewing, Kim P. Wabersich, Marcel Menner, and Melanie N. Zeilinger. Learning-based model pre-dictive control: Toward safe learning in control. Annual Review of Control, Robotics, and Autonomous Systems , 3:269–296, 2020. [23] Lukas Brunke, Melissa Greeff, Adam W. Hall, Zhaocong Yuan, Siqi Zhou, Jacopo Panerati, and An-gela P. Schoellig. Safe learning in robotics: From learning-based control to safe reinforcement learning. 

Annual Review of Control, Robotics, and Autonomous Systems , 5:411–444, 2022. [24] David Q. Mayne, James B. Rawlings, Christopher V. Rao, and Pierre O. M. Scokaert. Constrained model predictive control: Stability and optimality. Automatica , 36(6):789–814, 2000. [25] Franco Blanchini. Set invariance in control. Automatica , 35(11):1747–1767, 1999. [26] Jaime F. Fisac, Anayo K. Akametalu, Melanie N. Zeilinger, Shahab Kaynama, Jeremy H. Gillula, and Claire J. Tomlin. A general safety framework for learning-based control in uncertain robotic systems. 

IEEE Transactions on Automatic Control , 64(7):2737–2752, 2019. [27] Kim P. Wabersich and Melanie N. Zeilinger. A predictive safety filter for learning-based control of constrained nonlinear dynamical systems. Automatica , 129:109597, 2021. [28] Aaron D. Ames, Samuel Coogan, Magnus Egerstedt, Gennaro Notomista, Koushil Sreenath, and Paulo Tabuada. Control barrier functions: Theory and applications. In 2019 European Control Conference ,pages 3420–3431. IEEE, 2019. [29] Teodor Mihai Moldovan and Pieter Abbeel. Safe exploration in markov decision processes. In Proceedings of the 29th International Conference on Machine Learning , pages 1711–1718, 2012. arXiv:1205.4810. [30] Matteo Turchetta, Felix Berkenkamp, and Andreas Krause. Safe exploration in finite markov decision processes with gaussian processes. In Advances in Neural Information Processing Systems , volume 29, 2016. [31] Torsten Koller, Felix Berkenkamp, Matteo Turchetta, Joschka Boedecker, and Andreas Krause. Learning-based model predictive control for safe exploration and reinforcement learning, 2019. arXiv:1906.12189. 44 [32] Felix Berkenkamp, Matteo Turchetta, Angela P. Schoellig, and Andreas Krause. Safe model-based reinforcement learning with stability guarantees. In Advances in Neural Information Processing Systems ,volume 30, 2017. [33] Yanan Sui, Alkis Gotovos, Joel Burdick, and Andreas Krause. Safe exploration for optimization with gaussian processes. In Proceedings of the 32nd International Conference on Machine Learning , volume 37 of Proceedings of Machine Learning Research , pages 997–1005. PMLR, 2015. [34] Sanae Amani, Mahnoosh Alizadeh, and Christos Thrampoulidis. Linear stochastic bandits under safety constraints. In Advances in Neural Information Processing Systems , volume 32, 2019. [35] Roel Dobbe, Patricia Hidalgo-Gonzalez, Stavros Karagiannopoulos, Rodrigo Henriquez-Auba, Gabriela Hug, Duncan S. Callaway, and Claire J. Tomlin. Learning to control in power systems: Design and analysis guidelines for concrete safety problems. Electric Power Systems Research , 189:106615, 2020. [36] Henk J. van Waarde, Jaap Eising, Harry L. Trentelman, and M. Kanat Camlibel. Data informativity: A new perspective on data-driven analysis and control. IEEE Transactions on Automatic Control ,65(11):4753–4768, 2020. [37] S. R. Venkatesh and M. A. Dahleh. Identification in the presence of classes of unmodeled dynamics and noise. IEEE Transactions on Automatic Control , 42(12):1620–1635, 1997. [38] S. R. Venkatesh and M. A. Dahleh. On system identification of complex systems from finite data. IEEE Transactions on Automatic Control , 46(2):235–257, 2001. [39] Venkatesh Saligrama. A convex analytic approach to system identification. IEEE transactions on automatic control , 50(10):1550–1566, 2005. 45