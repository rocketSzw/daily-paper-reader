---
title: Robust Adaptive Backup Control Barrier Functions
title_zh: 鲁棒自适应备份控制屏障函数
authors: "Ersin Daş, David E. J. van Wijk, Tamas G. Molnar, Aaron D. Ames, Joel W. Burdick"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20842v1"
tags: ["query:uav-control"]
score: 8.0
evidence: 针对参数不确定非线性系统的鲁棒自适应备份控制屏障函数，可用于无人机控制
tldr: 针对存在参数不确定性的非线性控制仿射系统，提出鲁棒自适应备份控制障碍函数方法。通过逐元素认证的自适应估计器在线估计未知参数，并利用估计误差界收紧安全条件。采用对偶重构处理执行矩阵不确定性，构建基于二次规划的安全滤波器。理论证明该方法能保证系统在参数不确定和输入约束下的安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 861, \"height\": 730, \"label\": \"Figure\"}]"
motivation: 备份控制障碍函数依赖模型预测轨迹，但参数不确定性会导致预测不准确，亟需鲁棒方法保证安全。
method: 使用认证自适应估计器获得参数估计与误差界，基于估计模型计算备份流并收紧安全条件，对执行矩阵不确定性采用对偶重构。
result: 满足所提约束的控制器能在参数不确定下保证系统安全，且适用于输入约束场景。
conclusion: 提出的鲁棒自适应备份控制障碍函数有效处理了模型参数不确定性，拓展了备份CBF在实际系统中的应用。
---

## 摘要
针对漂移动力学和驱动矩阵均存在参数不确定性的非线性控制仿射系统，我们提出了一种鲁棒自适应备份控制屏障函数的概念。备份控制屏障函数通过预测系统在预认证安全控制器下的轨迹来保证安全性。然而，这些预测依赖于模型，当系统包含未知参数时可能不准确。为了解决这一问题，我们使用逐分量认证的自适应估计器来估计未知参数，该估计器提供参数自适应律和分量级的估计误差界。我们使用估计模型计算备份流，并利用这些认证界收紧安全条件。由此产生的安全条件考虑了预测流对参数估计误差的敏感性。此外，为了处理驱动矩阵中的不确定性，我们采用基于对偶性的重新表述，从而能够使用计算高效的基于二次规划的安全滤波器。我们证明了满足所提出的鲁棒自适应备份控制屏障函数约束的控制器能够在参数不确定性和输入约束下保证安全性。

## Abstract
We propose a notion of robust adaptive backup control barrier functions for nonlinear control affine systems with parametric uncertainty in both the drift dynamics and actuation matrix. Backup control barrier functions guarantee safety by predicting the system's trajectory under a pre-certified safe controller. However, these predictions rely on the model and can be inaccurate when the system contains unknown parameters. To address this issue, we estimate the unknown parameters using element-wise certified adaptive estimators that provide a parameter adaptation law and component-wise estimation error bounds. We compute the backup flow using the estimated model and tighten the safety conditions using these certified bounds. The resulting safety conditions account for the sensitivity of the predicted flow to parameter estimation errors. Moreover, to handle uncertainty in the actuation matrix, we use a duality-based reformulation that enables the use of a computationally efficient quadratic-program-based safety filter. We prove that controllers satisfying the proposed robust adaptive backup control barrier function constraints guarantee safety under parametric uncertainty and input constraints.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：备份控制屏障函数（bCBF）通过预测系统在预认证安全控制器下的有限时域轨迹来构造隐式安全集，从而在输入受限的非线性系统中保证安全性。然而，标准 bCBF 依赖精确模型进行预测；当系统存在参数不确定性（包括漂移动力学和驱动矩阵中的未知参数）时，预测的备份轨迹与真实轨迹可能严重偏离，导致安全性无法保证。
- **整体含义**：本文旨在扩展 bCBF 框架，使其能够处理模型参数不确定性，同时保持计算高效性（如 QP 形式的安全滤波器）。通过在线参数估计和鲁棒收紧策略，构建一个随时间缩小的内隐安全集近似，并证明在该近似集内系统能保持安全。

### 2. 论文提出的方法论

- **核心思想**：结合在线参数估计与鲁棒收紧，将备份控制屏障函数推广到参数不确定系统。利用“逐元素认证的自适应估计器”提供参数估计和分量级估计误差界；在此基础上计算估计模型下的备份流，并用误差界对安全约束进行收紧；针对驱动矩阵不确定性导致的非凸性，采用对偶性重构，使最终安全条件保持为仿射形式，从而可构建 QP 安全滤波器。
- **关键技术细节**：
  - 系统模型：`\dot{x} = f(x) + g(x)u + F(x)θ_f + ΣG_j(x)u θ_{g,j}`，参数 θ 未知但有已知上下界。
  - 估计器定义：需满足 (11)-(12)，并提供满足 (13) 的元素级误差界 ρ_i(t)。文中以 DREM 估计器为例（例 1），给出其更新律和误差界证明。
  - 鲁棒备份控制器：需满足假设 2（备份集 CB 对任意 θ∈Θ 前向不变）。在平面四旋翼例子中，作者构造了基于饱和控制的备份控制器，并通过 Lyapunov 分析证明了鲁棒不变性（命题 1）。
  - 备份流误差界：引理 2 利用 Lipschitz 常数和 Gronwall 不等式给出真实备份流与估计备份流之差的界 δ_φ，进而通过 Lipschitz 连续性得到收紧项 ϵ, ϵ_b。
  - 鲁棒安全条件：定义内近似集 \hat{C}_I(t)，要求对所有 τ∈[0,T] 满足 `\dot{\bar{h}} ≥ -α(\bar{h})` 等。这些条件可写为包含未知误差项 η 的不等式，通过对偶化转化为 (31)-(34) 的线性约束（含对偶变量 μ）。
  - QP 实现：约束在离散时域网格上施加，决策变量包含控制输入 u 和对偶变量 μ，目标是最小化 u 与主控制器 k_p 的偏差（式 35）。

### 3. 实验设计

- **场景**：平面四旋翼载荷投放任务，需避开墙面、保持最小高度和俯仰角约束。
- **数据集**：无公开数据集，使用仿真环境模拟参数不确定的动力学（真实参数使四旋翼比标称重约 47%）。
- **Baseline**：
  1. 主控制器 k_p 单独使用（不考虑安全）。
  2. 标称 bCBF（用标称模型 θ=0 计算备份流，无收紧和鲁棒化）。
- **对比方法**：提出的鲁棒自适应 bCBF（使用 DREM 估计器和收紧约束）。

### 4. 资源与算力

- **未明确说明**：论文未提及 GPU 型号、数量或训练时长。该工作为理论主导，仿真在常规 CPU 上即可运行。补充材料及代码仓库（GitHub）提供了仿真参数，但未报告具体计算资源。

### 5. 实验数量与充分性

- **实验数量**：论文仅展示了一个代表性仿真案例（平面四旋翼）。未进行多组随机参数、不同备份集构造、或与其他鲁棒方法（如鲁棒 MPC）的对比。
- **充分性评判**：
  - **不足**：仅单一场景，缺乏统计显著性和消融实验（如无自适应估计 vs. 有自适应、不同估计器类型、不同误差界计算方式）。
  - **部分充分**：仿真中对比了主控制器和标称 bCBF，直观展示了鲁棒自适应 bCBF 的安全性优势。参数估计器的收敛性也通过子图展示。
  - **偏差风险**：由于未公开随机种子、参数范围等细节，难以评估结果的一般性。补充材料中的命题 1 提供了理论保证，但实验验证较薄弱。

### 6. 论文的主要结论与发现

- 提出的鲁棒自适应 bCBF 框架能在参数不确定性和输入约束下保证系统安全。
- 证明了满足鲁棒安全条件（31)-(34) 的控制器能使内近似集 \hat{C}_I(t) 前向不变，从而系统始终停留在安全集 C_S 内。
- 随着参数估计误差界 ρ_i(t) 收缩，收紧项减小，内近似集趋近于真实隐式安全集 C_I，保守性逐渐降低。
- 通过平面四旋翼仿真验证了方法有效性：主控制器和标称 bCBF 均导致违规，而鲁棒自适应 bCBF 始终保持安全。

### 7. 优点

- **理论完善**：给出了从参数估计、流误差界、鲁棒条件到 QP 实现的完整理论链条，并证明了安全保证。
- **兼容性强**：估计器不限具体类型（只要满足逐元素认证即可），可灵活选用 DREM、RLS 等。
- **计算高效**：鲁棒条件通过对偶性转化为线性约束，最终保持 QP 形式，方便实时实现。
- **处理控制方向不确定性**：驱动矩阵中的不确定性通过对偶性处理，避免了传统方法中需要预设最坏情况下控制方向的问题。

### 8. 不足与局限

- **实验覆盖不足**：仅一个仿真例子，缺乏多场景、多参数、硬件实验验证，结论的一般性有待进一步确认。
- **假设较强**：要求参数上下界已知、备份控制器鲁棒前向不变、函数 Lipschitz 连续等。实际中这些条件可能难以严格满足。
- **离散化误差**：QP 约束在时域网格上施加，理论要求连续时间约束，实际需添加 margin 或细化网格，否则可能损失严格性。
- **可行性风险**：内近似集 \hat{C}_I(t) 可能为空（尤其初始参数界较宽时），需要备选切换策略（作者提及了切换至鲁棒备份控制器，但未详细分析切换平滑性）。
- **未考虑测量噪声**：假设状态完全可测，实际系统中状态估计误差会进一步增加不确定性。

（完）
