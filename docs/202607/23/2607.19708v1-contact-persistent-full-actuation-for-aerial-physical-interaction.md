---
title: Contact-Persistent Full Actuation for Aerial Physical Interaction
title_zh: 持久接触下的空中物理相互作用全驱动控制
authors: "Abhimanyu Khadga, Abhinav Sinha, Shashi Ranjan Kumar"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19708v1"
tags: ["query:uav-control"]
score: 8.0
evidence: 无人机物理交互的全驱动控制
tldr: 全驱动无人机认证通常基于秩条件或自由飞行性能，但在持续接触任务中不完整。本文提出接触持久全驱动理论，将任务力消耗后的残余力作为稳定性与机动性的度量。通过定义可行力多面体与残余权限半径，证明接触持久全驱动等价于任务力位于多面体内部。数值实验表明，满秩不足以保证接触可行，而中间倾角能更好保留残余力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19708v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 635, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19708v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1844, \"height\": 705, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19708v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 827, \"height\": 656, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19708v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19708v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1767, \"height\": 709, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19708v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 906, \"height\": 675, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19708v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 761, \"height\": 364, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19708v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1869, \"height\": 452, \"label\": \"Table\"}]"
motivation: 现有全驱动认证在接触交互中不足，需要任务依赖的可行性度量。
method: 结合刚体模型与形态力映射，定义可行力集、残余力集及残余权限半径。
result: 接触持久全驱动等价于任务力在内点，残余权限半径即到边界距离；满秩不保证接触可行。
conclusion: 提出残余权限边距作为安全滤波器，中间倾角最优，小/大倾角因侧向力或悬停裕度不足失效。
---

## 摘要
全驱动无人机通常通过控制分配矩阵的秩条件或自由飞行跟踪性能来认证。对于空中物理相互作用，这种认证可能不完整。在持续接触过程中，部分可用力旋量被交互任务消耗，仅剩余力旋量可用于稳定、扰动抑制和机动。本文引入了“持久接触下的全驱动”控制理论框架。将$\R^{3}\times\SO\left(3\right)$上的刚体模型与形态相关的力旋量映射相结合，该映射捕获固定倾斜、可变倾斜、同轴和过驱动的多旋翼架构。我们定义了执行器限制下的可行力旋量集、任务负载下的剩余力旋量集以及剩余控制裕度，这些裕度强化了通常基于秩的全驱动概念。主要结果表明，持久接触下的全驱动等价于任务力旋量位于受约束的可行力旋量多面体内部，并且剩余控制半径恰好是到多面体边界的距离。我们进一步引入了针对不可行和边界情况的带符号剩余裕度证书、松弛最大化分配证书以及鲁棒可实现性条件，该条件可用作裕度感知安全滤波器。对抽象倾斜六旋翼的数值评估表明，仅满秩并不保证可行的接触操作。中等倾斜角度在推动过程中保留了剩余控制裕度，而小倾斜或大倾斜由于侧向力不足或悬停裕度损失而失败。

## Abstract
Fully actuated unmanned aerial vehicles (UAVs) are usually certified through rank conditions on a control-allocation matrix or through free-flight tracking performance. For aerial physical interaction, this certification may be incomplete. During sustained contact, part of the available wrench is consumed by the interaction task, and only the residual wrench remains available for stabilization, disturbance rejection, and maneuvering. This paper introduces a control-theoretic framework for \emph{contact-persistent full actuation}. A rigid-body model on $\R^{3}\times\SO\left(3\right)$ is combined with a morphology-dependent wrench map that captures fixed-tilt, variable-tilt, coaxial, and overactuated multirotor architectures. We define feasible wrench sets under actuator limits, residual wrench sets under task loading, and residual authority margins that strengthen the usual rank-based notion of full actuation. The main result shows that contact-persistent full actuation is equivalent to interiority of the task wrench in the constrained feasible wrench polytope, and that the residual authority radius is exactly the distance to the polytope boundary. We further introduce a signed residual-margin certificate for infeasible and boundary cases, a slack-maximizing allocation certificate, and a robust implementability condition that can be used as a margin-aware safety filter. Numerical evaluation on an abstract tilted hexarotor shows that full row rank alone does not imply feasible contact operation. Intermediate tilt angles preserve residual authority during pushing, whereas small or excessive tilts fail because of lateral-force deficiency or hover-margin loss.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现有全驱动无人机的认证通常基于控制分配矩阵的秩条件（rank(M)=6）或自由飞行跟踪性能，但在持续接触任务（如推动、支撑）中，这些认证不完整。接触任务会消耗部分可用力旋量（wrench），剩余部分才用于稳定、扰动抑制和机动。因此，需要一种任务依赖的可行性度量。
- **整体含义**：本文提出**持久接触下的全驱动**（Contact-Persistent Full Actuation）概念，强调认证应关注任务力是否位于**可行力旋量多面体内部**，并量化**残余权限半径**（residual authority radius）。该框架为接触丰富场景下的无人机设计、控制分配和安全滤波提供了理论依据。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：定义受执行器限制的可行力旋量集 $\mathcal{W}(\boldsymbol{\alpha})$（紧凑凸多面体），以及任务负载后的残余力旋量集 $\mathcal{W}_{\text{res}}(\boldsymbol{\alpha}, \mathbf{w}_{\text{task}})$。持久全驱动要求存在 $\epsilon>0$ 使得 $\mathcal{B}_\epsilon(\mathbf{0}_6) \subseteq \mathcal{W}_{\text{res}}$，即任务力旋量在 $\mathcal{W}$ 内部。
- **关键技术细节**：
  - **定理1**：持久全驱动等价于 $\mathbf{w}_{\text{task}} \in \operatorname{int}(\mathcal{W})$，且最大残余半径 $\epsilon^* = \operatorname{dist}(\mathbf{w}_{\text{task}}, \partial\mathcal{W})$。
  - **半空间表示下的符号裕度**：若 $\mathcal{W} = \{\mathbf{w} : \mathbf{a}_j^\top \mathbf{w} \le b_j\}$，则 $\varphi_{\mathcal{W}}(\boldsymbol{\alpha}, \mathbf{w}) = \min_j (b_j - \mathbf{a}_j^\top\mathbf{w})/\|\mathbf{a}_j\|$ 为正表示内点。
  - **定理2（内部分配证书）**：若存在 $\boldsymbol{\lambda}_0 \in \operatorname{int}(\Lambda)$ 满足 $\mathbf{M}\boldsymbol{\lambda}_0 = \mathbf{w}_{\text{task}}$，则可得下界 $\epsilon_{\text{cert}} = \delta_\infty(\boldsymbol{\lambda}_0)/\|\mathbf{R}_M\|_{2\to\infty}$。
  - **松弛最大化分配证书（线性规划）**：寻找最大 $\delta$ 使得分配偏差在 $[\boldsymbol{\lambda}_{\min}+\delta\mathbf{1}, \boldsymbol{\lambda}_{\max}-\delta\mathbf{1}]$ 内且满足方程。
  - **鲁棒可实现性条件（Corollary 3）**：若 $\inf_{\mathbf{w}\in\mathcal{T}} \varphi_{\mathcal{W}}(\boldsymbol{\alpha}, \mathbf{w}) \ge \bar{w}_{\text{stab}} + \bar{w}_{\text{unc}}$，则所有含扰动命令均可行。
  - **裕度感知滤波器**：将需求命令投影到收紧的多面体上，保留指定裕度。
- **算法流程**（文字说明）：
  1. 根据形态参数 $\boldsymbol{\alpha}$，建立分配矩阵 $\mathbf{M}$ 和可行多面体 $\mathcal{W}$。
  2. 对给定任务集 $\mathcal{T}$，计算每个任务点的符号裕度，取最小值得到 $\varphi^*_{\text{push}}$。
  3. 若 $\varphi^*_{\text{push}} > 0$，则持久全驱动成立，且 $\epsilon^* = \varphi^*_{\text{push}}$；否则不可行。
  4. 可选：通过松弛最大化LP得到更紧的证书，或利用鲁棒条件设计安全滤波器。

## 3. 实验设计
- **数据集/场景**：使用**抽象对称六旋翼（hexarotor）**模型，无真实数据集。参数：$N=6$，$m=3.3\ \mathrm{kg}$，臂长 $\ell=0.35\ \mathrm{m}$，推力上限 $13\ \mathrm{N}$。任务场景为**定向推动（沿 $\mathbf{e}_1$ 方向）**，推动力 $\rho = F/(mg) \in [0,0.15]$，接触扭矩不确定性 $\|\boldsymbol{\mu}\| \le 0.04$，要求残余裕度 $\epsilon_0 = 0.025$。
- **基准（Benchmark）**：以**自由飞行跟踪性能**（rank条件、最小奇异值）作为对比，显示它们不足以认证接触操作。
- **对比方法**：
  - 不同固定倾斜角 $\gamma$（$0^\circ$、$10^\circ$、$20^\circ$、$30^\circ$、$36^\circ$、$37^\circ$、$40^\circ$、$50^\circ$）。
  - 固定形态 vs. 任务自适应形态（$\gamma$ 按方向最优选取）。
  - 符号裕度 $\varphi^*_{\text{push}}$ 与最小奇异值对比。
  - 不同推动力/扭矩限制下的两参数图。
  - 蒙特卡洛鲁棒性验证（20,000样本）。

## 4. 资源与算力
- **未明确说明**：论文为纯数值仿真（线性规划、多面体运算），未提及GPU型号、数量或训练时长。计算量小，普通CPU可完成。

## 5. 实验数量与充分性
- **实验组数**：
  - 形态扫描（约7个固定倾角，同时扫描连续范围）。
  - 方向自适应对比（8个方向或连续角度）。
  - 符号裕度与力需求关系（$\rho_{\max}$从0到0.30）。
  - 两参数（力-扭矩）可行性图。
  - 分配证书与精确裕度验证。
  - 裕度感知滤波器验证。
- **充分性与客观性**：实验覆盖了关键参数变化，与理论预测严格一致（如符号裕度正负交界对应可行性边界）。所有结果基于理论公式（如精确裕度解析式），无随机性偏差。较为充分，但缺少真实硬件实验和动态验证。

## 6. 主要结论与发现
- 持久接触下的全驱动等价于任务力旋量位于可行集内部，残余半径即到边界距离。
- **满秩（rank=6）不足以保证接触可行**：例如 $\gamma=10^\circ$、$20^\circ$ 虽然满秩，但符号裕度为负，推动任务不可行。
- **中等倾斜角（约36°）最优**：能保留最大残余权限和认证推动力（$F^*_{\text{push}}/mg\approx 0.188$）。
- **任务自适应形态**比固定形态最大方向容量提升31.3%，平均提升8.7%，但最差方向容量基本相同。
- **所提裕度证书和滤波器可以鲁棒地保证规定稳定裕度**，经蒙特卡洛验证无违例。

## 7. 优点
- **理论创新**：首次从残余权限视角定义接触持久全驱动，超越了单纯秩条件。
- **可计算性**：符号裕度、松弛最大化证书等均为低复杂度线性/凸规划，易于实现。
- **通用性**：框架适用于固定倾斜、可变倾斜、同轴、过驱动等多种形态。
- **实用性**：推导了鲁棒可实现性条件和安全滤波器，可直接用于控制分配和命令修改。

## 8. 不足与局限
- **静态分析**：仅考虑准静态平衡，未涉及动态过程（如伺服速率、瞬态姿态变化、饱和期间的稳定性）。
- **变倾斜形态描述不足**：仅对比了任务自适应准静态情况，未建模伺服速率和暂态可行性。
- **无硬件实验**：所有结果基于数值仿真，未在真实飞行平台上验证，实际执行器、噪声、延迟等可能影响。
- **单位归一化依赖**：力与扭矩归一化使用特征长度 $\ell$，但不同平台尺度差异可能改变趋势，论文也注明了这一点。
- **应用限制**：要求任务集紧致且形态参数已知；对于非凸或动态变化的任务集，需额外处理。

（完）
