Title: Bound-Optimized Task Choice for Path Integral Control

URL Source: https://arxiv.org/pdf/2607.23866v1

Published Time: Tue, 28 Jul 2026 01:21:18 GMT

Number of Pages: 7

Markdown Content:
# Bound-Optimized Task Choice for Path Integral Control 

Rylie Anderson 1, Goutam Das 2, Takashi Tanaka 3

Abstract — Path Integral (PI) control is a powerful sampling-based method for stochastic optimal control, but it requires a restrictive coupling between the noise covariance and the con-trol cost matrix that is rarely satisfied in practice, particularly in aerospace and cyber-physical systems. We propose Bound-Optimized Task Choice (BOTC), a framework that optimizes over the entire space of valid approximations, termed tasks, satisfying the PI coupling constraint. We prove that every task provides an upper bound on the true cost-to-go and that BOTC minimizes this bound. We derive a change-of-measure formulation that enables evaluation of all candidate tasks from a single set of Monte Carlo samples, eliminating the need to resample for each candidate task. The resulting optimization is parameterized by a positive semi-definite ma-trix. Furthermore, we propose a novel Normal-Inverse-Wishart distribution-based importance sampling scheme to improve global optimization. We validate BOTC on a finite-horizon stochastic linear-quadratic regulator problem, demonstrating that it tracks the constrained optimum. 

I. I NTRODUCTION 

Traditionally, non-linear, stochastic optimal control requires solving the non-linear Hamilton-Jacobi-Bellman (HJB) par-tial differential equation (PDE). However, the HJB is in-tractable for practical high-dimensional systems. PI con-trol is an alternative stochastic optimal control framework based on Monte Carlo trajectory sampling [1], [2]. The log-transformed HJB equation is linearized if a specific linear relationship between control cost and covariance holds: 

Σ(f ) = λB (f )R−1BT  

> (f)

(1) where Σ(f ) is noise covariance, B(f ) is the control effect matrix, and R is quadratic control cost. The (f ) subscripts denote the control and covariance matrices acting on the full state space rather than the directly actuated subspace. Applying the Feynman-Kac Lemma [3], the linearized HJB equation may be converted into an expectation over uncon-trolled trajectory cost, which can be evaluated with Monte Carlo methods. For applicable problems, PI control efficiently solves the HJB equation while avoiding its most common pitfalls: PI control is solved entirely forward in time and does not require explicit calculation of the cost gradient. PI control has since been further refined and success-fully applied to numerous challenging control problems. [4], [5] extended the framework to analogous discrete MDPs.      

> This work is supported by DARPA COMPASS program grant HR0011-25-3-0210 and AFOSR DSCT program grant FA9550-25-1-0347. All au-thors are associated with the Networked Control Systems lab at Purdue University. Emails: 1ande1946@purdue.edu, 2das347@purdue.edu, and 3
> tanaka16@purdue.edu.

[6] proposed the highly practical MPPI algorithm, which introduced an iterative update law. [7] demonstrated the effectiveness of MPPI in the non-linear, noisy environment of aggressive driving. [8] further extended MPPI to encompass uncertainty in system dynamics and implemented PI control on a wide variety of robotics tasks. Recently, [9] demon-strated the feasibility of MPPI in a quadcopter navigation task. Although the typical interpretation of (1) is as an intuitive connection between state noise and control authority [10], the constraint has broader implications. In many interesting ap-plications, including aerospace and cyber-physical systems, controls are designed primarily to navigate deterministic dynamics rather than simply reject perturbations. In such cases, as noted in [6], the constraint shifts the relative balance between actuator penalties in a manner with no physical meaning, impacting policy synthesis. [11] proposed an iterative solution to the same class of problems without assuming (1), but the proposed algorithm is both substantially more computationally intensive and requires sufficiently dif-ferentiable cost functions. In practice, to meet the requirements of (1), control cost, covariance, or both must be modified. Two simple but informative approaches are to either: allow covariance to imply control cost; or allow control cost to imply covari-ance. In either case, the implied term must be scaled by 

λ be to positive semi-definitely greater than its original. Both approaches distort the true control problem. The first approach overestimates control cost, favoring under-actuated strategies. The second disproportionately penalizes noise-sensitive trajectories, producing excessively risk-averse con-trol strategies. Both options introduce a systematic bias into policy synthesis, yet both approaches actually solve a harder control problem, giving an upper bound on the true expected cost. Handling of this trade-off in the literature has varied. [8] employed the first approach, disregarding control cost entirely. [7] struck a balance between these extremes, but that balance requires manual tuning through trial-and-error. Many practical control problems do not naturally satisfy the exact equality condition of (1) and must be approximated to a harder problem that does. The two possible methods of approximation listed above are simply the two extremes of a continuous space of valid approximations. No existing work optimizes over the full space of valid problem approx-imations. In this work, we address this gap by introducing Bound-Optimized Task Choice (BOTC). In this paper, we define and examine the space of all valid problem approximations and find the subspace of potentially arXiv:2607.23866v1 [eess.SY] 26 Jul 2026 optimal problem approximations. We present a method of estimating the expected cost as a function of a decision variable parameterizing this entire space without resampling. We explore a novel importance sampling scheme based on the Normal-Inverse-Wishart distribution. Finally, we validate BOTC against a simple LQR system. The remainder of this paper is organized as follows. Section II reviews PI control. Section III defines the space of valid problem approximations. Section IV optimizes ex-pected cost over the space of all valid approximations. Section V presents and analyzes the results of a simple LQR experiment. II. P ATH INTEGRAL CONTROL 

In this section, we review path integral control as a method of stochastic optimal control applicable to non-linear systems, affine in control, with quadratic control cost. Let x ∈ Rn be the state of the system. Let u ∈ Rm be the control signal. Let B(f ) ∈ Rn×m be the control effect matrix, where m is the number of control channels and n is the number of state dimensions. Finally, let dξ ∈ Rn be a Brownian disturbance. Therefore, the system may be modeled as: 

dx = f (x, t )dt + B(f )udt + dξ (2) where E[dξ idξ j ] = Σ (f ),ij , so Σ(f ) is the covariance of state noise. 

Assumption 1. Both Σ(f ) and B(f ) are constant. 

When constrained by (1), the range of B(f ) and the positive support of the noise must share the same state dimensions. Then, Σ(f ) and B(f ) may be partitioned into zero and non-zero components: 

B(f ) =

 0

B



, Σ(f ) =

0 00 Σ



(3) where B ∈ Rl×m and Σ ∈ Rl×l. Here, l ≤ n is the number of directly actuated state dimensions, so Rl×l is the directly actuated subspace that noise may act in. Although in some other work [12], Σ and B may be functions of x and t,Assumption 1 is necessary for the remainder of this paper. Finally, define the cost function with quadratic control cost and arbitrary state and terminal costs. Let ϕ(x) be terminal cost, q(x, t ) be state cost matrix, and R ∈ Rm×m be the quadratic control cost. Therefore, the expected cost becomes 

V (x, t ) = min  

> u

E



ϕ(x(tf )) +

Z tf

> t

h

q(x(τ ), τ ) + 12 u(τ )T R u (τ )

i

dτ 



(4) where the expectation is taken over trajectories of (2) under the admissible control policy u.For a system of the form of (2) and cost function of form (4), the Stochastic Hamilton-Jacobi-Bellman equation is 

− ∂tV = q(x, t ) + f (x, t )T ∇xV + 12 Tr(Σ (f )∇xx V )

− 12 ∇xV T B(f )R−1BT 

> (f)

∇xV (5) where optimal control is u∗ = −R−1BT ∇xV [1]. However, solving this non-linear PDE is extremely difficult for com-plex systems. Instead, we introduce the logarithm transform 

V = −λ log Ψ and make the simplifying assumption that 

Σ(f ) = λB (f )R−1BT 

> (f)

, or equivalently, Σ = λBR −1BT .Under the transform and assumption, the non-linear terms of the stochastic HJB equation cancel, reducing to a linear PDE with condition Ψ( x(tf ), t f ) = exp( λ−1ϕ):

∂tΨ = Ψ

λ q − f T ∇xΨ − 12 Tr(Σ (f )∇xx Ψ) (6) This linear PDE belongs to a class that may be transformed into an expectation by the Feynman-Kac Lemma [3]. Under Feynman-Kac, (6) becomes an expectation of a function of total path cost under uncontrolled dynamics: 

Ψ( x, t ) = EP



exp 



− Sλ

 

(7) where S = ϕ(x(tf )) + R tf 

> t

q(x(τ ), τ ) dτ is the total un-controlled path cost and P is the probability measure for uncontrolled dynamics ( u = 0 ). This expectation may be effectively calculated with Monte Carlo methods by sampling and evaluating uncontrolled trajectories. III. P ROBLEM APPROXIMATION 

This section introduces the concept of problem approxima-tion : the process of choosing and solving an alternative, tractable problem that provides a bound on the original problem’s cost function as well as a valid control sequence. For path integral control, this practice is already widespread, albeit implicit. When applying (7) in practice, the resulting 

Ψ is calculated for the control cost matrix implied by Σ

through the linear constraint, rather than the original R. As we will demonstrate, the expected cost will be at least the original expected cost because new problem defined by the implied control cost is harder .

Definition 1 (Hardness) . One problem is harder than another if its optimal expected cost is greater-than-or-equal to the other’s for all possible x and t:

V (x, t |θeasy ) ≤ V (x, t |θhard ) ∀x ∈ Rn, t ∈ [t0, t f ]

(8) 

where θeasy and θhard are the parameters defining the easy and hard problems, respectively. 

Although simply allowing R to be implied by Σ is the simplest method, it is neither the only nor generally optimal approximation. The constraint Σ = λBR −1BT admits infinitely many solutions if we permit positive semi-definite increases to both Σ and R. We refer to each such modified problem as a task . For any Σ+, R + ≥ 0, a new task is defined by the coupling applied to the augmented parameters. 

0 ≤ Σ + Σ + = λB (R + R+)−1BT (9) Furthermore, we will demonstrate that under the following assumption, this task is harder than the original problem. 

Assumption 2. The columns of B span Rl.Lemma 1. Under Assumption 2, the cost-to-go of a task is greater than or equal to the original’s if its R and Σ matrices are positive semi-definitely greater. 

V (x, t |Σ, R, λ ) ≤ V (x, t |Σ + Σ +, R + R+, λ )

∀Σ+ ≥ 0, R + ≥ 0 (10) 

Proof. This property is true by definition for R+, as the control cost at each timestep will be at least that of the original for any possible control sequence. 

uT Ru ≤ uT (R + R+)u = uT Ru + uT R+u, 

∀u ∈ Rm, R + ≥ 0 (11) As controls are linear with dynamics and quadratic with cost, there exists some optimal control sequence eu [13]. Since only directly actuated state dimensions may be affected by noise, per (3), any additional noise Σ+ ≥ 0 may be considered a stochastic addition to the state-effect of the existing policy. 

Budt = Beudt + deξ, deξ ∼ N (0 , Σ+) (12) Any deviation from the deterministic control sequence within the range of B results in a different but still admissible control policy. Since eu is an optimal solution, no deviation within the admissibly policy set can improve expected cost. From Assumption 2, deξ ∈ Range( B) = Rl everywhere. Therefore, the expected cost with covariance Σ + Σ + will be greater-than-or-equal to the original. □

Since the solution for R+ is unique for a given λ and Σ+

under Assumption 2, λ and Σ+ can entirely parameterize the space of valid tasks. However, Σ+ is now bounded to values that admit a solution for R+. Substituting (9) into inequality 

B(R + R+)−1BT ≤ BR −1BT and rearranging, we obtain: 

0 ≤ Σ+ ≤ λBR −1BT − Σ (13) 

Lemma 2. The optimal λ, denoted λ∗, is always the minimum value admitting solutions to the following linear constraint. 

min  

> λ

s.t Σ ≤ λBR −1BT (14) 

This value may be calculated as a max eigenvalue problem: 

λ∗ = Λ max (( BR −1BT )− 12 Σ( BR −1BT )− 12 ) (15) 

Proof. For a given valid task defined by ˆΣ, ˆR, and ˆλ satis-fying ˆΣ = ˆλB ˆR−1BT , any increase in λ can be considered a positive semi-definite addition to covariance. 

ˆΣ + ˆΣ+ = ( ˆλ + ∆ ˆλ)B ˆR−1BT

ˆΣ+ = ∆ ˆλBR −1BT (16) As demonstrated in Lemma 1, adding to either variable positive semi-definitely results in a greater-than-or-equal cost-to-go. By monotonicity, the minimum possible λ must be optimal. The minimum possible λ is the minimum that admits a solution to (9). Substituting Σ ≤ Σ + Σ + and λB (R +

R+)−1BT ≤ λBR −1BT into (9), we find Σ ≤ λBR −1BT

as the condition for viability. According to [14], LMIs of the form A ≤ λB may be solved as the maximum eigenvalue of B− 12 AB − 12 . □

Combining Lemma 1 and Lemma 2, we find that task choice is parameterized solely by Σ+, and the optimal task choice must lie along the following linear—but singular— constraint: 

0 ≤ Σ+ ≤ λ∗BR −1BT − Σ (17) Because the expected cost-to-go of any task approximated from a given problem will always be greater than that of the original problem, it serves as an upper bound on the problem’s expected cost. Although it is not possible to access the true cost directly with path integral methods, by minimizing expected cost over the space of all valid tasks, we minimize this upper bound on the expected cost-to-go. IV. T ASK CHOICE 

In this section, we propose a method of efficiently optimizing expected cost over the space of valid tasks formulated in Section III. First, we will demonstrate that it is possible to re-weight existing Monte Carlo samples drawn from any given Gaussian distribution in terms of any other Gaussian probability measure without resampling in discrete time. Then, we will formulate the problem of minimizing expected cost as an optimization problem over the covariance of this new probability measure. 

A. Covariance Scaling 

To find the expected cost under an updated covariance, 

Σ + Σ +, we will employ the likelihood ratio between the original probability measure and one defined by updated covariance. The approach is similar to the generalized im-portance sampling derived in [6], although the purpose is different. 

Assumption 3. Σ is non-singular. 

Lemma 3. Under Assumption 3, the log-transformed ex-pected cost Ψ for a task parameterized by Σ+ is given by 

Ψ = EP

 |Σ||Σ + Σ +|

 N

> 2

· exp 

 12 Tr((Σ −1 − (Σ + Σ +)−1)Z) − Sλ∗

 

Z =

> tf

X

> t=t0

z(t)z(t)T , z(t) = ∆x(t)∆t − f (x(t), t ) (18) 

where N is the number of time steps and Z is the scatter matrix of deviations from deterministic dynamics. Proof. Define new gaussian probability measure Q with covariance Σ + Σ +. Assumption 3 implies that Q ∼ P , as both probability measures assign non-zero probability to all values in Rl×l. If Q ∼ P , then there exists some f (ω) such that dQ (ω) = g(ω)dP (ω), and likewise some gx(x[t0,t ])

such that dQ x(x[t0,t ]) = gx(x[t0,t ])dP x(x[t0,t ]). Applying this change of measure to (7), we find: EQ



exp 



− Sλ

 

= EP



gx(x[t0,t f ]) · exp 



− Sλ

 

(19) By the Markov property, the likelihood ratio is the product of the probability of the next state, given the current state, and the probability of its history. Then, from the system definition in (2), the likelihood of the next state given the current is a function of the state derivative. 

dQ x

dP x

(x[t0,t +∆ t]) = dQ x

dP x

(xt+∆ t|xt) · dQ x

dP x

(x[t0,t ])=

 dQ x

dP x

 ∆xt+∆ t

∆t

 

· dQ x

dP x

(x[t0,t ])

Combining the recurrence relation, we obtain: 

gx(x[t0,t f ]) = 

> N

Y

> i=0

 dQ x

dP x

 ∆xti+∆ t

∆t

 

(20) Substituting the Gaussian PDF with mean f (x, t ) for both measures yields 

gx =

 |ΣP ||ΣQ|

 N

> 2

· exp 

 12 Tr((Σ −1 

> P

− Σ−1 

> Q

)Z)



(21) where multiplied exponentials have been contracted into a summation inside the exponential function and path-invariant terms have been either canceled or been combined into the determinant ratio. Finally, substituting (21) and ΣQ and ΣP

definitions into (19), we obtain (18). □

Remark 1. The likelihood ratio method as applied in this section is only possible in discrete time. In continuous time, likelihood ratios between measures of different covariance are always zero or positive infinity. This can be seen by taking the limit of (21) as ∆t → 0.B. Task Optimization 

Lemma 3 provides an expression for Ψ in terms of additive covariance Σ+. As Σ+ parameterizes the entire space of valid tasks, maximizing (18) gives the optimal task, provid-ing the lowest upper bound on the original problem’s ex-pected cost. In its current form, the matrix decision variable is inverted. However, we show that with a simple change of variables, (18) becomes a practical optimization problem over the space of all valid tasks. 

Proposition 1. After the invertible change of variables 

(Σ + Σ +)−1 = Σ −1 − D (22) 

the optimal task choice may be found as the solution to the following optimization problem. 

max 

> D

(

(|Σ|| Σ−1 − D|) N

> 2
> K

X

> k

exp 

 12 Tr( DZ k) − Sk

λ∗

)

(23) 

0 ≤ D ≤ Σ−1 − λ∗−1

(BR −1BT )−1 (24) 

Proof. Substituting (22) into (18), simplifying, and convert-ing the expectation to an empirical average over K Monte Carlo paths gives 

max 

> D

( |Σ||(Σ −1 − D)−1|

 N

> 2

·

> K

X

> k

exp 

 12 Tr( DZ k) − Sk

λ∗

)

(25) where Zk and Sk are the scatter matrix and total cost terms for the k-th sample, respectively. Recognizing that the determinant of an inverse is the inverse of the determinant and canceling terms simplifies the expression to (23). Since A ≤ B → B−1 ≤ A−1, ∀A, B > 0, then (17) can be inverted: 

λ∗−1

(BR −1BT )−1 ≤ (Σ − Σ+)−1 ≤ Σ−1

After substituting in (22) and rearranging, we obtain (24). 

Σ+ subject to (17) parameterizes the entire space of valid and potentially optimal tasks. Since Σ+ → D is a bijective mapping, D subject to (24) also parameterizes the entire task space. Therefore, the global maximizer of (23) is the globally optimal task. □

C. Numerical Considerations 

Since the upper bound on D given in (24) will always be singular, solving the problem in (23) as is would be quite difficult. Instead, the problem must be reformulated in the 

D upper bound’s non-singular subspace. This non-singular subspace may be found with eigen-decomposition of the upper bound of D:

Σ−1 − λ∗− 1BR −1BT = EM E T (26) where eigenvalues are sorted in ascending order and at least one eigenvalue will be 0 due to the choice of λ∗. Therefore, under the change of basis E, we may partition D and Σ−1 −

λ∗BR −1BT along the same indices. 

D = E

0 00 eD



ET , EM E T = E

0 00 fM



ET (27) Determinants and quadratic forms are independent of basis, so (23) can be represented in the basis of E as 

max 

> eD

(

(|Σ|| ET (Σ −1 − D)E|) N 

> 2

·

> K

X

> k

exp 

 12 Tr( eZk eD) − Sk

λ∗

)

(28) where eZ = ET ZE  

> (p)

and the (p) index denotes the bottom-right sub-matrix associated with positive eigenvalues in M and excluding the dimensions where ET DE is zero. Finally, due to the block structure of D, we can apply a Schur Complement to the determinant to reduce the dimen-sionality of the determinant. First, partition ET Σ−1E along the same dimensions as D.

T = ET Σ−1E =

T(z) T T

> (c)

T(c) T(p)



(29) Then, perform the Schur Complement on the matrix deter-minant and consolidate constants into H.

max 

> eD

(

(|Σ|| T(z)|| H − eD|) N

> 2
> K

X

> k

exp 

 12 Tr( eZ eD) − Sk

λ∗

)

0 ≤ eD ≤ fM , H = T(p) − T(c)T −1(z) T T

> (c)

(30) With this change of variables, we have an optimization problem over the full-rank PSD decision variable eD.

D. Wishart Importance Sampling 

In previous sections, we found an optimization problem theoretically yielding the optimal task, yet practical issues remain. Each sample’s contribution to Ψ( x, t ) will decay rapidly where Zk is distant from (Σ −1 −D)−1. However, Zk

will tend towards the sampling distribution’s covariance, so the estimate given in (23) will only be accurate in that neigh-borhood. Accuracy across the entire optimization domain would require drawing samples from a continuous range of covariances. Although such a scheme complicates im-portance sampling, the Normal-Inverse-Wishart (NIW) prior provides a closed-form solution for the marginal likelihood of Z. The NIW models the process of drawing a covariances matrix from an Inverse-Wishart distribution, then drawing N

samples from the Gaussian defined by the matrix. However, 

Σ+ is under the singular constraint of (13), and most samples drawn from the Inverse-Wishart distribution will not lie along this constraint, contributing little to the cost estimate. To address this shortfall, we define the Inverse-Wishart prior over the non-singular subspace of (13): 

Σ+ = G

0 00 Ξ+



GT , λ∗BR −1BT − Σ = GV G T

(31) where Ξ+ ∈ R(l−1) ×(l−1) is PSD and G is the eigenvector matrix of λ∗BR −1BT − Σ. Then, adding Σ to get the full covariance matrix, we transform Σ into the basis G and factor it into a singular matrix [α, β ][ α, β ]T and the partitioned block sub-matrix, ΞΣ.

Σ + Σ + = G

  α2 αβ T

αβ ββ T



+

0 00 ΞΣ + Ξ +

  

GT (32) Since we need to sample a single PSD matrix from the prior, absorb Ξ+ and ΞΣ into Ξ. Furthermore, we can transform the resulting Z draw into the basis of G to simplify the Wishart definition. We now have a well defined NIW system: 

Ξ ∼ IW (l−1) ×(l−1) (v, Y ), ZG ∼ W l×l(U, N )

U =

α2 αβ T

αβ ββ T + Ξ 



, ZG = GT ZG (33) 

Lemma 4. Under the constrained Inverse-Wishart prior defined over the non-singular subspace of (13) , the marginal likelihood of Z is 

p(Z) = exp   − Z(z)

> G
> 2α2

Γl−1( N +v 

> 2

)|Y |v/ 2|Z|(N −l−1) /2

2N/ 2αN Γl( N 

> 2

)Γ l−1( v 

> 2

)| ¯ZG + Y |(N +v)/2

¯ZG := α−2Z(z) 

> G

ββ T + Z(p) 

> G

− 2α−1Z(c) 

> G

βT

(34) 

Proof. The marginalization over Z is an integral over the domain of the likelihood: 

p(Z) = 

Z

> S+

p(ZG|Ξ) p(Ξ) dΞ (35) where S+ is the set of PSD matrices. Next, we substitute in the prior and likelihood expressions. 

p(Z) = 

Z

> S+

|ZG|(N −l−1) /2|Y |v/ 2

2(N l +vl −v)/2|U |N/ 2Γl( N 

> 2

)Γ l−1( v 

> 2

)|Ξ|(v+l)/2

· exp( − 12 (Tr( ZGU −1) + Tr( Y Ξ−1)) dΞ (36) Now, since the likelihood is defined by U while the prior is over Ξ, we do not automatically have conjugacy as demonstrated in [15]. The problematic terms are Tr( ZGU −1)

and |U |. These terms can be shown to be proportional to 

Tr( ZGΞ−1) and Ξ, so conjugacy may be regained. We will first demonstrate this for |U | by applying a Schur complement along the existing partition: 

|U | = α2|ββ T + Ξ − α−2αβαβ T | = α2|Ξ| (37) Next, simplifying Tr( ZGU −1) requires a block inversion along the existing partition. After minor simplification, we find: 

U −1 =

α−2 + α−2βT Ξ−1β −α−1βT Ξ−1

−α−1Ξ−1β Ξ−1



(38) 

Tr( ZGU −1) may alternatively be considered a sum over all indices of the Hadamard product ZG ⊙ U −1. Additionally, 

βT Ξ−1β = Tr( ββ T Ξ−1), so the trace becomes 

Tr( ZGU −1) = 

α−2Z(z) 

> G

+ Tr( α−2Z(z) 

> G

ββ T Ξ−1) + Tr( Z(p) 

> G

Ξ−1)−

α−11T (Z(c) 

> G

⊙ βT Ξ−1)1 − α−11T (Z(c) 

> G

⊙ Ξ−1β)1 (39) where ZG has been partitioned along the same axes as U −1:

ZG =

"

Z(z) 

> G

Z(c)

> G

Z(c) 

> G

Z(p)

> G

#

(40) Finally, a sum over elements of the Hadamard prod-uct of two vectors is equivalent to their dot prod-uct. Therefore, 1T (Z(c) 

> G

⊙ βT Ξ−1)1 = βT Ξ−1Z(c) 

> G

=Tr( Z(c) 

> G

βT Ξ−1). Since Ξ−1 is symmetric, 1T (Z(c) 

> G

⊙

Ξ−1β)1 = Tr( Z(c) 

> G

βT Ξ−1) as well. Applying these sim-plifications as well as the linearity of traces, we get 

Tr( ZGU −1) = α−2Z(z) 

> G

+ Tr( ¯ZGΞ−1) (41) where ¯ZG is as defined in (34). Returning to the marginalization, substitute (37) and (41) into (36) and perform basic simplifications. 

p(Z) = 

Z

> S+

|ZG|(N −l−1) /2|Y |v/ 2

2(N l +vl −v)/2Γl( N 

> 2

)Γ l−1( v 

> 2

)αN |Ξ|(N +v+l)/2

· exp 



− Z(z)

> G

2α2



· exp 



− 12 Tr(( Y + ¯ZG)Ξ −1



dΞ (42) Now, recognize the Inverse-Wishart kernel of 

IW (l−1) ×(l−1) (N + v, Y + ¯ZG), which must integrate to one. Therefore, marginal likelihood must be the ratio between the existing terms constant wrt Ξ and the normalization terms of the updated Inverse-Wishart kernel. This ratio simplifies to (34). □

Applying this marginal likelihood expression to impor-tance sampling, the likelihood ratio actually simplifies if we use the Wishart likelihood for the variable probability measure parameterized by D.

p(Z|D) = |Σ−1 − D|N/ 2|Z|(N −l−1) /2

2N l/ 2Γl( N 

> 2

)

· exp 

 12 Tr( Z(D − Σ−1)) 



(43) 

Proposition 2. Under the constrained NIW sampling scheme with marginal likelihood given by Lemma 4, the desirability function is 

Ψ = αN Γl−1( v 

> 2

)

2N (l−1) /2Γl−1( N +v 

> 2

)|Y |v/ 2 |Σ−1 − D| N

> 2
> K

X

> k=1



| ¯ZGk +Y | N +v 

> 2

exp 

 12 Tr( Zk(D−Σ−1)) − Sk

λ∗ + Z(z)

> Gk

2α2

 

(44) 

Proof. Substituting the ratio between (43) and (34) into (19), canceling terms, and converting to an empirical average over 

K samples yields (44). □

Although (44) is quite complicated, all additional terms except | ¯ZGk + Y | and Z(z) 

> Gk

are sample-invariant. All D-dependent terms are identical in form to (23), so the reduced-dimension formulation of Section IV-C applies directly. 

Remark 2. The methods described in this section may be extended to integrate over an Inverse-Wishart prior of lower rank as well by recursively applying (37) and (41) with successive bases and rank one perturbations. E. Simple Algorithm 

In this section, we present a very simple, online algorithmic implementation of the BOTC framework. The most obvious approach is to insert the task choice step between trajectory simulations and control generation, as defined in the original [1] formulation. 

Algorithm 1: Simple Online BOTC-PI 

Data: R, Σ, ϕ, q: problem parameters 

while task not complete do 

Collect K trajectory samples ; Solve optimization problem for D from either (23), (30), or (44) ; 

u∗ ← PKi=0 gx(D) exp 



− Sk

> λ∗

 dξ i 

> dt

;Send u∗(0) to actuators ;             

> Fig. 1. Static sampling: estimated (colored) and true (black) expected task cost as a function of constraint surface fraction ρ, with K= 1000 and
> ∆t= 0 .05 .
> Fig. 2. Wishart sampling: estimated expected task cost across the full constraint surface, with Inverse-Wishart mean at ρ= 0 .5and v= 10 .

V. S IMULATION RESULTS 

In this section, we present the results of simulated ex-periments on a simple stochastic, two-dimensional, finite-horizon LQR system. We aim to demonstrate that the BOTC framework outlined in Section IV accurately estimates the expected task cost —the cost under the modified control cost and covariance—across the entire constraint surface. We chose LQR for this experiment because the expected cost may be calculated analytically for any given initial position and provides a ground truth for expected task cost across the entire constraint surface. The system used for this experiment is defined by: 

A =

0.1 0.10 0.1



, B =

1 01 1



Σ, R =

1 00 2



, Q =

1 00 1



where A is uncontrolled dynamics, B is the control effect matrix, Q is both running and terminal state costs, and 

Σ and R are as previously defined. Note that this system does not conform to the PI constraint (1). The infinite-horizon LQR optimal feedback gain is used as the importance sampling mean for all distributions. Initial state for all tests is 

x(0) = [3 4] T . Although the shapes of the resulting curves are entirely state-dependent, performance is analogous for all initial states. The initial state was chosen to give an interior minimizer for demonstration purposes. Because the problem has two control dimensions, the optimization problem is one-dimensional as described in Section IV-C. For static sampling experiments, sampling is performed from various distributions corresponding to points along the 0 ≤ eD ≤ fM optimization domain. For readability, sampling distributions are parameterized by fraction along constraint surface, ρ ∈ [0 , 1] . All static sampling distributions were calculated with K = 1000 samples and ∆t = 0 .05 .Wishart sampling was performed with an inverse-Wishart mean midway along the constraint surface and v = 10 .As demonstrated in Figure 1, the static sampling method provides an estimate closely tangent to the true cost surface at the point corresponding to its sampling distribution. As such, in the neighborhood of its sampling distribution, it provides a highly accurate estimate of both the expected cost and its gradient wrt the decision variable. Figure 2 illustrates both the advantages and drawbacks of Wishart sampling. Wishart sampling provides a reasonable estimate of the expected cost across the entire optimization domain. However, its estimate of the curve is occasionally erratic, and therefore its derivative is not as reliable. As demonstrated by the improvement from the K = 500 curve to the higher sample count curves, Wishart cost surface estimates become significantly more accurate with greater samples. The contrast between Figures 1 and 2 suggests that static sampling is preferable for local optimization, while Wishart sampling is required for global optimization. VI. C ONCLUSION 

In this paper, we have presented Bound-Optimized Task Choice (BOTC), a framework for stochastic control extend-ing path integral methods to problems not satisfying the coupling constraint on control cost and covariance. This framework provides methods for finding the globally optimal PI-compliant approximate problem, thereby minimizing the upper bound on expected cost. To achieve this, we have derived the theoretical basis for mapping an optimal control problem to possible approxima-tions on that constraint’s surface. We found the conditions under which an approximated task gives an upper bound on the original problem’s expected cost. Then, we reduced that space of valid tasks to the subset of potentially optimal tasks. Next, we adapted existing methods to the purpose of calculating expected cost under an arbitrary probability measure without resampling. Optimizing this expected cost over the space of viable tasks allows us to find the globally optimal task, providing the lowest upper bound on the original problem’s expected cost attainable with PI control. We present a novel importance sampling scheme based on the Normal-Inverse-Wishart distribution to improve global task space optimization. Finally, we validate these methods on a simple LQR system, demonstrating both the potential and limitations of this framework. Further work will apply BOTC methods to concrete non-linear control problems, including both offline problem anal-ysis and online task optimization. Future iterations of the basic algorithm presented in Section IV-E will apply iterative methods for task optimization. The local accuracy of static sampling lends itself to gradient methods, while the global accuracy Wishart sampling will enable such an algorithm to escape local minima. While this paper uses the most basic PI formulation as proposed by [1], future work should attempt to integrate BOTC methods with other PI frameworks such as MPPI [6] and the information theoretic formulation in [16]. REFERENCES [1] H. J. Kappen, “Path integrals and symmetry breaking for optimal con-trol theory,” Journal of statistical mechanics: theory and experiment ,vol. 2005, no. 11, p. P11011, 2005. [2] ——, “An introduction to stochastic control theory, path integrals and reinforcement learning,” AIP Conf. Proceedings , vol. 887, no. 1, pp. 149–181, 02 2007. [3] B. Øksendal, Stochastic differential equations . Springer, 2003. [4] E. Todorov, “Linearly-solvable Markov decision problems,” Advances in neural information processing systems , pp. 1369–1376, 2007. [5] ——, “Efficient computation of optimal actions,” Proceedings of the national academy of sciences , vol. 106, no. 28, pp. 11 478–11 483, 2009. [6] G. Williams, A. Aldrich, and E. A. Theodorou, “Model predictive path integral control: From theory to parallel computation,” Journal of Guidance, Control, and Dynamics , vol. 40, no. 2, pp. 344–357, 2017. [7] G. Williams, P. Drews, B. Goldfain, J. M. Rehg, and E. A. Theodorou, “Aggressive driving with model predictive path integral control,” in