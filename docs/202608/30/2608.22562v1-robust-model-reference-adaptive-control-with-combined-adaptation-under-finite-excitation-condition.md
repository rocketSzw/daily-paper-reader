---
title: Robust Model Reference Adaptive Control with Combined Adaptation under Finite Excitation Condition
title_zh: 有限激励条件下具有组合自适应的鲁棒模型参考自适应控制
authors: "Manish Patel, Arnab Maity"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22562v1"
tags: ["query:uav-control"]
score: 8.0
evidence: 面向多输入多输出非线性系统的自适应控制算法，可直接用于无人机自适应控制
tldr: 自适应控制中参数收敛通常依赖持久激励(PE)条件，但PE需未来数据，难以在线保证。针对一类含未知对角控制效率矩阵和非参数不确定性的MIMO非线性系统，提出基于修正Gram-Schmidt正交化构造记忆项的算法，在有限激励(FE)条件下使参数误差系数矩阵为单位阵，并设计组合自适应律。闭环跟踪与估计误差指数衰减到原点邻域，衰减率仅由用户设定增益和系统常数决定，与激励水平无关。该方法消除了时变增益需求，克服了并发学习、DREM等对激励水平的依赖。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22562v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1232, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1649, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 759, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 762, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1630, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1656, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1661, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 784, \"height\": 502, \"label\": \"Figure\"}]"
motivation: 参数收敛依赖持久激励(PE)条件，PE需未来数据难以在线验证，且现有方法收敛率受激励水平限制。
method: 提出修正Gram-Schmidt构造记忆项，在有限激励下使参数误差系数矩阵为单位阵，并设计组合自适应律。
result: 闭环跟踪与估计误差指数衰减至显式界邻域，衰减率仅由用户增益和系统常数决定，与激励无关。
conclusion: 在可在线验证的FE条件下实现参数理想收敛，消除对激励水平的依赖，优于并发学习等方法。
---

## 摘要
在自适应控制中，线性参数化形式的参数不确定性由未知参数和已知回归信号组成。未知参数收敛到其理想值需要回归器满足持续激励（PE）条件，而该条件依赖于未来数据，因此无法在线保证。基于记忆的参数更新律通过使理想参数在可在线验证的有限激励（FE）条件下收敛来解决这一问题。本文针对一类具有未知对角控制效能矩阵和有界非参数不确定性的多输入多输出非线性系统，提出了一种利用修正Gram-Schmidt正交化过程构造记忆项的新算法。在有限激励条件下，所构造的记忆项在参数估计误差动态中产生单位系数矩阵。该单位系数矩阵消除了对时变自适应增益的需求，能够给出参数估计误差的显式最终界，并在记忆项下保持非参数不确定性界的结构。在此基础上，针对有限激励条件下的控制器增益估计，开发了一种组合自适应律。闭环跟踪和估计误差被证明指数衰减到原点的一个邻域，该邻域以显式最终界为特征，衰减率仅取决于用户定义的增益和系统常数，与回归器激励水平无关。这消除了收敛速度对回归器激励水平的依赖，而这是并发学习、记忆回归器扩展和DREM等现有方法的一个关键局限性。

## Abstract
In adaptive control, parametric uncertainties in linear-in-parameter form consist of unknown parameters and known regressor signals. Convergence of the unknown parameters to their ideal values requires the regressor to satisfy a persistent excitation (PE) condition, which depends on future data and is therefore infeasible to guarantee online. Memory-based parameter update laws address this by enabling ideal parameter convergence under the online-verifiable finite excitation (FE) condition. In this paper, a new algorithm is proposed to construct a memory term via the Modified Gram-Schmidt orthogonalization procedure for a class of multi-input multi-output nonlinear systems with an unknown diagonal control effectiveness matrix and bounded nonparametric uncertainties. Under the finite excitation condition, the constructed memory term yields an identity coefficient matrix in the parameter estimation error dynamics. The identity coefficient matrix eliminates the need for time-varying adaptation gains, enables an explicit ultimate bound on the parameter estimation error, and preserves the structure of the nonparametric uncertainty bound under the memory term. Building on this, a combined adaptation law is developed for controller gain estimation under FE. The closed-loop tracking and estimation errors are shown to decay exponentially to a neighborhood of the origin, characterized by an explicit ultimate bound, with a decay rate that depends solely on user-defined gains and system constants, independent of the level of regressor excitation. This removes the dependence of the convergence rate on the level of regressor excitation, a key limitation of existing approaches such as concurrent learning, memory regressor extension, and DREM.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在自适应控制中，参数收敛通常依赖持续激励（PE）条件，而PE条件要求回归器在整个时间轴上持续丰富，本质上依赖于未来数据，无法在线验证和保证。
- **现有方法的局限**：
  - 并发学习（Concurrent Learning）、记忆回归器扩展（MRE）、DREM等方法将PE条件放宽为在线可验证的有限激励（FE）条件，但收敛率直接取决于回归器激励水平——对于病态矩阵，最小特征值或行列式可能极小，导致收敛缓慢或需要极大的时变自适应增益。
  - 记忆项通过对回归器矩阵进行非正交变换，将非参数不确定性以非线性方式混合，使得推导估计误差的显式最终界变得困难。
  - 现有方法（如DREM）需要多个滤波器，滤波器截止频率的选择缺乏系统性准则。
- **论文意义**：针对一类含未知对角控制效能矩阵和有界非参数不确定性的MIMO非线性系统，提出了一种新的记忆项构造算法，在FE条件下使参数估计误差动态的系数矩阵为单位阵，从而消除对时变增益的依赖、获得显式最终界，并使收敛率与激励水平完全解耦。

## 2. 论文提出的方法论

### 2.1 核心思想

- 通过**修正Gram-Schmidt正交化（Modified Gram-Schmidt, MGS）**直接对有限窗口内采集的回归器矩阵进行正交化，得到正交矩阵$\Psi_{fb}$，满足$\Psi_{fb}\Psi_{fb}^\top = I$。
- 对输出矩阵施加**相同的正交变换**，从而保持线性参数化结构：$y_{fb} = \Psi_{fb}^\top w + \xi_{fb}$，其中变换后的非参数不确定性$\xi_{fb}$的界可以显式推导。

### 2.2 记忆项构造算法（Algorithm 1）

- 采集$q$个时间实例的滤波回归器$\phi_f(t_i)$，构成矩阵$\Phi_{fc} \in \mathbb{R}^{q \times q}$；对应采集输出$y_f(t_i)$构成矩阵$Y_{fc}$。
- 使用MGS过程逐列正交化$\Phi_{fc}$得到$\Phi_{fb}$，正交化过程分为两步：先对候选方向$v_k$进行Gram-Schmidt投影，再检查残差范数$\|v_k\|$是否大于阈值$\delta_2$，确保列之间足够线性无关。
- 对$Y_{fc}$施加**相同的变换**得到$Y_{fb}$，通过数学归纳法证明线性参数化结构保持不变。
- 引入两个设计参数：$\delta_1$（最小可接受回归器范数）和$\delta_2$（最小可接受残差范数），保证对非参数不确定性的鲁棒性。

### 2.3 参数更新律（定理2）

- 参数更新律：$\dot{\hat{w}} = \gamma_w \Gamma_w \Psi_{fb}(y_{fb} - \Psi_{fb}^\top \hat{w})$
- 其中$\gamma_w$为激励标志：当$k \leq q$（正交基未构造完成）时$\gamma_w = 0$，估计保持不变；当$k > q$（正交基构造完成）时$\gamma_w = 1$。
- 由于$\Psi_{fb}$是正交矩阵，参数估计误差动态变为$\dot{\tilde{w}} = -\Gamma_w \tilde{w} + \Gamma_w \Psi_{fb}\xi_{fb}$，系数矩阵恰为单位阵（当$\Gamma_w$为标量增益时），误差分量之间**完全解耦**，收敛率$k_w = \lambda_{\min}(\Gamma_w)$仅由用户设定增益决定。
- 在无非参数不确定性时，误差动态退化为$\dot{\tilde{w}} = -\Gamma_w \tilde{w}$，指数收敛到零。

### 2.4 组合自适应律（定理3）

- 控制器增益更新律（36）包含两个模式：
  - **$\gamma_i = 0$时**：退化为标准σ-修正自适应律，保证闭环信号有界。
  - **$\gamma_i = 1$时**：激活记忆项驱动的组合自适应，利用参数估计$\hat{A}$、$\hat{\Lambda}$通过匹配条件间接计算控制器增益估计。
- 切换条件（37）：$\hat{\Lambda}\Lambda_s > 0$且$\lambda_{\min}(\hat{\Lambda}) > \lambda$，确保增益更新方向正确且控制效能矩阵估计充分良态。

### 2.5 稳定性保证

- 构造Lyapunov函数$V = e^\top Pe + \text{tr}(\tilde{K}_x^\top \tilde{K}_x \Lambda_s\Lambda) + \cdots + \tilde{w}^\top \tilde{w}$。
- 在FE条件下，闭环误差向量$\chi$满足$\|\chi(t)\|^2 \leq \kappa_1 \|\chi(t_q)\|^2 e^{-k(t-t_q)} + \rho_{ub}$，即**指数衰减到显式最终界**$\rho_{ub}$。
- 衰减率$k = \frac{\min(0.5\lambda_{\min}(Q), k_\Lambda, 1)}{\max(\lambda_{\max}(P), \lambda_{\max}(\Lambda_s\Lambda), 1)}$，仅取决于用户定义增益和系统常数，与回归器激励水平无关。

## 3. 实验设计

- **数据集/场景**：
  - **例1（参数识别）**：使用线性回归模型，分为两个研究。
    - Study 1：回归器仅为时间函数，对比收敛行为。
    - Study 2：回归器包含由已知微分方程生成的状态变量（状态相关回归器），更接近实际场景。
  - **例2（飞机纵向控制）**：采用Lavretsky 2009年论文中的飞机纵向动力学模型，含状态线性不确定性$\theta_1$、控制效能不确定性$\Lambda = 0.5$、高斯型非线性状态不确定性$\theta_2$（7个参数），跟踪攻角指令$\alpha_{cmd}$。
- **Benchmark**：以参数估计误差范数和追踪误差范数作为评估指标。
- **对比方法**：
  - 并发学习（Concurrent Learning, CL）
  - 记忆回归器扩展（Memory Regressor Extension, MRE）
  - DREM（Dynamic Regressor Extension and Mixing）
  - 以及Lavretsky的组合自适应控制器（用于飞机对比）。

## 4. 资源与算力

- **论文中未明确披露**任何与算力相关的信息，例如GPU型号/数量、训练时长、计算平台等。
- 仿真均在常规数值环境中完成（推测为MATLAB等标准工具），但没有提供具体硬件配置或运行时间。
- 如果读者关注算力需求的评估，需要结合实际复现成本另行估算。

## 5. 实验数量与充分性

- **实验数量**：共3组主要仿真场景（两个参数识别研究 + 一个飞机控制仿真）。
- **对比充分性**：
  - 参数识别Studies 1和2分别对比了CL、MRE、DREM和所提方法，覆盖了不同回归器结构（时间依赖和状态依赖），且区分了相同增益和调谐增益两种情形，对比客观。
  - 飞机控制仿真与Lavretsky的组合自适应控制器进行了对比，展示了跟踪精度和估计收敛优势。
- **不足**：
  - 缺少对算法参数（$\delta_1$、$\delta_2$）的敏感性分析/消融实验。
  - 没有涉及非参数不确定性存在时的鲁棒性验证（仿真中飞机部分假设$\xi = 0$）。
  - 缺乏与σ-修正方法在非参数不确定性下的直接对比。
  - 缺少真实硬件/飞行实验验证。

## 6. 论文的主要结论与发现

- 在FE条件下，通过MGS正交化构造的记忆项使参数估计误差动态的系数矩阵成为单位阵，参数误差分量解耦，收敛率仅取决于用户增益。
- 提出的组合自适应控制方案保证闭环跟踪误差和估计误差**指数衰减到原点邻域**，且最终界是**显式的**，可基于已知系统和设计参数计算。
- 与并发学习、MRE、DREM相比，所提方法的收敛率**不依赖回归器激励水平**，消除了激励水平对收敛速度的制约，无需针对激励水平调谐增益。
- 在飞机纵向控制的仿真中，所提方法在攻角跟踪精度和舵面偏转的平稳性上均优于Lavretsky的组合自适应控制器。

## 7. 优点

- **理论贡献突出**：首次在MIMO非线性系统+未知对角控制效能矩阵+非参数不确定性的框架下，实现了FE条件下的单位矩阵系数参数误差动态，获得显式最终界和激励无关的收敛率。
- **方法简洁实用**：基于MGS的算法实现简单，$\delta_1$、$\delta_2$两个参数有明确的物理意义（最小激励强度和最小线性独立角度），实际设置较为直观。
- **理论分析完整**：从滤波建模、正交化变换、不确定性传播界的推导到Lyapunov稳定性分析环环相扣，定理间逻辑严谨。
- **实验对比公平**：对CL、MRE、DREM在相同增益下对比后又调谐各自增益对比，体现了所提方法无需调谐的优势。

## 8. 不足与局限

- **仿真覆盖面有限**：未验证非参数不确定性$\xi \neq 0$时的闭环性能，而这是论文核心场景之一。飞机仿真仅在理想（无扰）条件下进行，削弱了鲁棒性声明的说服力（尽管最终界理论推导是有的）。
- **实验场景规模较小**：参数数量较少（q = 4和飞机系统），未展示高维场景下MGS构造效率和参数维度对最终界的影响。
- **未考虑实际工程因素**：未考虑传感器噪声、执行器饱和、离散化效应、通信延迟等非理想因素。
- **单数据集局限**：论文Remark 9也承认，单数据集更新在非参数不确定性显著时最终界较大，批处理/递归扩展虽被提及但未在本文中实现。
- **界偏保守**：Theorem 1的最终界推导中使用了一些保守估计（如$\|v_j\| < 1$），实际不确定性可能远小于上界，论文也承认这一点，且未在仿真中展示最终界的紧致性。
- **缺少消融实验**：没有系统研究$\delta_1$、$\delta_2$、滤波系数$f$等关键参数对收敛速度和最终界的影响。

（完）
