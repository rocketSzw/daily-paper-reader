Title: Trusted Polytopic Action Sets for Fast Planning in Underactuated Systems

URL Source: https://arxiv.org/pdf/2608.24019v1

Published Time: Wed, 26 Aug 2026 00:30:09 GMT

Number of Pages: 6

Markdown Content:
# Trusted Polytopic Action Sets for Fast Planning in Underactuated Systems 

Akshay Jaitly and Siavash Farzan 

Abstract — Underactuated systems pose a challenge for convex motion planning because their dynamically feasible motions lie on a manifold of trajectories in function space. Building on our earlier formulation of polytopic action sets (PAS), this paper presents a method for rapidly generating, online, trusted convex sets of short-horizon actions for underactuated and potentially nonlinear systems. Around a nominal trajectory, we construct local finite-dimensional action coordinates in which each parameter vector encodes a complete nearby motion through an affine trajectory map, rendering collision-avoidance and control bounds linear. To remain consistent with the nonlinear dynamics, we introduce a dynamics-violation metric and extract a trusted convex inner approximation using an IRIS-inspired inflation procedure directly in action space. The resulting PAS are reusable convex families of actions that can be queried and composed with linear programs, and a PAS-guided tree expansion treats nodes as composed reachable families rather than single trajectories, coupling local nonlinear fidelity with convex reuse for longer-horizon planning. The planner solves cluttered planar scenes in tens of milliseconds (14–78 ×

faster than a kinodynamic RRT baseline) and reduces terminal error on a nonlinear underactuated benchmark by 26–86% over sampling and NLP baselines. 

I. I NTRODUCTION 

Convex representations have become a powerful tool in motion planning because they allow large classes of safety and trajectory constraints to be enforced with scalable optimization methods. In robotics, this is commonly realized by covering free or configuration space with convex regions and planning over those regions with Iterative Regional Inflation by Semidefinite programming (IRIS) and Graphs of Convex Sets (GCS) style methods [1], [2]. Representative examples include IRIS-style region inflation, fast sampling-based variants, and graph-based planning over families of con-vex sets [3], [4]. These methods have substantially expanded the range of problems that can be solved with linear, quadratic, and conic optimization. Differentiable time-varying corridors offer a complementary route [5], [6], but none of these approaches address underactuated or nonlinear dynamics. For underactuated systems, the dominant source of nonconvexity is often the dynamics: feasible motions do not fill an open subset of trajectory space, but instead lie on a thin dynamics-consistent manifold. Even a known collision-free corridor then contains only a small subset of dynamically realizable trajectories, which complicates any direct extension of convex free-space methods to kinodynamic planning.                  

> Akshay Jaitly is with Onyx Robotics, Boston, MA 02210, USA (e-mail: akshay@onyx-robotics.com). Siavash Farzan is with the Department of Electrical Engineering, California Polytechnic State University, San Luis Obispo, CA 93407, USA (e-mail: sfarzan@calpoly.edu).
> Fig. 1. Geometric view of PAS construction. Top: a local LTV approximation around the nominal parameterizes nearby trajectories as affine functions of the action parameter γ. Bottom: in these action coordinates, a trusted PAS (red polytope) is extracted as a convex subset of the feasible-trajectory manifold. Here Mis the dynamics-consistent trajectory manifold; x∗(·)
> the nominal trajectory and x(·)a neighbor on M;δx, δu (·)the state/input deviations; and Tx∗Mthe tangent space, locally identified with the action-coordinate space γ∈Rnγ.

Sampling-based kinodynamic planners enforce dynamics feasibility by tree expansion with forward simulation or steering [7]. Related methods, including funnel libraries, reachable-set methods, and trajectory optimization, certify local tubes or compute locally feasible nominal motions for nonlinear systems [8], [9], [10], [11]. Complementary control-oriented approaches, including nonlinear and tube-based model-predictive control [12], [13] and control-barrier-function safe steering [14], also enforce dynamic feasibility online, but do so by solving an optimization problem at each step rather than by reusing precomputed motion families. Our objective is different. We seek reusable convex families of short-horizon trajectory parameters that can be queried and composed online with linear programs. In our earlier work [15], we introduced polytopic action sets (PAS) as convex subsets of a finite-dimensional motion-parameter space that encode entire families of short-horizon trajectories. Once available, PAS can be composed across time and queried via linear programs for long-horizon planning. For underactuated systems, however, feasible trajectories lie on a manifold in function space, and building convex subsets of that manifold directly is generally intractable. This paper addresses that gap. The key idea is to move the convexification step from state space into a local action-coordinate space : a finite-dimensional vector space whose points γ parameterize complete short-horizon trajectories around a nominal motion through the affine map of Eq. (1). Around a nominal feasible motion, we build a finite-arXiv:2608.24019v1 [eess.SY] 25 Aug 2026 dimensional affine parameterization of nearby trajectories via local linearization, within which collision and control constraints become linear inequalities. We then restrict this set to parameters for which the nonlinear dynamics are well approximated, using a trust region defined through a dynamics-violation metric. Since the trust region is nonconvex and available only through oracle queries, we compute a convex inner approximation by adapting IRIS-ZO [3] to the action-parameter space, which is amenable to batched GPU evaluation for online generation of trusted PAS. Rather than convexifying the full nonlinear manifold globally, we construct local convex families of actions anchored to nominal motions and kept within a prescribed tolerance of the true dynamics. Each PAS is compact and reusable. It encodes an entire convex family of short-horizon trajectories, so the same PAS can be queried repeatedly (projected onto different targets, composed with other PAS, or post-processed by a trajectory-level quadratic program (QP)) without rebuilding any local model, using only linear programs. We integrate these trusted PAS into a sampling-based tree expansion procedure. In contrast to classical kinodynamic Rapidly-exploring Random Tree (RRT), tree nodes represent composed convex families of multi-segment trajectories obtained by enforcing continuity across a sequence of PAS, giving the planner access to reachable sets rather than reachable points. Fig. 1 summarizes the geometric viewpoint underlying our construction. The main contributions of this paper are: i. A linearly constrained convex action parameterization that encodes sampled collision-avoidance and actuator-bound constraints as halfspace inequalities in a finite-dimensional trajectory-parameter space. ii. Online construction of trusted polytopic action sets 

by intersecting the linear action set with a nonlinear dynamics-consistency trust region defined by a trajectory-defect metric. iii. Adaptation of IRIS-ZO to action-parameter space using batched oracle evaluations to compute a convex inner approximation of the trusted action set. iv. A PAS-guided tree expansion scheme that grows com-posed reachable families until the goal is reached. II. P OLYTOPIC ACTION SETS 

A Polytopic Action Set (PAS) is a convex polytope A in a finite-dimensional parameter space Rnγ , where each point 

γ ∈ A encodes a short-horizon trajectory via an affine map: 

x(t; γ) = x∗(t) + R(t)γ, u(t; γ) = u∗(t) + U (t)γ, (1) for nominal trajectory (x∗(t), u ∗(t)) and linear maps R(t),

U (t) derived from the local dynamics (constructed in Section III). The nominal action γ = 0 always encodes the nominal trajectory. 

A. Reachable Sets 

A key quantity for planning is the reachable set at time t:the set of states attainable by some γ ∈ A ,

X (t) := {x∗(t) + R(t)γ | γ ∈ A} . (2)    

> (a) (b) Fig. 2. (a) Reachable sets of a single PAS for the 2D double integrator, drawn at sampled time indices; the dashed curve is the nominal trajectory
> x∗(t)about which the PAS is constructed, and sampled collision constraints enter as halfspaces separating the reachable sets from the obstacle. (b) Composing PAS extends the reachable set toward the goal, the mechanism underlying our PAS-guided tree expansion (Sec. IV).

Because A is a polytope and R(t) is linear, X (t) is itself a polytope. Fixing the initial state simply adds a linear constraint on γ. Querying membership, checking goal con-tainment, or finding the closest reachable state to a target all reduce to linear programs (LPs) over γ. Fig. 2a illustrates the forward reachable sets of a PAS for a double-integrator navigating obstacles, color-coded by time. 

B. Composability 

A central benefit of PAS is that they compose cleanly across segments while preserving convexity. Given two sequential PAS Aj with trajectory maps xj (t; γj ), j ∈ { 1, 2}, the set of jointly feasible parameter pairs satisfying inter-segment continuity, 

C := {(γ1, γ 2) | γj ∈ A j , x 1,N (γ1) = x2,0(γ2)} , (3) is a convex polyhedron, since all constraints are linear in 

(γ1, γ 2). The multi-segment composition and its convexity-preservation argument are developed in our earlier work [15]. This extends inductively as composing ℓ trusted PAS yields a single convex polyhedron whose feasible points are continuous multi-segment trajectories, over which post-hoc trajectory optimization (e.g., minimizing control effort) is a single convex program. III. PAS G ENERATION 

We construct a trusted PAS around a nominal trajectory by building local action coordinates, imposing linear constraints, and refining with a nonlinear trust-region test (Fig. 1). 

A. Local Trajectory Parameterization 

Dynamically feasible motions of a nonlinear underactuated system lie on a dynamics-consistent manifold in function space. We construct local action coordinates around a nominal feasible trajectory: a finite-dimensional parameterization of nearby motions in which constraints become linear. Consider the nonlinear underactuated system 

˙x(t) = f (x(t), u (t)) , x (t) ∈ Rnx , u (t) ∈ Rnu , n u<n x.

(4) Let (x∗(t), u ∗(t)) be a nominal feasible trajectory over t ∈

[0 , T ], where T is the per-PAS planning horizon, discretized into N steps of size ∆t = T /N . Linearizing about this nominal gives a Linear Time-Varying (LTV) system with Jacobians 

A(t) := ∂f ∂x (x∗(t), u ∗(t)) 

, B(t) := ∂f ∂u (x∗(t), u ∗(t)) 

,

governing the evolution of deviations δx (t) := x(t) − x∗(t)

and δu (t) := u(t) − u∗(t):

˙δx (t) = A(t) δx (t) + B(t) δu (t). (5) Integrating (5) yields 

δx (t) = Φ( t, 0) δx 0 +

Z t

> 0

Φ( t, s ) B(s) δu (s) ds, (6) where the state transition matrix Φ( t, s ) satisfies  

> ∂∂t

Φ( t, s ) = A(t) Φ( t, s ), Φ( s, s ) = I. We parameterize the input deviation as δu (t) = L(t) η, where L(t) ∈ Rnu×nη

encodes a chosen function class (e.g., a piecewise-linear (PWL) profile with M knots). Here L(t) is a fixed user-supplied basis matrix and η ∈ Rnη a static coefficient vector. For the PWL basis used here, η collects the M knot input values that L(t) interpolates; knots are within-horizon control degrees of freedom, distinct from the composed segments 

(one PAS each) of Section IV. Substituting gives 

δx (t) = Φ( t, 0) δx 0 + K(t) η, (7) where K(t) := R t 

> 0

Φ( t, s ) B(s) L(s) ds is the input-to-state map. Collecting the free quantities into 

γ := 

δx 0

η



∈ Rnγ , nγ = nx + nη , (8) and defining R(t) := Φ( t, 0) K(t), U (t) := 0 L(t),every perturbed trajectory is affine in γ as in (1). Setting 

γ = 0 recovers the nominal. 

Remark 1: When A, B are constant (the Linear Time-Invariant (LTI) case), the construction reduces to the standard controllability Gramian, so LTI is a special case. In practice 

Φ( t, 0) and K(t) are computed numerically by integrating the LTV dynamics on the discretization grid. A key advantage of this parameterization is that endpoint constraints are linear in γ. An initial condition x(0) = ¯ x0

requires R(0) γ = ¯ x0 − x∗(0) , and a terminal constraint 

x(T ) = ¯ xT becomes R(T ) γ = ¯ xT − x∗(T ). Both are linear feasibility conditions on γ.

B. Linear Collision and Control Constraints 

The local coordinates let us enforce sampled geometric and actuator constraints directly in the parameter space γ.Let Kobs ⊆ { 0, . . . , N } be the collision-checking indices and, for each k, let Pk be a set of sampled forbidden points. For p ∈ Pk, the γ that relate to trajectories that reach p

must be excluded. A separating half-space follows from any margin 0 < ϵ p,k < ∥p − x∗

> k

∥22:

 R⊤ 

> k

(p − x∗

> k

)⊤γ ≤ ∥ p − x∗

> k

∥22 − ϵp,k . (9) The nominal action γ = 0 satisfies (9) by construction, while any γ placing the state at p violates it. Stacking over all k

and p gives 

Acol γ ≤ bcol . (10)       

> Fig. 3. Dynamics-defect level sets ζ(γ)in action space. The trusted PAS (orange) lies inside the sublevel set ζ(γ)≤ζmax .

Let umin , u max ∈ Rnu denote componentwise actuator bounds. When the control parameterization u(t) = L(t)γu defines a convex combination of known control points, we can enforce bounds on those control points to ensure feasibility of u(t):

Auγ ≤ bu. (11) Combining (10) and (11) yields 

Alin := {γ ∈ Rnγ | Acol γ ≤ bcol , A uγ ≤ bu} . (12) By construction γ = 0 ∈ A lin , so the nominal motion is retained, and Alin is a convex family of locally admissible actions under the linearized model. Fig. 2a shows the reachable sets of such an action set for a double integrator: collision constraints project to hyperplanes separating the reachable tube from configurations in collision. This suffices for LTI or LTV systems. Its limitation is that 

Alin does not guarantee consistency with the full nonlinear dynamics away from the nominal, which we address next. 

C. Dynamics-Consistency Trust Region 

We measure departure from the nonlinear dynamics by a one-step defect on the discretization grid. At each index 

k we compare the next state xk+1 (γ) of the parameterized trajectory to a one-step integration of the true dynamics from (xk(γ), u k(γ)) , and sum the squared residuals. This vanishes on the nominal and grows as the parameterization diverges from the true dynamics. Define the dynamics-violation functional 

ζ(γ):=  

> N−1

X

> k=0

xk+1 (γ)−xk(γ)−∆t f  xk(γ), u k(γ) 22 (13) By construction ζ(0) = 0 , since the nominal trajectory is dynamically feasible. As ∥γ∥ grows, the linear approximation becomes less accurate and ζ(γ) increases, as illustrated in Fig. 3. The trust region in parameter space is the sublevel set 

Rtrust (ζmax ) := {γ ∈ Rnγ | ζ(γ) ≤ ζmax } , (14) where ζmax > 0 is a user-chosen tolerance. Fig. 3 illustrates this sublevel set together with the trusted PAS it contains. Smaller values yield more conservative action sets that stay closer to the nominal dynamics manifold, larger values admit broader exploration at the cost of reduced local model fidelity. The set Rtrust (ζmax ) is generally nonconvex. Our target is therefore the nonconvex intersection 

Ωtrust := Alin ∩ R trust (ζmax ). (15) 

D. Zeroth-Order Action-Space Inner Approximation 

We compute a convex inner approximation of Ωtrust by adapting IRIS-ZO [3] to the action-parameter space γ rather than configuration space. Define the feasibility oracle 

g(γ) = 

(

1, if ζ(γ) ≤ ζmax ,

0, otherwise, (16) so that g(γ) = 1 if and only if the trajectory encoded by 

γ lies within the prescribed dynamics tolerance. Evaluating 

g(γ) requires only forward evaluation of the parameterized trajectory and residual, no gradients or nonlinear solves are needed, which makes the construction compatible with large batched evaluations. We compute a convex inner approximation of Ωtrust by adapting the cutting stage of IRIS-ZO to the action-parameter space γ. Starting from A(0) := Alin , which contains γ = 0 

by construction, we iteratively refine the polytope, with the goal of separating the interior of the polytope from the space where the linearized model is inaccurate. At each iteration, we sample a batch of candidate parameters from the current polytope and evaluate g(γ) in parallel. For each infeasible sample, we bisect along the ray from 0 to γ to localize the boundary of Rtrust . After Miter rounds, the resulting polytope is the final trusted polytopic action set :

Atrust := A(Miter ), Atrust ⊆ A lin . (17) By construction, Atrust is anchored at the dynamically feasible nominal γ = 0 , and each accumulated halfspace removes a sampled infeasible direction, so the action set is concentrated around the nominal trajectory. Because the refinement is zeroth-order and sample-based, Atrust is an 

approximate convex inner approximation of Ωtrust rather than a certified one. The nonconvex boundary of Rtrust is resolved up to the sampling resolution, and the approximation tightens as the number of cutting rounds Miter and action-space samples per round increase. The residual approximation gap, and the conservatism with which dynamically feasible motions are excluded, depend on the local geometry of Rtrust 

around the nominal. They are more pronounced for strongly nonlinear systems or poorly conditioned LTV models, and negligible in the LTI case, where the model is exact and no refinement is needed. We revisit this in Section V. IV. PAS-G UIDED TREE EXPANSION 

We use trusted PAS as primitives in a sampling-based tree expansion. Each tree node represents a composed convex family of multi-segment motions, giving the planner access to reachable sets rather than point states, as shown in Fig. 2b. 

A. Node Representation by Composed Action Sets 

Let T = ( V, E ) be the search tree rooted at xinit , each edge one trusted PAS. A node v represents a sequence 

{A 1, . . . , Aℓ(v)} of action sets, each representing a segment 

j,

Aj = {γj ∈ Rnγ | Aj γj ≤ bj }, (18) with local affine trajectory maps xj,k (γj ) = x∗ 

> j,k

+ Rj,k γj ,

uj,k (γj ) = u∗ 

> j,k

+ Uj,k γj .Stacking segment parameters into Γv := [ γ⊤ 

> 1

· · · γ⊤

> ℓ(v)

]⊤,the composed feasible set is 

Cv := 

n

Γv Aj γj ≤ bj , j = 1 , . . . , ℓ (v), (19) 

x1,0(γ1) = xinit ,xj,N (γj ) = xj+1 ,0(γj+1 ), j = 1 , . . . , ℓ (v) − 1

o

.

All constraints are linear in Γv , so Cv is a convex polyhedron whenever nonempty. The terminal reachable set of node v is 

XTv := xℓ(v),N (γℓ(v)) Γv ∈ C v , (20) a convex set of terminal states reachable by a continuous sequence of trusted local actions. The superscript in XTv

denotes terminal , not the horizon length T .

B. Expansion Procedure 

The planner grows the tree until some node’s reachable set contains xgoal . Each expansion proceeds as follows. 

1) Sample and select a node: Draw a random target 

xrand (optionally biased toward xgoal ). Select a node v⋆

v⋆ = arg min  

> v∈V

∥W (ˆ xv − xrand )∥2 , (21) where W is a diagonal weight matrix masking irrelevant state components, and ˆxv is a characteristic state representing node 

v. This reduces node selection to a nearest-neighbor lookup. 

2) Project into the reachable set: Solve one LP to find the closest point (in a weighted ℓ∞-norm) in XTv⋆ to xrand :

min   

> Γv⋆, r

r s.t. Γv⋆ ∈ C v⋆ , W (xTv⋆ (Γ v⋆ ) − xrand ) ∞ ≤ r. 

(22) The optimizer yields an xseed we can reach from xinit .

3) Steer and build a new PAS: From xseed , a short-horizon steering routine produces a nominal trajectory toward 

xrand . Applying Section III yields a new trusted PAS Anew .A child node is formed by appending Anew and enforcing continuity at xseed :

Cv+ := 

n

(Γ v⋆ , γ new ) Γv⋆ ∈ C v⋆ , A new γnew ≤ bnew ,xTv⋆ (Γ v⋆ ) = xnew ,0(γnew )

o

. (23) This set is nonempty by construction since Γ⋆v⋆ and γnew = 0 

both reach xseed . Fig. 2b shows the resulting reachable set, newly formed to contain xrand .

4) Goal test and caching: Test whether xgoal ∈ XTv+ by solving a linear feasibility program. If feasible, recover the complete multi-segment plan and terminate. If not, the closest reachable point to xgoal within XTv+ becomes the cached ˆxv+

for future node selection. Return to step 1. V. R ESULTS AND DISCUSSION 

A. Simulation Setup 

We evaluate PAS-RRT on two systems: (i) a 2D double integrator ( nx = 4 , nu = 2 , LTI) navigating obstacle fields, and (ii) a cartpole swing-up ( nx = 4 , nu = 1 ), for which PAS are constructed from local LTV approximations of the underactuated nonlinear dynamics. The double integrator uses a 10 ×10 m workspace with circular obstacles of ra-dius r = 0 .6 m, ∆t = 0 .1 s, and u ∈ [−5, 5] 2 N. The cartpole uses ∆t = 0 .015 s, u ∈ [−10 , 10] N, a cart-position range 

x ∈ [−3, 3] m, and W = diag(0 , 1, 1, 1) , so the swing-up goal does not constrain the final cart position. Control inputs are parameterized with a piecewise-linear (PWL) basis with M

knots. For cartpole, the trust-region test uses ζmax = 0 .05 . For the LTI system, no trust-region refinement is needed because the local model is exact. The nominal trajectory seeding each PAS is produced by a sampling-based steering routine: random PWL control sequences over the M knot times are rolled out under the true dynamics and ranked by W -masked terminal distance to the target. The LTI system draws 512 Gaussian knot samples ( σ=3 N, clipped to the actuator bounds) and the cartpole 1024 (σ=5 N). The IRIS-ZO refinement uses 256 

action-space samples and 12 bisection steps per round, with 

Miter = 1 (cartpole) and 0 (LTI). Sampling and rollouts run batched on an NVIDIA RTX 5060 GPU, planning LPs use HiGHS and the smoothing QP uses OSQP on the CPU. The line-search Nonlinear Programming (NLP) baseline runs a finite-difference Gauss–Newton search with 8 restarts, treating 

ζ ≤ ζmax as a soft penalty. 

B. LTI Obstacle Avoidance 

We sweep PWL knots M ∈ { 5, 10 , 15 , 20 }, horizon steps 

N ∈ { 10 , 20 , 30 }, and obstacle count nobs ∈ { 4, . . . , 25 }

(occupancy ≈ 4.5–28% ), with 50 randomized trials per configuration (3,600 total). PAS-RRT succeeds in all trials (500-expansion budget, goal tolerance 0.5 m). Representative solutions at intermediate densities appear in Fig. 4, with reach-able tubes widening at intermediate times and contracting near constraints. The upper block of Table I reports planning cost versus occupancy at fixed discretization ( M = 10 , N = 20 ): runtime grows smoothly from 9.6 ms at ≈ 4.5% to 127 ms at ≈ 28% , with 2.5–4.9 composed segments (the diamonds in Figs. 4–5 mark segment boundaries , where consecutive PAS are joined by the continuity constraints of (19)). Extending the sweep into the high-occupancy regime (lower block of Table I), success holds at 100% up to ≈ 32% 

occupancy, then degrades to 92% near 41% and 82% at 63% .These failures are workspace-connectivity limited: the free passages approach the planner’s collision margin, so some instances admit no margin-respecting corridor, resembling a denser variant of Fig. 4. Against a kinodynamic RRT baseline (OMPL) on the same scenes, under matched control bounds and collision margins, PAS-RRT is 14 –78 × faster (e.g., ≈ 67 ms versus ≈ 2.0 s at 20 

obstacles) at comparable final path cost, by replacing per-node forward simulation with convex reuse of composed action sets. To exercise local-minimum escape, we add two scenes with non-trivial topology (Fig. 5): a narrow passage with a single gap, and a bug-trap in which a U-shaped obstacle wall encloses the goal. PAS-RRT reaches the goal in all 

50 trials of each. The bug-trap takes roughly an order of magnitude more expansions than the narrow-passage scene (105 vs. 10 expansions) as the tree backs out of the cul-de-sac.   

> Fig. 4. Representative PAS-RRT solutions at 12 obstacles (left) and 16 obstacles (right). Magenta: final trajectory; colored tubes: forward reachable sets; dashed: nominal steering rollouts.
> Fig. 5. Scenes with non-trivial topology: a narrow passage (left) and a bug-trap enclosing the goal (right). PAS-RRT solves both; trajectories are minimum-effort QP solutions.

A minimum-control-effort QP solved over the composed set yields the trajectories in Fig. 5. 

C. Cartpole Swing-Up with Local LTV PAS 

The cartpole swing-up ( θ = π→0) tests PAS-RRT on nonlinear underactuated dynamics. We vary the PWL param-eterization ( M knots), horizon length ( T ), and the number of cutting rounds Miter , and report medians over 20 trials per configuration. A single steering rollout reaches the goal in no tested configuration: even at T = 3 .0 s, M = 20 , the best of 1,024 

samples leaves a W -masked terminal error well above tolerance. PAS instead recovers a goal-reaching action through the goal-projection LP over a convex family of trajectories. As shown in Fig. 6, for M = 40 the PAS solution reduces this error by 26 –86% over direct sampling for T ∈ [1 .5, 3.0] s, whereas the line-search NLP baseline does not improve on sampling: it applies ζ ≤ ζmax as a soft penalty, so its iterates can drift outside the trusted region, and it returns a single point rather than a queryable family.                                                            

> TABLE I LTI OBSTACLE AVOIDANCE :PLANNING COST VS .WORKSPACE OCCUPANCY (M= 10 ,N= 20 , 50 TRIALS PER ROW ). LOWER BLOCK :HIGH -OCCUPANCY REGIME .
> nobs Occ. [%] Success [%] Expansion Iters Plan [ms] Seg. 44.5 100 5.8 9.6 2.5 89.0 100 10.5 18.3 3.0 12 13.6 100 13.2 32.5 3.3 16 18.1 100 20.0 40.8 3.8 20 22.6 100 38.6 66.5 4.4 25 28.3 100 59.8 127.0 4.9 36 40.7 92 81 305 6.0 48 54.3 90 112 547 5.0 56 63.3 82 105 552 5.0

Fig. 6. Single-PAS cartpole goal reachability ( M = 40 , ζmax = 0 .05 ). Left: 

W -masked terminal error vs. horizon; right: relative improvement over direct sampling. PAS outperforms sampling and the NLP baseline. TABLE II PAS-RRT CARTPOLE SWING -UP RESULTS (∆t=0 .015 S, 20 TRIALS ,

> MEDIAN

). R ESID .: AVG . PER -STEP DYNAMICS DEFECT ALONG SOLUTION .

M T Miter Expansion Iters Depth Total Build LP Resid. [s] [ms] [ms] [ms] 

Shorter horizon 

20 0.75 1 41.5 5.0 1297 8.1 21.2 0.016 30 1.50 1 5.5 2.0 184 16.3 18.8 0.024 

Longer horizon 

10 3.00 1 1.0 1.0 26 19.0 3.7 0.001 20 3.00 1 1.5 1.0 44 24.9 6.9 0.006 20 3.00 5 2.0 1.5 154 88.6 10.5 0.003 20 4.00 1 1.0 1.0 42 28.5 6.5 0.006 

Table II summarizes planning performance. Shorter hori-zons require more expansions and larger tree depth, while longer horizons ( T ≥ 3.0 s) solve in 1–2 expansions at depth 

1–2, with runtime dominated by PAS construction. Raising the number of cutting rounds Miter mainly adds halfspaces and LP cost. The per-step dynamics defect stays small at longer horizons ( ≤ 0.006 for T ≥ 3.0 s), the largest values arising in the strongly nonlinear swing-through regime. Sweeping ζmax from 0.005 to 1.0 on a representative configuration, planner success stays at 100% and planning time near 30 ms, while the per-step residual of the returned plan grows monotonically from ≈ 2×10 −3 to ≈ 1×10 −2.The tolerance trades fidelity against exploration smoothly, without a sharp threshold. Under control bounds and a cart-position range matched to PAS-RRT, the kinodynamic RRT baseline finds no swing-up within a 30 s budget. With the cart-position limits relaxed it succeeds, but ≈ 15 –20 × slower than PAS-RRT, which swings up in ≈ 40 –70 ms under the original constraints. The composed PAS provides a corridor of dynamically consistent trajectories connecting start to goal, with the backward reachable set contracting toward the upright equilibrium. An open-source implementation of the proposed method is available at: 

github.com/akshay5312/paamp_underactuated 

VI. C ONCLUSIONS 

This paper presented a method for rapidly generating trusted polytopic action sets for control-constrained systems. The key findings are: (i) a single action-space cutting round (Miter = 1 ) is usually sufficient, as additional rounds add LP overhead without reducing tree expansions; (ii) the per-PAS horizon T is the dominant planning parameter, with longer horizons reducing tree depth to as few as one or two expan-sions; and (iii) the convex-family structure of PAS recovers goal-reaching actions through the goal-projection LP (22) that no single steering rollout, and the NLP baseline, attains. The trusted action set is an approximate convex inner approximation: as the IRIS-ZO refinement is zeroth-order and sample-based, its quality improves with the cutting budget, and the residual gap and the conservatism with which feasible motions are excluded depend on system nonlinearity and on the conditioning of the local model at the nominal. The tolerance ζmax is well-conditioned over the swept range. The method remains inherently local: performance degrades when nominal rollouts are poor and, as the high-occupancy and dead-end studies show, in workspaces whose feasible passages are narrower than the planner’s collision margin. Sampled col-lision constraints are likewise conservative with respect to the discretization. Future work will focus on adaptive trust-region selection, less conservative continuous-time safety certificates, and tighter coupling between PAS generation and steering. REFERENCES [1] R. Deits and R. Tedrake, Computing Large Convex Regions of Obstacle-Free Space Through Semidefinite Programming . Cham: Springer International Publishing, 2015, pp. 109–124. [2] T. Marcucci, M. Petersen, D. von Wrangel, and R. Tedrake, “Motion planning around obstacles with convex optimization,” Science Robotics ,vol. 8, no. 84, p. 7843, 2023. [3] P. Werner, T. Cohn, R. H. Jiang, T. Seyde, M. Simchowitz, R. Tedrake, and D. Rus, “Faster algorithms for growing collision-free convex polytopes in robot configuration space,” 2024, arXiv:2410.12649. [4] T. Marcucci, J. Umenberger, P. Parrilo, and R. Tedrake, “Shortest paths in graphs of convex sets,” SIAM Journal on Optimization , vol. 34, no. 1, pp. 507–532, 2024. [5] A. Jaitly, J. Arrizabalaga, and G. Li, “Trajectory planning using safe ellipsoidal corridors as projections of orthogonal trust regions,” 2025, arXiv:2509.19734. [6] J. Arrizabalaga, Z. Manchester, and M. Ryll, “Differentiable collision-free parametric corridors,” in 2024 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , 2024, pp. 1839–1846. [7] S. M. LaValle and J. J. KuffnerJr., “Randomized kinodynamic planning,” 

The International Journal of Robotics Research , vol. 20, no. 5, pp. 378–400, 2001. [8] A. Majumdar and R. Tedrake, “Funnel libraries for real-time robust feedback motion planning,” The International Journal of Robotics Research , vol. 36, no. 8, pp. 947–982, 2017. [9] M. Althoff, “Reachability analysis of nonlinear systems using con-servative polynomialization and non-convex sets,” in Proceedings of the 16th International Conference on Hybrid Systems: Computation and Control , ser. HSCC ’13. New York, NY, USA: Association for Computing Machinery, 2013, p. 173–182. [10] D. M. Murray and S. J. Yakowitz, “Differential dynamic programming and newton’s method for discrete optimal control problems,” Journal of Optimization Theory and Applications , vol. 43, no. 3, pp. 395–414, 1984. [11] Y. Tassa, N. Mansard, and E. Todorov, “Control-limited differential dynamic programming,” in 2014 IEEE International Conference on Robotics and Automation (ICRA) , 2014, pp. 1168–1175. [12] D. Mayne, J. Rawlings, C. Rao, and P. Scokaert, “Constrained model predictive control: Stability and optimality,” Automatica , vol. 36, no. 6, pp. 789–814, 2000. [13] D. Mayne, M. Seron, and S. Rakovi´ c, “Robust model predictive control of constrained linear systems with bounded disturbances,” Automatica ,vol. 41, no. 2, pp. 219–224, 2005. [14] A. D. Ames, S. Coogan, M. Egerstedt, G. Notomista, K. Sreenath, and P. Tabuada, “Control barrier functions: Theory and applications,” in 

2019 18th European Control Conference (ECC) , 2019, pp. 3420–3431. [15] A. Jaitly and S. Farzan, “PAAMP: Polytopic action-set and motion planning for long horizon dynamic motion planning via mixed integer linear programming,” in 2024 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , 2024, pp. 7617–7624.