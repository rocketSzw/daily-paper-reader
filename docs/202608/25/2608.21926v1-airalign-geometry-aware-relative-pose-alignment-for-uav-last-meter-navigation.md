---
title: "AirAlign: Geometry-Aware Relative Pose Alignment for UAV Last-Meter Navigation"
title_zh: AirAlign：面向无人机最后阶段导航的几何感知相对位姿对齐
authors: "Jinyi Zhou, Shuo Feng, Yufei Wu, Piji Li"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.21926v1"
tags: ["query:world-uav"]
score: 9.0
evidence: 面向无人机最后一米导航的几何感知深度学习，直接匹配无人机自主导航需求
tldr: 无人机在低空环境的最后阶段导航中，面向目标信息获取或操控等精细作业，需要更高精度的相对位姿对齐，然而无人机视角与目标外观的剧烈变化使得该任务极具挑战。AirAlign提出一种仅依赖RGB图像对的相对位姿对齐框架，以预训练视觉几何重建模型为骨干提取几何感知特征；为更好利用有限训练数据，采用场景不相交的多折训练集划分进行交叉验证与模型选择，推理时对多个模型输出取平均集成。在ACMMM 2026 PairUAV挑战赛上取得有效且鲁棒的结果，全面的消融实验验证了各组件的贡献，为无人机末段导航提供可靠方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.21926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1612, \"height\": 963, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.21926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.21926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 625, \"label\": \"Table\"}]"
motivation: 无人机最后阶段导航任务对位姿对齐精度要求日益提高，但剧烈的视角与外观变化使得相对位姿估计非常困难，现有方法鲁棒性不足。
method: AirAlign以预训练视觉几何重建模型为骨干提取几何感知特征，采用场景不重叠的多折训练集进行交叉验证与模型选择，推理时对多个模型输出取平均生成集成结果。
result: 在PairUAV挑战赛上验证了方法的有效性与鲁棒性，全面的消融实验进一步确认了每个设计组件对最终性能的贡献。
conclusion: AirAlign为无人机末段导航提供了一种可靠且泛化性好的仅用彩色图像的相对位姿对齐框架，能够应对实际场景中的视角与外观变化。
---

## 摘要
无人机在现代低空环境中的导航需要在最终接近阶段进行更精确的位姿对齐，以获取目标信息或执行操作，这使得“最后一米”导航变得越来越重要。然而，严重的视点和外观变化使这项任务具有挑战性。为解决这一问题，我们提出了AirAlign，一种用于无人机RGB图像对相对位姿对齐的框架。AirAlign使用预训练的视觉几何重建模型作为主干，从源-目标图像对中提取几何感知特征。此外，为了更好地利用有限的训练数据，我们将训练集划分为多个场景不相交的折叠，用于未见场景的交叉验证和模型选择。在推理过程中，所选模型的预测结果取平均值，形成整体框架的集成输出。在ACMMM 2026多媒体无人机研讨会上的PairUAV挑战赛中的实验证明了我们方法的有效性和鲁棒性，同时全面的消融研究验证了每个组件的贡献。

## Abstract
Unmanned aerial vehicle (UAV) navigation in modern low-altitude environments requires more accurate pose alignment in the final approach stage for target information acquisition or manipulation, making "last-meter" navigation increasingly important. However, severe viewpoint and appearance variations make this task challenging. To tackle this problem, we propose AirAlign, a framework for RGB-only image-pair relative pose alignment for UAVs. AirAlign uses a pretrained visual geometry reconstruction model as the backbone to extract geometry-aware features from source-target image pairs. In addition, to better utilize the limited training data, we split the training set into multiple scene-disjoint folds for unseen cross-validation and model selection. During inference, the predictions of the selected models are averaged to form the ensemble output of the overall framework. Experiments on the PairUAV challenge at the ACMMM 2026 Workshop on UAVs in Multimedia demonstrate the effectiveness and robustness of our method, while comprehensive ablation studies validate the contribution of each component.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：无人机在基础设施巡检、搜索救援、物流配送等低空应用中，不仅需要到达目标附近，还需要在“最后一米”阶段进行高精度的相对位姿对齐（相对平移与朝向），以支持安全着陆、精确投放或近距离观测。
- **核心挑战**：
  - GNSS在近距离下精度不足，难以支持细粒度位姿估计；
  - 源图像与目标图像存在显著的视角和外观变化，传统基于外观或全局语义的匹配难以恢复空间关系；
  - 任务要求仅依赖RGB图像，无额外深度传感器或辅助模态，显式建模3D几何困难。
- **问题定义**：给定源-目标RGB图像对，预测二者之间的相对平移（translation）和相对航向角（heading）。
- **论文意义**：提出AirAlign框架，将视觉几何重建模型用于无人机末段导航的位姿对齐任务，在PairUAV挑战赛上验证了有效性。

## 2. 方法论

- **总体思想**：利用预训练视觉几何重建模型（π³）提取几何感知特征，代替传统的关键点匹配或纯外观特征，使模型具备更强的空间几何理解能力。
- **关键技术细节**：
  - **骨干网络**：采用π³的预训练组件（DINOv2编码器 + 交替的视角自注意力与全局自注意力 + 相机位姿解码器 + 点图解码器），分别输出相机位姿特征和3D点图特征。
  - **特征表示**：将源/目标图像的相机位姿特征拼接为 `f_cam`，点图特征拼接为 `f_pts`。
  - **预测头**：
    - 航向预测头：仅使用 `f_cam`，输出二维单位方向向量（cos, sin），通过归一化避免直接回归角度的训练困难。
    - 平移预测头：同时使用 `f_cam` 和 `f_pts`，经MLP后接sigmoid输出归一化平移值。
  - **损失函数**：由四部分组合：
    - 航向的Smooth L1损失；
    - 相对角度误差损失（考虑360°周期）；
    - 平移的Smooth L1损失；
    - 相对距离误差损失（反归一化后计算）。
  - **训练策略**：
    - 将训练集按场景划分成多个互不重叠的fold，每个fold轮流作为验证集，剩余fold训练模型，选择验证集上最终得分最低的checkpoint；
    - 训练时冻结π³骨干，仅训练预测头，并将骨干输出特征缓存以提升效率；
    - 推理时对所有fold训练出的模型预测取平均（集成），航向由平均后的方向向量通过atan2恢复角度，平移为平均预测值再反归一化。

## 3. 实验设计

- **数据集**：PairUAV挑战赛数据集（ACMMM 2026 Workshop on UAVs in Multimedia），使用官方提供的训练集和测试集。
- **Benchmark**：PairUAV挑战赛的官方评价指标（距离相对误差与角度相对误差的平均值），图像分辨率448×448。
- **对比方法**：
  - 与官方baseline（0.633957）对比，AirAlign取得0.002790分，排名第6；
  - 与挑战赛其他队伍（如light、liang、ze_rong等）对比，性能位列前茅。
- **消融实验**：
  - 不同fold数量（3、5、7）；
  - 是否使用相对角度损失、相对距离损失；
  - 航向头是否加入点图特征、平移头是否去掉相机位姿特征或点图特征。

## 4. 资源与算力

- **论文未明确说明**使用了具体型号和数量的GPU、训练时长等算力资源。
- 从实现细节可推断：骨干冻结且特征缓存，训练过程主要在预测头上进行；batch size为2048，每轮训练550个epoch，采用AdamW优化器，输入为448×448图像。
- 整体计算开销相对较低，但具体资源需求无法从论文中获知。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验1组（挑战赛测试集结果对比）；
  - 消融实验共7组（fold数量3组、辅助损失2组、输入特征3组）。
- **充分性评估**：
  - 实验覆盖了框架的核心设计选择，验证了每个关键组件的作用；
  - 但仅在单一挑战赛数据集上进行评估，未在额外数据集或跨数据集场景下验证泛化性；
  - 与榜单前几名相比，AirAlign在距离误差上偏高（0.003175），说明方法仍有提升空间；
  - 消融实验客观、公平，采用相同训练和评估设置。

## 6. 主要结论与发现

- AirAlign在PairUAV挑战赛上取得0.002790的最终得分，较官方baseline（0.633957）有数量级提升，排名第6，验证了基于视觉几何重建的几何感知特征对无人机末段位姿对齐任务的有效性。
- 5折场景不相交的交叉验证与模型集成为最优配置，优于3折和7折。
- 航向预测应仅依赖相机位姿特征，加入点图特征反而降低性能；平移预测则同时需要相机位姿和点图特征。
- 相对角度损失和相对距离损失作为辅助监督，能显著提升对应误差项的精度。

## 7. 优点

- **创新性**：将预训练视觉几何重建模型引入无人机“最后一米”导航的相对位姿对齐任务，利用几何感知特征而非纯外观特征，思路新颖。
- **简洁有效**：骨干冻结、特征缓存、轻量MLP预测头，训练和推理开销小，适合机载部署场景。
- **训练策略稳健**：场景互不重叠的交叉验证充分利用有限数据，并采用多模型集成提高鲁棒性。
- **设计合理**：根据任务特性为航向和平移设计不同输入特征组合，并通过消融验证了设计决策的合理性。
- **评估规范**：严格遵循挑战赛官方指标，与baseline和榜单队伍对比，结果可信。

## 8. 不足与局限

- **资源信息缺失**：未报告GPU型号、数量、训练时间等，难以判断可复现性和实际部署成本。
- **泛化性有限**：仅在PairUAV单一数据集上测试，没有覆盖不同城市、天气、光照等场景变化，泛化能力尚未充分证明。
- **与顶尖方案差距**：在距离相对误差上明显高于部分领先队伍（如第一名0.002277），说明平移估计仍有改进空间。
- **骨干冻结的潜在限制**：冻结π³可能限制了任务自适应能力，微调或部分微调或许能进一步提升性能。
- **集成策略的代价**：多个模型集成增加了推理计算量，未讨论实时性是否满足无人机在线导航需求。
- **消融范围有限**：未探索更复杂的预测头结构、其他几何特征融合方式、不同损失权重等，研究的深度有待加强。

（完）
