---
title: Topological Feasibility Guarantees for Differentiable Predictive Control
title_zh: 可微预测控制的拓扑可行性保证
authors: "Guangyu Wu, Ján Drgoňa"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10332v1"
tags: ["query:uav-control"]
score: 8.0
evidence: 利用可微分系统动力学实现预测控制策略学习，并提供可行性保证，契合可微分模拟与学习控制。
tldr: 可微预测控制（DPC）通过自监督学习近似显式模型预测控制，虽计算高效，但离线训练策略的可行性保证仍是开放问题；现有方法多为概率性保证或依赖在线安全滤波器。本文从拓扑与几何视角分析DPC内嵌动力学所诱导的可达安全集，提出基于控制屏障函数代理损失的新型离线学习策略，从而在无需在线滤波器的前提下，从有限训练样本建立严格确定性可行性保证。闭环仿真表明约束违反率随样本数增加单调降至零，说明该策略可获结构性形式安全证书，而这在黑盒强化学习等方法中不可实现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10332v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 648, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 886, \"height\": 597, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 748, \"height\": 561, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 746, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 760, \"height\": 638, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 762, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 760, \"height\": 638, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 765, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 836, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 759, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 755, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10332v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 833, \"height\": 559, \"label\": \"Figure\"}]"
motivation: 现有DPC可行性保证仅为概率性或需在线安全滤波器，缺乏离线确定性强保证。
method: 利用拓扑分析诱导可达安全集，提出基于CBF代理损失的离线自监督学习策略。
result: 有限样本下导出确定性可行性保证，仿真约束违反随样本量增加单调降至零。
conclusion: 揭示DPC较黑盒方法能获得形式化安全证书，为学习控制提供新视角。
---

## 摘要
可微预测控制（DPC）是一种用于逼近显式模型预测控制（MPC）策略的自监督学习方法，与基于在线优化的MPC相比，具有显著的计算优势。然而，可行性保证作为安全控制的核心要求，目前要么以概率方式提供，要么通过在线安全滤波器实现。离线策略优化缺乏严格的可行性保证仍然是一个开放性问题。本文通过引入对诱导可达安全集的新型拓扑分析，为DPC建立了确定性可行性保证，且无需在线安全滤波器。利用DPC固有的基于模型的特性，即将可微系统动力学直接嵌入计算图，我们从拓扑和几何角度分析了学习控制策略及相应系统状态的属性。受我们理论分析的启发，我们提出了一种新颖的自监督离线策略学习策略，利用带有控制屏障函数（CBFs）的代理损失。关键在于，这些属性不仅显著改善了策略训练，而且使得从有限数量的训练样本中推导出严格的、确定性的可行性保证成为可能。广泛的闭环仿真验证了我们的理论发现，表明随着训练样本量的增加，经验约束违例单调减少至零。最终，这项工作表明DPC策略优化能够产生形式化的安全证书，而这在结构上无法通过传统黑盒方法（如强化学习（RL）或基于监督学习的近似MPC）获得，从而为基于学习的控制中的可行性保证提供了新视角。

## Abstract
Differentiable predictive control (DPC), a self-supervised learning approach for approximating explicit model predictive control (MPC) policies, offers significant computational advantages over online optimization-based MPC. However, feasibility guarantees, a core requirement for safe control, are currently provided either probabilistically or via online safety filters. The lack of rigorous feasibility guarantees for offline policy optimization remains an open problem. This paper establishes deterministic feasibility guarantees for DPC using a novel topological analysis of the induced reachable safe set, without requiring online safety filters. By exploiting the inherent model-based nature of DPC, in which differentiable system dynamics are embedded directly into the computational graph, we analyze the properties of the learned control policies and the corresponding system states from topological and geometric perspectives. Inspired by our theoretical analysis, we propose a novel self-supervised offline policy learning strategy that utilizes a proxy loss with Control Barrier Functions (CBFs). Crucially, these properties not only significantly improve policy training but also enable the derivation of strict, deterministic feasibility guarantees from a finite number of training samples. Extensive closed-loop simulations validate our theoretical findings, demonstrating that the empirical constraint violations monotonically decrease to zero as the training sample size increases. Ultimately, this work illustrates that DPC policy optimization yields formal safety certificates that are structurally unattainable with conventional black-box methods, e.g., reinforcement learning (RL) or supervised learning-based approximate MPC, thereby providing a new perspective on feasibility guarantees in learning-based control.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：模型预测控制（MPC）通过在线求解带约束的最优控制问题获得安全性，但在线计算成本高；显式 MPC 虽将计算转移到离线，但面临临界区域数量随规模指数增长的问题；近似 MPC（如神经网络逼近）推理快，却通常只能提供概率性可行性保证，或依赖在线安全滤波器/二次规划修正。
- **核心问题**：能否在**完全离线**训练学习型控制器（尤其是可微预测控制，DPC）的同时，获得**确定性的可行性保证**，而不借助在线优化或在线安全滤波？
- **整体含义**：论文从**拓扑与几何视角**分析 DPC 中嵌入可微系统动力学所诱导的“可达安全集”，提出基于控制屏障函数（CBF）代理损失的离线自监督学习策略，证明在有限训练样本下可获得严格、确定性的闭环保真可行性保证。该结果凸显了“白盒”DPC 相对于黑盒强化学习/监督式近似 MPC 的结构性优势：能够导出形式化安全证书。

## 2. 方法论

### 2.1 核心思想

- 将 DPC 描述为参数化最优控制问题：显式策略 $\pi_W(x_k,\xi_k)$ 嵌入闭环仿真计算图，通过反向传播时间（BPTT）优化代价函数 $L_{NC}$。
- 引入**离散时间零化 CBF 更新条件**：
  \[
  r_{k+1}(x_k) = h(x_{k+1}) - (1-\eta)h(x_k) > 0
  \]
  保证安全集的前向不变性，即一旦系统进入可行域，控制策略将使其一直保持在可行域内。
- 通过证明可达安全集 $X_r(k)$ 的紧致性、每个训练轨迹在最优状态邻域内的局部可行性（开球），并利用 **Heine–Borel 定理**说明有限个训练样本的可行性开邻域可覆盖整个可达安全集，从而获得有限样本下的确定性可行性保证。

### 2.2 关键技术细节

- **假设条件**：系统动力学/安全函数/神经策略的局部 Lipschitz 连续性；CBF 条件在可行初始化下存在可行控制轨迹。
- **核心结论**：
  - 每个满足严格 CBF 条件的训练样本诱导局部可行性半径：
    \[
    \epsilon_k^{(i)} = \frac{r_{k+1}(x_k^{*(i)})}{L}
    \]
    其中 $L$ 为均匀 Lipschitz 常数。
  - 当训练样本足够稠密，使这些可行性球覆盖每一步的可达安全集时，学习策略在测试初始状态下递归可行，无需在线滤波器。
  - 违反概率随训练样本数 $m$ 增加单调不增，并在有限样本数下收敛到 0。

### 2.3 算法：CBF-proxy DPC 两阶段训练

- **阶段 1（软惩罚 warm-start）**：用标准 DPC 损失 $L_{NC}$（含 ReLU/GeLU 软惩罚）预训练策略网络，使其快速进入名义可行轨迹流形。
- **阶段 2（离线 CBF 代理微调）**：引入带松弛变量 $\delta \ge 0$ 的 CBF 残差，并构造 CBF 代理损失：
  \[
  L_{CBF} = \sum_{i,k} \left( \|x_k^{(i)}(W) - \breve{x}_k^{(i)}\|^2 + \rho \delta^2 - \tau \sum_l \log r_{k+1,l} \right)
  \]
  其中对数障碍项将策略推向安全集严格内部，松弛变量 $\delta=0$ 作为可行性证书。
- **输入约束**通过在策略网络输出层使用饱和激活函数（如归一化 tanh）结构上强制满足。
- 全部训练在离线完成，在线推理仅需一次前向传播，无需安全滤波器或在线优化。

## 3. 实验设计

### 3.1 实验场景与 Benchmark

论文使用三个非线性/欠驱动控制基准场景：

1. **非完整移动机器人导航**（unicycle 模型）：
   - 状态 $x=[x,y,\theta]$，控制 $u=[v,\omega]$；
   - $10\times10$ 栅格地图，10% 随机静态障碍物；
   - 训练样本数 $N_s = 100,200,400,600,800,1000$；
   - 神经网络为 3 层隐藏层 MLP（512/256/128，GeLU），训练 800 轮 + 200 轮 CBF 微调；
   - 测试：1000 个未见环境中的碰撞率/违反率。

2. **开环不稳定线性系统镇定**：
   - 随机生成 $A\in\mathbb{R}^{2\times2}$（最大特征值实部 >0.2）与 $B\in\mathbb{R}^{2\times1}$；
   - 输入约束 $u\in[-20,20]$，状态约束 $x\in[-16,16]^2$；
   - 训练样本数：50/75/100/200/300 个初始条件；
   - 采用课程学习（预测时域 10→200）+ 两阶段训练；
   - 测试：1000 个随机初始条件，仿真 1000 步（50s）；
   - 对比基线：**vanilla DPC**（无 CBF 代理损失）。

3. **12 维线性化四旋翼镇定**：
   - 状态含位置、欧拉角、线速度、角速度，输入为四个旋翼推力；
   - 姿态约束 $\phi,\theta\in[-\pi/6,\pi/6]$，执行器饱和；
   - 训练样本数极少：6 到 16 个初始状态；
   - 测试：2000 个随机初始条件；
   - 对比基线：**标准 DPC（软惩罚损失）**。

### 3.2 评价指标

- 逐点违反比例（pointwise violation ratio）
- 轨迹违反比例（trajectory violation ratio）
- 收敛到目标/原点的测试轨迹比例（如 96.4% 与 100%）
- 训练样本数量对违反率的影响曲线（单调下降趋势）

## 4. 资源与算力

- 论文**未明确报告**所使用的 GPU 型号、数量、训练总时长或具体计算资源。
- 仅能从算法描述推断：使用 Adam/AdamW 优化器，批次大小 64，学习率 $10^{-3}$ 到 $5\times10^{-4}$，训练轮数最多约 1000 轮（移动机器人场景）。
- 因此无法从文中定量评估训练成本；这在一定程度上削弱了可复现性/实际部署成本的参考价值。

## 5. 实验数量与充分性

- **实验数量**：
  - 3 个系统场景（移动机器人、不稳定线性系统、四旋翼）；
  - 每个场景均有“训练样本数增加→违反率/失败率下降”的系统性扫描；
  - 移动机器人场景有 6 个训练规模档位；不稳定线性系统有 5 个档位；四旋翼有 3 个档位（6/12/16）；
  - 测试规模较大（1000 或 2000 个随机测试样本）；
  - 包含与 vanilla DPC 的对比以及 CBF 代理损失的消融（图 10）。
- **充分性评估**：
  - **优点**：场景覆盖了“非线性运动规划”“开环不稳定线性系统”“高维欠驱动系统”三类代表性问题，具有一定广度；测试样本数充足，能展示违反率随样本数变化的趋势。
  - **局限**：未报告多次随机种子的均值/方差/置信区间，单次实验可能存在偏差；缺乏与其他安全学习控制方法（如在线安全滤波器、预测安全滤波器、概率保证方法）的系统对比；缺少推理时间/计算开销的比较；故障案例（如移动机器人障碍物碰撞的具体形态）未深入分析。

## 6. 主要结论与发现

- 提出了**拓扑可行性分析框架**，证明 DPC 的可行闭环状态序列集为紧集，且可由有限个训练样本诱导的可行性开邻域覆盖。
- 构建了 **CBF-proxy DPC 两阶段训练算法**，通过离线软惩罚 warm-start + 对数障碍 CBF 代理微调，使训练轨迹满足严格 CBF 条件，$\delta=0$ 作为可行性证书。
- 从理论上证明了：**当训练样本覆盖可达安全集时，学习策略可确定性地递归可行，无需在线安全滤波器**；违反概率随训练样本数增加单调下降，并在有限样本下收敛到 0（定理 3.11）。
- 仿真结果与理论一致：三种场景下，约束违反比例均随训练样本数单调下降并最终趋于零；CBF 代理损失显著优于 vanilla DPC，尤其在数据稀少时（如四旋翼 16 个训练样本即可稳定全部 2000 个测试样本）。

## 7. 优点

- **理论创新性强**：首次从拓扑/几何角度为 DPC 建立严格确定性可行性保证，弥补了该领域“仅概率保证”或“需在线滤波”的空白。
- **方法简洁且可操作**：CBF 代理损失完全融入可微计算图，离线训练，在线推理无额外优化负担，保持了 DPC 的快速推理优势。
- **实验与理论呼应**：违反率随样本量单调下降的实证结果，直接验证定理 3.11；四旋翼实验展示出在极小样本（6–16）下的鲁棒性。
- **强调白盒结构性优势**：通过访问系统 Jacobian 与 Lipschitz 结构，实现可量化的安全性，而黑盒 RL 难以获得类似形式化证书。
- **算法实用性强**：两阶段训练有效缓解了严格障碍函数在安全集外数值脆弱的问题，结合软惩罚与对数障碍，兼顾探索与细化。

## 8. 不足与局限

- **存在性而非构造性**：作者承认可行性覆盖与样本需求目前是存在性证明（Heine–Borel），尚未给出可直接用于数据采样的构造性覆盖算法；如何确定最小样本数仍依赖理论保守半径。
- **假设较强**：依赖系统动力学、安全函数、神经策略的 Lipschitz 常数，以及 CBF 条件可得性——在实际复杂系统中这些常数可能难以准确估计。
- **采样策略未深入**：训练样本假设“足够稠密/分布良好”，但未提出主动采样算法；随机采样可能无法高效覆盖可达安全集，导致高维系统中样本需求过大。
- **实验充分性有限**：缺少多次随机种子统计、与其他前沿安全学习方法的全面比较、计算资源/训练时间报告；四旋翼实验使用线性化模型，无法说明在真实强非线性气动环境下的表现。
- **安全余量保守性**：使用均匀 Lipschitz 常数得到的可行性半径可能过于保守，导致理论上所需样本数远大于实际；反之，若 Lipschitz 估计不准，也会威胁保证的有效性。
- **输入约束通过网络结构强制满足**，但状态约束的严格性依赖于 CBF 微调的收敛，若 $\delta$ 未严格收敛到 0 或训练提前截止，确定性保证仍需额外验证。

（完）
