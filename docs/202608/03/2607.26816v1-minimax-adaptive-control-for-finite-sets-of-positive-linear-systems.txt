Title: Minimax adaptive control for finite sets of positive linear systems

URL Source: https://arxiv.org/pdf/2607.26816v1

Published Time: Thu, 30 Jul 2026 00:58:23 GMT

Number of Pages: 34

Markdown Content:
# Minimax Adaptive Control for Finite Sets of Positive Linear Systems 

Fethi Bencherki ⋆ Anders Rantzer ⋆⋆Department of Automatic Control, Lund University, Lund, Sweden 

{fethi.bencherki, anders.rantzer}@control.lth.se 

Abstract 

We present a minimax adaptive control framework for discrete-time positive linear systems with parametric uncertainty and adversarial disturbances. The uncertainty in the system dynamics is assumed to lie in a finite set of possible plants. We formulate the problem as a dynamic game between the controller, which minimizes the cost, and an adversary, which selects both the disturbances and the plant dynamics to maximize the cost. An equivalent reformulation of the original game transforms the problem into a standard minimax two-player zero-sum dynamic game. This enables the problem to be addressed via minimax dynamic programming. We provide an explicit solution to the Bellman inequality, yielding stabilizing, positivity preserving policies without requiring an initially stabilizing controller. The resulting controller enjoys robustness guarantees in the form of bounded ℓ1-gain from disturbances to errors. Once the uncertain parameters have been sufficiently estimated, the controller behaves like a standard H∞-type optimal controller for positive linear systems. The theoretical findings are supported by numerical experiments illustrating the resulting adaptive controller in action. 

Keywords. minimax adaptive control, positive systems, game-theoretic control, dynamic pro-gramming, Bellman equation, data-driven control 

# 1 Introduction 

Technological advances have led to engineering systems and networks of increasing scale, inter-connections, and dynamical complexity. In many applications, the underlying physics are only partially understood, and the resulting mathematical models are therefore only approximate. As a consequence, uncertainties introduced at the modeling stage must be accounted for in controller design. This has motivated a substantial literature on robust and adaptive control, leading to the seminal work on self-tuning regulators [7] and followed by important contributions on stochastic adaptive control and convergence analysis [19, 29, 21]. Comprehensive treatments of the topic can be found in the classic textbooks [6, 20] and in more recent surveys that connect adaptive control with modern learning-based control and finite-sample analysis [4, 3, 27, 39]. A central theme in adaptive control is the interplay between regulation and learning. A controller must maintain good closed loop performance using the information currently available, while also gathering informative data that improves its internal model of the plant over time. This tradeoff has been studied extensively since the early self-tuning regulator literature, and it remains central to modern adaptive control, learning-based synthesis, and finite-sample analysis [7, 19, 29, 21, 3]. More recent work has sharpened this perspective by clarifying how finite horizon guarantees, regret bounds, and robustness considerations shape adaptive control design [39, 3]. 1arXiv:2607.26816v1 [math.OC] 29 Jul 2026 The minimax adaptive control framework, first introduced in [17], addresses parametric uncertain-ties and disturbances in a worst-case sense. Cast in a game-theoretic framework [8], the controller is the minimizing player and the disturbances act as the maximizing player. This perspective has recently been revisited with an emphasis on discrete-time minimax optimization [32]. One major advantage is that minimax adaptive controllers do not require an initially stabilizing policy to provide provable performance guarantees. This makes the framework especially attractive in settings with large modeling errors or poorly modeled dynamics. Related work has used this formulation to derive explicit ℓ2-gain guarantees and robust simultaneous stabilization results for finite sets of LTI plants [11, 15, 35]. Positive systems are a structured class of dynamical models in which nonnegative initial conditions and positivity-preserving inputs guarantee nonnegative states and outputs. This makes them a natural modeling framework for queueing, routing, and flow networks [38, 5]; traffic network models [16]; epidemic and compartmental dynamics [16]; and more general monotone processes on nonnegative state spaces [33, 36]. Their structure can also be exploited to obtain scalable analysis and synthesis methods, since stability and controller design is conducive to scalability [33, 5, 36]. In particular, positive systems admit linear Lyapunov functions, linear programming based controller synthesis, and distributed control architectures that are computationally efficient than general quadratic or semidefinite formulations [12, 18, 34, 1, 26]. Recent work has also developed data-driven and adaptive methods for positive systems, including direct stabilization from persistently exciting data and linear programming/copositive Lyapunov synthesis [37, 28], data informativity conditions for distributed positive stabilization [25], data-driven identification and positive subspace system identification from input-output data [41, 42], dynamic output feedback synthesis for externally positive systems [2], and robust adaptive online control based on data-driven Bellman/Q-learning equations [10, 9]. Motivated by these developments, we propose a framework for minimax adaptive control of discrete-time positive linear systems. The proposed controllers learn the system parameters online from incoming data while preserving positivity and robustness against adversarial disturbances. We do not impose statistical assumptions on the uncertain parameters or disturbances; instead, the model is deterministic, and the unknown quantities are selected by an adversary that seeks to maximize the cost function. By solving a minimax Bellman inequality, we derive explicit adaptive control laws that stabilize the system without requiring an initially stabilizing policy. We establish these results for the case in which the unknown system parameters belong to a finite set, and we show that the closed loop remains robust to unmodeled dynamics and mild modeling errors while remaining computationally tractable and scalable for large networked systems. See Fig. 1 for an illustration of the problem considered in the paper. 

1.1 Contributions 

The main contributions of this work are: 

(C1). Minimax adaptive dual control formulation for positive linear systems. We formulate a minimax dynamic game for positive systems between an adversary, which selects the unknown dynamics and disturbance sequence in an adversarial manner, and a controller, which selects the control inputs. This formulation compels the controller to learn the true dynamics in order to stabilize the system, thereby giving rise to an exploration–exploitation tradeoff and a natural dual-control setting. 

(C2). Extension of [23] to positivity preserving disturbances. We show that, in the known system parameters case, our problem extends the setting of [23] by allowing sign-indefinite disturbances, rather than only nonnegative disturbances as previously done, provided that they preserve state positivity. Despite this enlarged disturbance class, the resulting model 2Positive Linear system 

> Minimax Adaptive Controller
> Adversary

Errors 

State Measurement Control action 

Disturbances 

System Parameters 

Figure 1: Feedback interconnection of a discrete-time positive linear system with uncertain parameters, external disturbances, unmodelled dynamics, and an adaptive controller. The goal is to design the controller so as to minimize the worst case induced ℓ1-gain from disturbances to errors, while preserving positivity and accounting for parametric uncertainty. based problem still admits a solution via linear programming, as in [23]. This extension in the admissible disturbance class is essential for the success of the adaptive minimax approach proposed in this paper. 

(C3). Reformulation of the original problem. We reformulate the original proposed problem in terms of the next state and model-specific nonnegative data histories, thereby obtaining a standard minimax zero-sum dynamic game between an adversary, which selects the positive next states, and a controller, which responds through its choice of inputs. We establish the equivalence between the original problem and its reformulated counterpart, and show that solving one yields a solution to the other through minimax dynamic programming. This allows us to shift our focus to the reformulated problem. 

(C4). Explicit solutions to the Bellman inequality. We derive closed form adaptive policies by providing an explicit solution to the associated Bellman inequality for systems with unknown input direction. For such systems, we also quantify the minimum ℓ1-gain achievable in the scalar case under the certainty equivalence policy, which provides an upper bound on the optimal ℓ1-gain. We then generalize these results to a finite set of linear time-invariant plants. 

(C5). Motivating applications. We illustrate how the resulting adaptive policies can be deployed in practice by considering two representative positive linear system applications: a two reservoir network with uncertain transfer direction and a four mode multiclass job fluid queueing network inspired from [13]. 

1.2 Organisation 

After introducing notation in Section 1.3, Section 2 presents the problem formulation. Section 3 derives a data-history reformulation of the original problem, which enables its conversion into a standard positive minimax dynamic game formulation, addressed via minimax dynamic programming in Section 4. Section 5 presents our main results in the form of explicit solutions to the Bellman inequality, first for the special case of input sign uncertainty and then for the general case of finite set of positive LTI plants, accompanied by numerical examples. Finally, Section 6 concludes the paper and discusses directions for future research. Preliminary lemmas, their proofs, and the proofs of the main results are deferred to Appendices A and C. Appendix B addresses the model based counterpart of the problem considered here. 31.3 Notation and Preliminaries 

We let Rn 

> +

denote the nonnegative orthant in dimension n. The vector 1 denotes the all-ones vector, with dimension understood from context. For any vector x, |x| denotes the elementwise absolute value. For a vector x, max x and min x denote its largest and smallest entries, respectively. For a set M, |M| denotes its cardinality. If z(A,B ) 

> t

∈ R+ is defined for every pair (A, B ) ∈ M , we write Zt := {z(A,B ) 

> t

}(A,B )∈M for the indexed collection of nonnegative real numbers z(A,B ) 

> t

, one for each (A, B ) ∈ M . The ℓ1-norm of a sequence w = {wt}∞ 

> t=0

is given by 

∥w∥1 := ∑∞ 

> t=0

1⊤|wt| = ∑∞ 

> t=0

∥wt∥1. Inequalities between vectors and matrices are understood elementwise. 

# 2 Problem formulation 

In this paper, the focus is the positive linear system class presented next. 

2.1 Positive system class 

We consider the discrete-time positive linear systems of the form 

xt+1 = Ax t + Bu t + wt, x0 ∈ Rn

> +

where the dynamics are parameterized by A ∈ Rn×n 

> +

and B ∈ Rn×m. We constrain the input 

ut to lie in a positivity preserving admissible input set U(xt) that depends on the current state 

xt ∈ Rn

> +

, where 

U(x) ⊆ {u ∈ Rm : Ax + Bu ≥ 0, for all x ∈ Rn 

> +
> }

.

Thus, at each time t ∈ N, the control action satisfies ut ∈ U (xt). Concrete choices of U(xt) are discussed and specified in section 2.3. The disturbance w is restricted to a set W(x, u ) that preserves state positivity pointwise in time. Specifically, we define 

W(x, u ) := {w ∈ Rn : w ≥ − (Ax + Bu ), for all x ∈ Rn

> +

, for all u ∈ U (x)} .

These sets are such that, for every t ∈ N, the next state xt+1 remains nonnegative for all nonnegative xt, ut ∈ U (xt) and wt ∈ W (xt, u t). In particular, wt can be negatively large in magnitude as long as it does not render the state negative. We assume that the true, unknown positive system belongs to a compact model class M consisting of admissible system matrices 

(A, B ). To this end, we define the set M next. 

Definition 1 (Model set M): Let M ⊆ Rn×n 

> +

× Rn×m be a compact set of admissible system parameters (A, B ) ∈ M .The set M captures the parametric uncertainty in the dynamics. 

Assumption 1: The true unknown system parameters (A⋆, B ⋆) lie in M, i.e., (A⋆, B ⋆) ∈ M .

In the next subsection, the minimax dual control problem considered in this paper is introduced. 

2.2 A minimax dual control problem for positive system 

We consider the following linear minimax dynamic game problem for the class of positive systems introduced above. 4Problem 1: Find a causal policy μ solving 

J∗(x0) = inf  

> μ

sup  

> (A,B )∈M
> w≥− (Ax +Bu )
> T∈N
> T∑
> t=0
> (

s⊤xt + r⊤ut − γ⊤|wt|)

s.t. xt+1 = Ax t + Bu t + wt, x0 ∈ Rn

> +

,ut = μt

> (

x0, . . . , x t; u0, . . . , u t−1

> )

, ∀t ∈ N,ut ∈ U (xt), ∀t ∈ N.

Note that in Problem 1, we restrict the policy map μ to causal control policies which are required to generate positivity preserving inputs ut ∈ U (xt). Additionally, we require the worst case stage cost to be nonnegative, as stated in the following assumption. 

Assumption 2 (Positivity of the stage cost): The stage cost weights satisfy s ∈ Rn

> +

, r ∈ Rm,and γ ∈ Rn

> +

. Moreover, for every (A, B ) ∈ M , t ∈ N, xt ∈ Rn

> +

, and ut ∈ U (xt), we assume 

max 

> wt∈W (xt,u t)
> (

s⊤xt + r⊤ut − γ⊤|wt|

> )

≥ 0.

Remark 1: Since ut ∈ U (xt) guarantees Ax t + Bu t ≥ 0, we have 0 ∈ W (xt, u t). Therefore, Assumption 2 simplifies to s⊤xt + r⊤ut ≥ 0.

Unlike standard minimax dynamic games for positive linear systems (cf. [8, 24]), in Problem 1 the maximizing adversary selects not only the disturbance sequence w, but also the unknown dynamics (A, B ) ∈ M . In the absence of uncertainty in (A, B ), the problem reduces to an 

H∞-style optimal control problem for positive linear systems. When (A, B ) is unknown, however, the controller must learn the true dynamics in order to stabilize the system. This naturally leads to solutions in the form of adaptive dual controllers that balance learning and control, and can outperform linear state-feedback designs under model uncertainty [32]. Next, we motivate the study of problems in the form of Problem 1 by relating them to established problems in the optimal control of positive linear systems. This connection also suggests suitable choices for the admissible input set U(x).

2.3 Connections to existing positive system formulations and classes 

When the model is known, i.e., when the uncertainty set reduces to M = {(A⋆, B ⋆)}, Problem 1 covers, as special cases, several classes of positive linear systems considered in the model-based optimal control over positive systems literature; see [23, 24, 30, 31]. These special cases correspond to the following choices of the admissible input set U(x):

(i) The works [22, 23, 24] consider the admissible input set 

U(x) := { u ∈ Rm : |u| ≤ Ex }, E ∈ Rm×n 

> +

.

Together with the elementwise conditions A ≥ | B|E, s > E ⊤|r|. This ensures positivity of the closed-loop system and nonnegativity of the stage cost. Unlike [23], which permits only nonnegative disturbances wt ≥ 0, we allow disturbances of either sign, provided that they preserve positivity of the next state. This is encoded by the elementwise constraint 

wt ∈ W (xt, u t), for all t ∈ N. Define 

K(E) := 

> {

K ∈ Rm×n : K = Λ E, Λ = diag( λ1, . . . , λ m), λ i ∈ {− 1, +1 }, ∀i ∈ { 1, . . . , m }

> }

.

5K(E) is the set of sign-saturated feedback gains associated with the constraint |u| ≤ Ex .In the model-based setting, the works [23, 24] show that it is sufficient, without loss of optimality, to restrict attention to static state-feedback policies of the form 

ut = Kx t, K ∈ K (E).

Under this construction, it is shown in [23] that the problem reduces to a linear program whenever the game is feasible. 

(ii) In the works [30, 31], the input is partitioned as u = ( u1, . . . , u n), where ui ∈ Rmi and  

> ∑ni=1

mi = m. Let 

E⊤ =

> [

E1 · · · En

> ]

∈ Rn×n 

> +

.

Define 

K(E) := 

> {

K ∈ Rm×n : ∀i ∈ { 1, . . . , n }, 1⊤Ki = E⊤ 

> i

or 1⊤Ki = 0 

> }

,

where Ki denotes the i-th block row of K, corresponding to ui ∈ Rmi . The admissible input set is of the form 

U(x) := 

> {

u ∈ Rm 

> +

: ∃ K ∈ K (E) such that u = Kx 

> }

.

In this setup, it is also assumed that A + BK ≥ 0 for all K ∈ K (E). Finally, to maintain nonnegativity of the stage cost s⊤x + r⊤u, it suffices to assume s, r ≥ 0, since inputs are nonnegative by design. This class of positive systems also leads to a linear program and has been shown to be equivalent to stochastic shortest path problems, making it an appealing candidate application for future work. In Appendix B, we derive the model-based solution of Problem 1 in the nominal case where 

M = {(A⋆, B ⋆)}, and for the choice of U(x) in (i) . This is useful for three reasons: 

(1) From Section 5 onward, we focus on input sets of the form (i) , although analogous results can be derived for (ii) in the same spirit. 

(2) Unlike [23], our model-based formulation allows a broader class of disturbances: rather than requiring disturbances to be nonnegative, we only require them to be positivity preserving. We therefore derive the solution to this more relaxed model-based problem in Appendix B. 

(3) Understanding the nominal model-based solution plays an important role in deriving the adaptive, model-free solution. 

Remark 2: The extension of the admissible disturbance set from nonnegative disturbances, 

w ≥ 0, to positivity preserving disturbances, w ∈ W (x, u ), plays a crucial role in enabling the success of our approach. This will become clear in the next section. 

# 3 Reformulated problem via the nonnegative historical data 

In this section, we take a first step toward solving problem 1 by reformulating it as a standard minimax zero-sum dynamic game through the introduction of so called historical data variables. This reformulation, inspired by [32], enables the use of standard minimax dynamic-programming theory to address the problem. The results in this section and in Section 4 hold for the general class of input sets U(x) introduced in Section 2.1, and are stated in that generality. Recall the original minimax problem with unknown dynamics, formulated as Problem 1. 6Reparameterization via the next state. Introduce the next state sequence v, with vt := xt+1 .

Then, wt = vt − (Ax t + Bu t), and the constraint w ≥ − (Ax + Bu ) is equivalent to v ≥ 0,independently of the adversary’s choice of (A, B ). Substituting wt = vt − (Ax t + Bu t) yields the equivalent problem 

inf  

> μ

sup    

> (A,B )∈M
> v≥0
> T∈N
> T

∑

> t=0

(

s⊤xt + r⊤ut − γ⊤|vt − Ax t − Bu t|

)

s.t. xt+1 = vt, x0 ∈ Rn

> +

,ut = μt

(x0, . . . , x t; u0, . . . , u t−1

), ut ∈ U (xt).

(3.1) Since the feasibility condition v ≥ 0 does not depend on (A, B ), maximizing jointly over 

(A, B ) ∈ M and v ≥ 0 can be carried out in any order. Hence, problem (3.1) can be written equivalently as 

inf  

> μ

sup   

> v≥0

sup  

> (A,B )∈M
> T∈N
> T

∑

> t=0

(

s⊤xt + r⊤ut − γ⊤|vt − Ax t − Bu t|

)

s.t. xt+1 = vt, x0 ∈ Rn

> +

,ut = μt

(x0, . . . , x t; u0, . . . , u t−1

), ut ∈ U (xt).

(3.2) If one instead assumes nonnegative disturbances, w ≥ 0, then the next state constraint becomes 

v ≥ Ax + Bu, which depends on the unknown true dynamics (A⋆, B ⋆). Imposing this constraint for all pairs (A, B ) ∈ M would introduce conservatism in the adversary’s choice, since positivity only needs to hold for the true pair selected by the adversary. This motivates our preference for the alternative formulation w ≥ − (Ax + Bu ), or, equivalently, v ≥ 0, which should hold irrespective of which pair (A, B ) is selected by the nonnegative adversary v.

Model-specific historical data. For each model (A, B ) ∈ M , define the scalar nonnegative cumulative history by 

z(A,B ) 

> t+1

:= 

> t

∑ 

> τ=0

γ⊤∣∣vτ − Ax τ − Bu τ

∣∣. (3.3) Equivalently, this quantity evolves according to the recursion 

z(A,B ) 

> t+1

= z(A,B ) 

> t

+ γ⊤∣∣vt − Ax t − Bu t

∣∣, z(A,B )0 = 0 . (3.4) We then define the historical data collection, indexed by M, as 

Zt := {z(A,B ) 

> t

}(A,B )∈M , (3.5) which collects the history variables associated with all dynamics pairs (A, B ) ∈ M .

Minimax game with historical data. Using the definitions in (3.3) – (3.5) , the reformulated problem (3.2) can be equivalently written as follows. 7Problem 2: Find a causal policy η solving 

inf  

> η

sup  

> v≥0
> T∈N
> T

∑

> t=0

(s⊤xt + r⊤ut

) − min  

> (A,B )∈M

z(A,B ) 

> T+1

s.t. xt+1 = vt, x0 ∈ Rn

> +

,z(A,B ) 

> t+1

= z(A,B ) 

> t

+ γ⊤∣∣vt − Ax t − Bu t

∣∣, z(A,B )0 = 0 , ∀ (A, B ) ∈ M ,Zt := {z(A,B ) 

> t

}(A,B )∈M , Z0 := {0}(A,B )∈M ,ut = η(xt, Z t) ∈ U (xt), ∀ t ∈ N.

Remark 3: Note that constructing the history vector Zt does not require knowledge of the true dynamics (A, B ), since its construction exhausts all possible pairs in the model set M.

Remark 4: In the reformulated problem 2, the unknown true parameters (A, B ) appear only in terminal cost and not in the extended dynamics (xt, Z t). Consequently, the control law η(·)

need not depend on the full state history; it depends only on the current extended state (xt, Z t).The problem is cast as a standard zero-sum minimax dynamic game in which the adversary chooses the positive next state vt ≥ 0 to maximize the cost and the controller chooses the input 

ut ∈ U (xt) to minimize it. 

Remark 5: The reformulation technique used here follows a principle similar to that in [32], with one important difference. In [32], the history variable is defined in terms of a global covariance historical-data matrix that is independent of the model set M. By contrast, in our formulation, the history variables are defined locally for each possible pair (A, B ) ∈ M . This distinction is specific to our ℓ1 setup. We next address problem 2 using minimax dynamic programming. 

# 4 Minimax dynamic programming for positive systems 

Dynamic programming operators. For a value function V (x, Z ), define the operators F and 

Fu by 

FV (x, Z ) := min  

> u∈U (x)

max 

> v≥0

{

s⊤x + r⊤u + V (v, Z +)

}︸ ︷︷ ︸  

> =: FuV(x,Z )

, (4.1) where Z+ denotes the updated nonnegtaive historical data collection 

Z+ := {z(A,B )+ }(A,B )∈M ,

whose entries are updated via 

z(A,B )+ = z(A,B ) + γ⊤∣∣v − Ax − Bu ∣∣, ∀ (A, B ) ∈ M .

Initialization and value iteration. We initialize value iteration with the terminal cost function 

V0(x, Z ) = − min  

> (A,B )∈M

z(A,B ). (4.2) The value iteration is then carried recursively by 

Vk+1 (x, Z ) = FVk(x, Z ) = min  

> u∈U (x)

max 

> v≥0

{

s⊤x + r⊤u + Vk(v, Z +)

}

. (4.3) 8This defines the sequence of value functions {Vk}k∈N.Given all the ingredients introduced above, we are now able to state one of the main results of the paper. This result establishes a crucial equivalence between the original Problem 1 and its reformulation as a standard dynamic game, Problem 2. 

Theorem 1: Given γ ∈ Rn 

> +

and a parameter set M, as in Definition 1, satisfying Assumption 1, define F by (4.1) and the sequence {Vk}k∈N by (4.2) –(4.3) , with z(A,B ) constructed for each 

(A, B ) ∈ M . Suppose that Assumption 2 holds. Then, the following statements hold. (i) Problems 1 and 2 have the same value. This value is finite if and only if the sequence 

{Vk(x, 0) }∞ 

> k=0

is bounded above for every x ∈ Rn

> +

. In that case, the limit V∗ := lim k→∞ Vk

exists and satisfies J∗(x0) = V∗(x0, 0) .

(ii) Let η∗(x, Z ) be the minimizing u in FV∗(x, Z ). Then, η∗ is optimal control law for problem 2, and the control law 

μ∗ 

> t

(x0, . . . , x t, u 0, . . . , u t−1) := η∗

> (

xt, 

> {t−1∑
> τ=0

γ⊤∣∣xτ +1 − Ax τ − Bu τ

> ∣∣}
> (A,B )∈M
> )

.

is optimal for the original minimax problem 1. (iii) If there exists a function V (x, Z ) such that V ≥ V0 and 

Fη V (x, Z ) ≤ V (x, Z ) for all (x, Z ) ∈ Rn 

> +

× R|M| ,

where FηV (x, Z ) := FuV (x, Z )∣∣u=η(x,Z ), then the policy μ defined by 

ut = μt(x0, . . . , x t, u 0, . . . , u t−1) := η

> (

xt, 

> {t−1∑
> τ=0

γ⊤∣∣xτ +1 − Ax τ − Bu τ

> ∣∣}
> (A,B )∈M
> )

.

satisfies the cost upper bound Jμ(x0) ≤ V (x0, 0) .

Proof. See Appendix C.1. 

Remark 6: Theorem 1 has three main implications: 

(1) The original problem 1 and the reformulated problem 2 have the same optimal value, as item (i) above shows. 

(2) Any policy that solves the reformulated problem optimally, or suboptimally, gives a policy for the original problem with the same optimality, or suboptimality, guarantee, as items (ii) -

(iii) above state. 

(3) Any solution of the Bellman inequality associated with the reformulated problem provides an upper bound for the value of the original problem, achieved by the corresponding induced policy, as item (iii) states. Having established the equivalence between the original problem, Problem 1, and the reformulated problem, Problem 2, we now focus on the reformulated problem. To this end, we relax the Bellman equation and instead consider the corresponding Bellman inequality, as stated in Theorem 1 (iii) ,for which a solution will be derived in the sequel. 95 Explicit solutions to the Bellman inequality 

To illustrate how the results of Theorem 1 can be applied, we now focus on the setting described in item (i) of Subsection 2.3. Similar results for the setting in item (ii) can be obtained by following the same approach to the one developed here. Accordingly, we let 

U(x) := { u ∈ Rm : |u| ≤ Ex }, ∀x ∈ Rn

> +

,

where the matrix E is governed by the following assumption. 

Assumption 3: We assume that there exists E ∈ Rm×n 

> +

such that: (i) For all (A, B ) ∈ M , A ≥ | B|E.(ii) The stage-cost parameters satisfy s ≥ E⊤|r|.

The two conditions in Assumption 3 have the following interpretations. 

(i) This condition ensures positivity of the closed-loop system for all admissible dynamics 

(A, B ) ∈ M including the true pair (A⋆, B ⋆). Indeed, for every t ∈ N, xt ∈ Rn

> +

, and ut

satisfying |ut| ≤ Ex t, we have 

Ax t + Bu t ≥ Ax t − | B|| ut| ≥ (A − | B|E)xt ≥ 0

for all (A, B ) ∈ M .

(ii) This condition implies Assumption 2. Whenever Ax t + Bu t ≥ 0, and since γ ∈ Rn

> +

, we have, for every t ∈ N, xt ∈ Rn

> +

, and ut satisfying |ut| ≤ Ex t,

max 

> wt≥− (Ax t+Bu t)
> (

s⊤xt+r⊤ut−γ⊤|wt|

> )

= s⊤xt+r⊤ut ≥ s⊤xt−| r|⊤|ut| ≥ (s−E⊤|r|)⊤xt ≥ 0.

Hence, the worst-case stage cost is nonnegative. We begin by providing a solution to the Bellman inequality for the special case of input sign uncertainty, and then generalize it to the case of a finite set of LTI plants afterwards. 

5.1 Positive systems with unknown input direction 

We consider positive linear systems with unknown input direction. For scalar unstable systems, it can be shown that it is not possible to synthesize a static state-feedback law that stabilizes both input directions simultaneously: stabilizing one direction necessarily destabilizes the other; see [40] for further details. In this setting, the uncertainty enters only through the sign of the input matrix, while the state matrix is common across the models. Specifically, we consider the two model unknown sign class 

M := {(A, B ), (A, −B)} ⊂ Rn×n 

> +

× Rn×m, (5.1) for which the historical data collection reduces to Zt = {z(+)  

> t

, z (−) 

> t

}, where z(+)  

> t

:= z(A,B ) 

> t

and 

z(−) 

> t

:= z(A, −B) 

> t

where the model-specific histories are updated as 

z(σ)+ = z(σ) + γ⊤∣∣ v − Ax − σBu ∣∣, z(σ)0 = 0 , σ ∈ { +, −} .

Recall also the input constraint |u| ≤ Ex . Without a loss of optimality, Such inputs can be expressed as u = Kx , with K belonging to 

K(E) := 

> {

K ∈ Rm×n : K = Λ E, Λ = diag( λ1, . . . , λ m), λ i ∈ [−1, 1] 

> }

.

The next result gives an explicit expression for an adaptive controller satisfying a prescribed 

ℓ1-gain bound γ whenever the pair (A, B ) is stabilizable. 10 Theorem 2: Let K+, K − ∈ K (E), and consider the model class M in (5.1) . Let (s, r, E ) be as in Assumption 3. Suppose there exist p+, p−, and h satisfying 0 ≤ max {p+, p −} ≤ h ≤ γ such that the following inequalities hold (i) pσ ≥ s + K⊤ 

> σ

r + ( A + σBK σ)⊤pσ, for all σ ∈ { +, −} ,

(ii) h ≥ s + K⊤ 

> k

r + ( A + σBK k)⊤pσ, for all k, σ ∈ { +, −} with k̸ = σ, 

(iii) h⊤x ≥ (s⊤ + r⊤Kk + h⊤A)x + ( h − γ)⊤|BK kx|, k ∈ { +, −} , for all x ∈ Rn

> +

.

Then, the Bellman inequality F V (x, Z ) ≤ V (x, Z ) holds for 

V (x, Z ) = max 

> {

p⊤

> +

x − z(+) , p ⊤−x − z(−), h ⊤x − 12

> (

z(+) + z(−))} 

Consequently, the certainty equivalence policy 

ut = Kkt xt, kt = arg min  

> σ∈{ +,−}

z(σ) 

> t

, (5.2) 

satisfies the cost bound Jμ(x0) ≤ h⊤x0. If, in addition, m = 1 (the single input case), then inequality (iii) above simplifies to the state independent inequality (iii’) h⊤ ≥ s⊤ + r⊤Kk + h⊤A + ( h − γ)⊤|B|E, k ∈ { +, −} .

Proof. See Appendix C.2. The next remark provides a recipe for the solving the vector inequalities in Theorem 2. 

Remark 7 (Recipe for solving (i) –(iii) ): The inequalities in Theorem 2 can be solved in the following sequence: 

(i) First, for each σ ∈ { +, −} , one may solve the corresponding equality 

pσ = s + A⊤pσ − E⊤∣∣∣ r + σB ⊤pσ 

> ∣∣∣

,

which gives the optimal cost achieved by the model-based controller for the known sign σ.The corresponding optimal model-based gain is 

K⋆σ = − diag (sgn( r + σB ⊤pσ)) E. 

(ii) Next, with p+ and p− fixed, choose h large enough so that the inequalities hold. Since there are only two mixed cases, namely (σ, k ) = (+ , −) and (σ, k ) = ( −, +) , the smallest feasible choice is the componentwise maximum 

h = max 

> {

s + r⊤K− + p⊤

> +

(A + BK −), s + r⊤K+ + p⊤−(A − BK +)

> }

.

In particular, this yields h ≥ p+ and h ≥ p−, since the right-hand sides are obtained by applying the controller designed for the wrong, and potentially destabilizing, input direction. 

(iii) Finally, with p+, p−, and h fixed, choose γ ≥ h sufficiently large so that the last inequality holds. For fixed p+, p−, and h, these inequalities are linear in γ, so a feasible value of γ

can be readily obtained. In the single input case m = 1 with Kk = ±E, condition (iii) reduces to (iii’) , so the last step becomes a componentwise linear inequality in γ, independent of x.11 Next, we provide an example showcasing the results obtained in Theorem 2. 

Example 1 (Two-reservoir network with uncertain transfer direction): Consider the two-reservoir water network shown in Figure 2. The states x1,t and x2,t represent the nonnegative stored water volumes in each reservoir. The reservoirs are connected through the surrounding network and through a controllable interconnection pipe equipped with a pump. The dynamics are linearized to xt+1 = Ax t ± Bu t + wt, with 

A =

[ 12910 25710 

]

, B =

[ 15

− 25

]

, E =

[

1 1

]

, s =

[11

]

, r = 15 .

Here, the matrix A models passive storage and hydraulic coupling. Each reservoir retains part of its own water volume over one time step, captured by the diagonal elements of A, while the off diagonal entries account for network mediated redistribution between the reservoirs. The input vector B has one positive and one negative entry because a transfer command moves water from one reservoir to the other, meaning one stored volume increases while the other decreases. The uncertainty ±B represents the fact that the effective direction of the interconnection pump is unknown, for instance because of reversed installation or an uncertain sign convention in the supervisory controller. The disturbance wt represents exogenous inflows and withdrawals at the reservoirs, such as rainfall, upstream supply variations, consumer demand, or unmeasured leakage, while preserving nonnegativity of the next state xt+1 consistent with the physical requirement that stored water volumes cannot be negative. We have K+ = E, K − = −E and Assumption 3 holds since 

A =

[ 12910 25710 

]

≥

[ 15152525

]

= |B|E, s =

[11

]

>

[ 1515

]

= E⊤|r|.

Moreover, eig (A) = 3±√73 10 , so the open loop system is unstable. In particular, the true pump direction must be identified in order to stabilize the reservoir volumes. We now apply the recipe of Remark 7 to extract the adaptive policy. Solving (i) gives the smallest positive 

p+ =

[

4 8

]⊤

, p− =

[ 8316 3

]⊤

.

The corresponding model-based optimal gains are then 

K⋆ 

> +

= − sgn( r + B⊤p+)E = E, K⋆ 

> −

= − sgn( r − B⊤p−)E = −E, 

so they coincide with the gains chosen above. Next, the inequalities (ii) become 

h ≥ s + K⊤− r + p⊤

> +

(A + BK −) = 

[ 42 562 5

]⊤

, h ≥ s + K⊤ 

> +

r + p⊤−(A − BK +) = 

[ 94 15 134 15 

]⊤

.

Hence the smallest feasible choice is the componentwise maximum 

h = max 

{[ 42 562 5

]⊤

,

[ 94 15 134 15 

]⊤}

=

[ 42 562 5

]⊤

.

Finally, using the single input condition (iii’) , one obtains 

γ1 + 2 γ2 ≥ 292 5 , γ ≥ h. 

Taking γ1 at its smallest admissible value γ1 = h1 = 42 5 , the smallest corresponding γ2 is γ2 = 25 ,

so one minimal choice is γ =

[ 42 5 25 

]⊤

. Simulations with the control law (5.2) are shown in Figure 3. In this interpretation, the certainty-equivalence controller initially increases control activity in order to learn the true pump direction; once the history variables identify the correct sign, the controller collapses to the model-based law for the true network dynamics. For more details on the implementation, refer to the code 1.

> 1Code available at: https://github.com/Fethi-Bencherki/minimax-adaptive-control-of-positive-systems

12 Reservoir 1 

## x1

> Reservoir 2

## x2±ut 

> passive storage/coupling wt, 1wt, 2

Figure 2: Two-reservoir water network with uncertain transfer direction. The states x1 and x2

denote stored water volumes, ut is the pump command, and wt represents exogenous inflows and withdrawals. The matrix A captures passive storage and hydraulic coupling, while the uncertainty ±B means that a positive control input may transfer water in either direction. Next, we derive an upper bound on the minimum achievable gain γ for the certainty-equivalence (CE) policy in (5.2) over the model class M in the scalar case with r = 0 , that is, with no input cost. We denote this gain by γCE (a).

Corollary 1 (Scalar CE gain and cost upper bounds): Consider the scalar case n = m = 1 with model class M = {(a, b ), (a, −b)}, where a > 0 and b > 0. Let s = 1 and r = 0 , and set E = ab ,

K+ = −E, and K− = E. Hence, Theorem 2 applies to the CE controller 

ut = −kt ab xt, kt = arg min  

> σ∈{ +,−}

z(σ) 

> t

. (5.3) 

Then, the conditions in Theorem 2 (i)-(iii) reduces to the simpler scalar inequalities (i) p ≥ 1, (ii) h ≥ 1 + 2 ap, (iii) h ≥ 1 + 2 ah − γa. 

Moreover, the smallest value of γ for which there exist p, h ∈ [0 , γ ] satisfying (i)–(iii) is 

γCE (a) = max {1 + 2 a, 4a} =

> 

1 + 2 a, 0 < a ≤ 12 ,

4a, a ≥ 12 . (5.4) 

The corresponding minimizing values are p⋆ = 1 and h⋆ = 1 + 2 a, and the certainty equivalence policy proposed here certifies the cost bound Jμ(x0) ≤ (1 + 2 a)x0.

Proof. See Appendix C.3. 

Remark 8: In the absence of input cost, r = 0 , Corollary 1 shows that γCE (a) is an upper bound on the best ℓ1-gain certified by Theorem 2 from the disturbance w to the state x under the scalar CE policy in (5.3). Equivalently, the certified performance bound can be written explicitly as 

> ∞∑
> t=0

xt ≤ (1 + 2 a)x0 + max {1 + 2 a, 4a}

> ∞∑
> t=0

|wt|.

The existence conditions for the stabilizing certainty-equivalence policy proposed in Theorem 2 are only sufficient and therefore may not capture the best ℓ1-gain achievable by the CE policy. One may conjecture that the CE policy achieves the optimal ℓ1-gain bound, analogous to the conjecture posed for the quadratic setting in [40]. The derivation of such optimal ℓ1-gain bound is deferred to future work. We next generalize Theorem 2 to a finite set of positive LTI models (A, B ) ∈ M .13 0 10 20 30 40 

0

2

4

6

t 

> x1
> Adaptive Optimal

0 10 20 30 40 

0

1

2

3

4

t 

> x2
> Adaptive Optimal

0 10 20 30 40 

−5

0

5

t 

> ut
> Adaptive Optimal

0 10 20 30 40 

−2

0

2

t 

> wt
> wt, 1wt, 2

0 10 20 30 40 

0

1,000 

2,000 

t 

> z(σ)
> t
> z(+) z(−)

0 10 20 30 40 

−1

0

+1 

t 

> sign
> Estimated sign True sign

Figure 3: Closed loop simulation for Example 1, comparing the adaptive controller for the unknown input direction with the optimal controller that knows the true sign. The panels show, from top left to bottom right, x1, x2, the control input ut, the positivity-preserving disturbance 

wt, the history variables z(+) and z(−), and the true and estimated signs. The true sign is fixed but unknown to the adaptive controller, while disturbances may be negative as long as they preserve state positivity. The adaptive controller initially uses probing to identify the input direction; once the sign is learned, its behavior coincides with the known dynamics controller. 

5.2 Explicit solution of the Bellman inequality in the general case 

We now turn our focus to finding an explicit expression for an adaptive controller satisfying a prescribed bound on the ℓ1-gain for finite model sets of the form 

M = {(A1, B 1), . . . , (AM , B M )} ⊂ Rn×n 

> +

× Rn×m, (5.5) 14 for which the historical data collection reduces to Zt = {z(1)  

> t

, . . . , z (M ) 

> t

}, where z(i) 

> t

:= z(Ai,B i) 

> t

for 

i = 1 , . . . , M . In this case, problem 2 becomes 

J∗(x0) = inf  

> η

sup  

> v≥0
> T∈N

( T∑

> t=0

(s⊤xt + r⊤ut

) − min   

> i∈{ 1,...,M }

z(i) 

> T+1

)

s.t. xt+1 = vt, x0 ∈ Rn

> +

,z(i) 

> t+1

= z(i) 

> t

+ γ⊤∣∣vt − Aixt − Biut

∣∣, z(i)0 = 0 , i = 1 , . . . , M, Zt := {z(1)  

> t

, . . . , z (M ) 

> t

}, Z0 = {0, . . . , 0},ut = ηt(xt, Z t), |ut| ≤ Ex t.

(5.6) A solution to the Bellman inequality corresponding to Problem 5.6 is characterized in the next theorem. 

Theorem 3: Let K1, . . . , K M ∈ K (E) ⊂ Rm×n, and consider the model set M in (5.5) . Let 

(s, r, E ) be as in Assumption 3. Suppose there exist vectors pij ∈ Rn

> +

, for i, j ∈ { 1, . . . , M }, such that 0 ≤ pij = pji ≤ γ, and such that, for all x ∈ Rn 

> +

and all i, j, k ∈ { 1, . . . , M }, excluding the case i̸ = j = k, the inequality 

p⊤

> jk

x ≥ s⊤x+r⊤Kkx+p⊤ 

> ij

max {Aix+BiKkx, A j x+Bj Kkx}− 12 γ⊤∣∣(Ai −Aj )x+( Bi −Bj )Kkx∣∣

(5.7) 

holds. Define the linear value functions 

V ij (x, Z ) := p⊤ 

> ij

x − 12

(z(i) + z(j)), V (x, Z ) := max  

> i,j

V ij (x, Z ).

Then, V solves the Bellman inequality, i.e. FV (x, Z ) ≤ V (x, Z ) holds. Consequently, the certainty equivalence control law 

ut = Kkt xt, kt = arg min   

> i∈{ 1,...,M }

z(i)

> t

satisfies the performance bound Jμ(x0) ≤ max i,j p⊤ 

> ij

x0.

Proof. See Appendix C.4. 

Remark 9: The model index kt ∈ { 1, . . . , M } selected by the certainty equivalence policy is given by 

kt = arg min  

> i∈{ 1,...,M }
> t−1

∑ 

> τ=0

γ⊤∣∣xτ +1 −Aixτ −Biuτ

∣∣ = arg min  

> i∈{ 1,...,M }
> t−1

∑ 

> τ=0

∥∥diag( γ)(xτ +1 − Aixτ − Biuτ

)∥ ∥1 .

The selected model is the one that minimizes the cumulative γ-weighted ℓ1-norm of the disturbance prediction error up to time t.

Remark 10: The inequalities (5.7) admit an interesting interpretation. 

(i) If i = j = k, then (5.7) reduces to 

p⊤ 

> ii

x ≥ s⊤x + r⊤Kix + p⊤ 

> ii

(Ai + BiKi)x, 

which is the standard linear algebraic inequality for the plant (Ai, B i) with parameter pii .The quantity p⊤ 

> ii

x provides an upper bound on the closed loop infinite horizon cost when model i is known. 15 (ii) If i = j̸ = k, then (5.7) becomes 

p⊤

> ik

x ≥ s⊤x + r⊤Kkx + p⊤ 

> ii

(Ai + BiKk)x, 

showing that the additional cost incurred by applying a controller not matched to the true model is upper bounded by the gap p⊤

> ik

x − p⊤ 

> ii

x. 

(iii) If i̸ = j, then the negative term − 12 γ⊤∣∣(Ai − Aj )x + ( Bi − Bj )Kkx∣∣ captures the benefit of learning: even when a mismatched controller is used, improved model knowledge reduces the cost to go. 

Example 2 (Recipe for solving (5.7) ): One possible way of solving the inequalities in (5.7) for the model set 

M = {(A1, B 1), . . . , (AM , B M )}

can be carried out as follows. 

(i) First, consider the cases in which all indices are the same, i.e., i = j = k. For each model 

(Ai, B i), i ∈ { 1, . . . , M }, we solve the corresponding model-based fixed-point equation 

pii = s + A⊤ 

> i

pii − E⊤∣∣r + B⊤ 

> i

pii 

> ∣∣

, γ ≥ pii . (5.8) This yields the Cost vectors p11 , . . . , p M M . The associated model-based optimal gains are 

K⋆i = − diag (sgn( r + B⊤ 

> i

pii ))E, i = 1 , . . . , M. (5.9) This step fixes the diagonal Cost vectors p11 , . . . , p M M and the corresponding model-based gains K⋆ 

> 1

, . . . , K ⋆M .

(ii) Next, consider the cases in which i = j̸ = k. For each unordered pair {j, k } with 

j, k ∈ { 1, . . . , M } and j̸ = k, define the symmetric mixed cost vectors pjk := pkj . We then choose pjk large enough to dominate the two right hand sides corresponding to the cases 

(i, j, k ) = ( j, j, k ) and (i, j, k ) = ( k, k, j ). A feasible componentwise choice is 

pjk = max 

> {

s + K⊤ 

> k

r + (Aj + Bj Kk

> )⊤

pjj , s + K⊤ 

> j

r + (Ak + BkKj

> )⊤

pkk 

> }

, j̸ = k, (5.10) where the maximum is taken componentwise. These inequalities correspond to applying the controller designed for one model to the other model. In other words, they capture the cost increase that may occur when the wrong controller is applied. This step fixes the symmetric mixed cost vectors pjk = pkj for all j̸ = k, corresponding to the cases i = j̸ = k.

(iii) Finally, consider the cases with i = k̸ = j. With all pij fixed, choose γ ∈ Rn 

> +

sufficiently large so that 0 ≤ pij ≤ γ, i, j ∈ { 1, . . . , M } and the remaining inequalities in (5.7) hold. At this stage, the remaining admissible mixed index cases are precisely those with i = k̸ = j, while the cases i̸ = j = k are excluded by assumption. For each such triple, the corresponding inequality is linear in γ. Since there are only finitely many index combinations, a feasible value of γ can be obtained by choosing it sufficiently large so that all remaining inequalities hold. We summarize these steps in Algorithm 1. 

Remark 11: The recipe provided in Algorithm 1 may not yield the tightest cost vectors pij and 

ℓ1-gain bound γ. It can be refined to produce better solutions by using techniques similar to those in [15], adapted to our positive system, linear cost setting. We illustrate the theoretical results obtained in this section with the following numerical example. 16 Algorithm 1 Recipe for solving (5.7).  

> 1:

Input: Model set M = {(A1, B 1), . . . , (AM , B M )}, input matrix E, cost vectors s, r . 

> 2:

Output: Cost vectors pij , gains K⋆i , and gain bound γ. 

> 3:

Step 1: Solve the diagonal cases i = j = k using (5.8), and compute K⋆i using (5.9).  

> 4:

Step 2: For each j̸ = k, set pjk = pkj according to (5.10).  

> 5:

Step 3: Choose γ sufficiently large so that 0 ≤ pij ≤ γ and the remaining cases i = k̸ = j

in (5.7) hold.  

> 6:

Return: {pij }, {K⋆i }, and γ.

External arrivals 

Admission command ut

Job classes 

Class 1 Class 2 Class 3 

Q1 Q2 Q3

w1 w2 w3

S1 S2 S3departures dep departures 

Figure 4: Three queue multiclass job queueing network. The scalar admission command ut

regulates the external workload entering queues Q1 and Q2. Queues Q1, Q2, and Q3 are processed by servers S1, S2, and S3, respectively. A portion of the workload served from Q1 may be routed to Q2 or directly to the shared downstream queue Q3, while another portion may leave the modeled network. Similarly, a portion of the workload served from Q2 may be routed to Q1 or 

Q3, while another portion may leave the modeled network. The disturbances w1,t , w2,t , and w3,t 

represent signed net workload mismatch in the three queues. Depending on the unknown mode, the same signed command ut may either increase or decrease the amount of work admitted into the network. 

Example 3 (Four-mode multiclass job queueing network): We construct a four mode example of multiclass job fluid queueing network (MFQNET) inspired by the model in [13]. We consider a queueing network with three job classes and three associated queues. Jobs of classes 1 and 2 wait in two upstream queues and are served by servers S1 and S2, respectively, while class 3 corresponds to a common downstream queue served by S3. Accordingly, the state is x = [ x1 x2 x3 ]⊤ ∈ R3+,where x1, x2, and x3 denote the workloads in queues 1, 2, and 3, respectively. Over one sampling period, a portion of the workload in queue 1 may remain there awaiting service, while another portion may be served and subsequently routed to queue 2 or directly to queue 3; similarly, a portion of the workload in queue 2 may remain there awaiting service, while another portion may be served and subsequently routed to queue 1 or directly to queue 3. See Figure 4 for a schematic representation. We model the control action by a scalar input u ∈ R, interpreted as a signed deviation from a nominal admission level for the class-1 and class-2 queues. 

(+) u > 0: the admission command increases the incoming workload admitted into the class-1 and class-2 queues relative to the nominal level; 

(–) u < 0: the admission command decreases the incoming workload admitted into the class-1 17 and class-2 queues relative to the nominal level, so that a larger portion of the external arrivals remains outside the modeled network. We take 

U(xt) = { ut ∈ R : |ut| ≤ Ex t }, E =

> [

1 1 0

> ]

,

so that the magnitude of the control action cannot exceed the total workload currently present in queues Q1 and Q2. We consider four operating modes: 

(i) Mode 1: The dynamics are given by (A1, B 1), and a positive input increases the workload admitted into queues Q1 and Q2.

(ii) Mode 2: The dynamics are given by (A2, B 2). As in Mode 1, a positive input increases the workload admitted into queues Q1 and Q2, but the baseline propagation through the network differs because A2̸ = A1.

(iii) Mode 3: The dynamics are given by (A3, B 3), and the sign of the input effect on the upstream queues is reversed: a positive input decreases the workload admitted into queues 

Q1 and Q2.

(iv) Mode 4: The dynamics are given by (A4, B 4). As in Mode 3, a positive input decreases the workload admitted into queues Q1 and Q2, but the baseline propagation through the network differs because A4̸ = A3.The dynamics are 

xt+1 = Aixt + Biut + wt, i ∈ { 1, 2, 3, 4},

where 

A1 =

> 

0.80 0 .08 00.08 0 .70 00.12 0 .08 0 .84  

> 

, A 2 =

> 

0.68 0 .08 00.08 0 .78 00.12 0 .10 0 .86  

> 

, A 3 =

> 

0.84 0 .08 00.08 0 .84 00.08 0 .08 0 .84  

> 

, A 4 =

> 

0.82 0 .08 00.08 0 .84 00.06 0 .08 0 .84  

> 

,B1 = B2 = [0.08 0 .08 0 ]⊤ , B3 = [−0.08 −0.08 0 ]⊤ , B4 = [−0.08 −0.08 0 ]⊤ .

These matrices have the following interpretation for each mode i:

• the j-th column of Ai describes how the workload initially present in queue j is distributed after one sampling period; 

• the diagonal entry (Ai)jj represents the fraction of that workload that remains in queue j,corresponding to work that is still awaiting service; 

• each off-diagonal entry (Ai)ℓj , with ℓ̸ = j, represents the fraction of the workload initially in queue j that is processed during the sampling period and then routed to queue ℓ.Moreover, the column sums of Ai are at most one. The qunatity 1 − ∑

> ℓ

(Ai)ℓj represents the fraction of workload initially in queue j that is served during the sampling period and exits the modeled network. Thus, Ai captures the net one step effect of retention, service, routing, and departures in mode i.The vector Bi determines the effective direction of the scalar control in mode i. In modes 1 and 2,

Bi ≥ 0, so a positive input ut increases the workload admitted into queues Q1 and Q2. In modes 

3 and 4, this effect is reversed in the upstream queues, so the same positive input decreases the workload admitted into queues Q1 and Q2. In all four modes, the input has no direct effect on queue Q3, since the third component of each Bi is zero. Thus, the controller does not know in advance whether the unknown mode belongs to the release family {1, 2} or the diversion family 

{3, 4}.18 We take s = [ 1 1 1 ] ⊤, r = 15 , and γ = [ 10 8 8 ] ⊤. The disturbance wt ∈ R3 represents the part of the queue evolution not captured by the nominal model Aixt + Biut, such as unmodeled arrivals, departures, rerouting, or other unexpected changes over one sampling interval. In particular: 

• wj,t > 0 means that queue Qj contains more workload than predicted by the nominal dynamics, for example due to extra arrivals, slower service, or additional rerouting into that queue; 

• wj,t < 0 means that queue Qj contains less workload than predicted by the nominal dynamics, for example due to cancellations, abandonment, faster service, or rerouting out of that queue. The only requirement is that the next state remain nonnegative, namely wt ≥ − (Aixt + Biut).

Assumption 3 holds since direct computation gives 

|B1|E = |B2|E = |B3|E = |B4|E =

> 

0.08 0 .08 0 0.08 0 .08 0 0 0 0 

> 

.

Each of these matrices is componentwise dominated by the corresponding Ai. Moreover, E⊤|r| =[ 1515 0] ⊤ and s = [1 1 1] ⊤, so s > E ⊤|r| componentwise. The model-based optimal controllers do not coincide across the four modes. Applying Algorithm 1 to this four mode example yields the diagonal certificates 

p11 =

> [

5.54 3.42 6.25 

> ]⊤

, p22 =

> [

4.14 5.05 7.14 

> ]⊤

,p33 =

> [

7.08 7.08 6.25 

> ]⊤

, p44 =

> [

6.06 7.08 6.25 

> ]⊤

.

and the corresponding optimal known-mode gains 

K⋆ 

> 1

= K⋆ 

> 2

= −E, K⋆ 

> 3

= K⋆ 

> 4

= E. 

since r + B⊤ 

> 1

p11 > 0, r + B⊤ 

> 2

p22 > 0, r + B⊤ 

> 3

p33 < 0, and r + B⊤ 

> 4

p44 < 0. Hence modes 1 and 2

favor one sign of the scalar input, while modes 3 and 4 favor the opposite sign. In particular, applying the controller designed for modes 1 and 2 in modes 3 and 4 yields 

ρ(A3 − B3E) = 1 .0800 , ρ(A4 − B4E) = 1 .0703 ,

so the corresponding closed-loop systems are not asymptotically stable. The symmetric mixed cost vectors are then chosen as 

p12 = p21 =

> [

5.5357 5.0476 7.1429 

> ]⊤

, p13 = p31 =

> [

8.9500 8.9500 6.2500 

> ]⊤

,p14 = p41 =

> [

7.7603 8.7859 6.2500 

> ]⊤

, p23 = p32 =

> [

8.9500 8.9500 7.1429 

> ]⊤

,p24 = p42 =

> [

7.7603 8.7859 7.1429 

> ]⊤

, p34 = p43 =

> [

7.0833 7.0833 6.2500 

> ]⊤

.

The remaining gain bound γ is chosen sufficiently large so that 0 ≤ pij ≤ γ for all i, j ∈ { 1, . . . , 4}

and the remaining inequalities in (5.7) hold; one convenient feasible choice is γ =

> [

15 15 15 

> ]⊤

.

Simulations of the control law (5.2) are shown in Figure 5. As noted earlier, the adaptive policy initially explores in order to learn the true dynamics. At time t = 30 , the true dynamics switch; in response, the controller again explores sufficiently to identify the new mode and then collapses back to the corresponding model-based optimal control law. For more details on the implementation, refer to the code 2.

> 2Code available at: https://github.com/Fethi-Bencherki/minimax-adaptive-control-of-positive-systems

19 0 10 20 30 40 50 60 

0

2

4

t

> x1

Adaptive Optimal Mode switch 

0 10 20 30 40 50 60 

0

2

4

t

> x2

Queue 2 workload Adaptive Optimal Mode switch 

0 10 20 30 40 50 60 

0

2

4

t

> x3

Adaptive Optimal Mode switch 

0 10 20 30 40 50 60 

−5

0

5

t

> u

Admission command Adaptive Optimal Mode switch 

0 10 20 30 40 50 60 

0

500 

1,000 

t

> Z

z(1) z(2) z(3) z(4) 

0 10 20 30 40 50 60 

1

2

3

4

t

> mode

True mode vs estimated mode True mode Estimated mode Mode switch 

Figure 5: Closed loop trajectories under the adaptive certainty equivalence controller and the optimal controller with known mode. The dashed vertical line indicates the mode switching time. The figures show the workloads in queues Q1–Q3, the admission command, the history variables, and the true and estimated modes. 20 5.3 Adversary policy in the learning setting 

Consider a two model uncertainty set M = {(a1, b 1), (a2, b 2)}. For a prespecified input constraint set U(x), let K1 and K2 denote the optimal state feedback gains for the plants (a1, b 1) and 

(a2, b 2), respectively, where K1, K 2 ∈ K (E). Fix indices i, k ∈ { 1, 2}. Suppose the true plant is 

(ai, b i) while the controller applies the state feedback law u = Kkx. As shown in the proof of Theorem 3 (cf. (C.5)), when γ is above a suitable threshold, the adversary selects 

v∗ = max {(a1 + b1Kk)x, (a2 + b2Kk)x} = max  

> ℓ∈{ 1,2}

aℓ + bℓKk.

Relative to the true plant (ai, b i), the induced disturbance is 

w∗(i, k ) = v∗ − (ai + biKk)x =

> (

max 

> ℓ∈{ 1,2}

(aℓ + bℓKk) − (ai + biKk)

> )

x. 

Since each gain Ki is chosen to optimally stabilize its own plant, it minimizes the resulting closed loop factor for that plant over the admissible class K(E). In particular, for all ℓ̸ = i,

ai + biKi ≤ ai + biKℓ, ∀ Ki, K ℓ ∈ K (E).

Let j̸ = i denote the other index in {1, 2}. Two cases arise. 

(a) Matched gain ( k = i̸ = j). In this case, the adversary picks 

w∗(i, i ) = 

> (

max {ai +biKi, a j +bj Ki}− (ai +biKi)

> )

x = ((aj +bj Ki)−(ai +biKi))x ≥ 0,

i.e., the adversary switches to the other model whenever doing so increases the next state. 

(b) Mismatched gain ( k = j̸ = i). Since j = arg max ℓ∈{ 1,2}(aℓ + bℓKj ), we have 

w∗(i, j ) = 

> (

max {ai+biKj , a j +bj Kj }− (ai+biKj )

> )

x = ((ai+biKj )−(ai+biKj ))x = 0 ,

the adversary reinforces the controller’s incorrect gain choice by setting the disturbance to zero, since the resulting closed loop factor is already the larger one. As a result, the adversary may deviate from the model-based case in [23], where the choice w∗ = 0 

is independent of the controller’s gain selection. This aspect is specific to our learning setup: because the controller must infer which model is active, the adversary can exploit mismatches between the true plant and the applied gain to make the observed dynamics appear more consistent with the wrong model. In this sense, it actively complicates identification by steering the next state toward the model that maximizes (aℓ + bℓKk

> )

x.

# 6 Conclusion and future directions 

This paper proposes a minimax adaptive control framework for positive linear systems. The problem is formulated as a minimax dynamic game between the controller and an adversary that selects both the disturbance sequence and the true dynamics. This formulation naturally compels the controller to explore and learn the true dynamics in order to stabilize the system, thereby giving rise to the explore–exploit tradeoff commonly discussed in dual control and reinforcement learning. We showed how past data can be compressed into history variables, on which the stabilizing adaptive control policy depends. These history variables contain the information needed for the controller to balance learning and control. By reformulating the problem in terms of the history 21 variables, we showed that the Bellman inequality can be solved when the adversary is free to choose from a finite set of possible positive LTI plants. While the present work takes a step in this direction, a complete theory of dual adaptive control for positive systems remains an open problem. Several important avenues for future research remain open: 

(1) One natural extension is to allow uncertainty not only in the dynamics but also in the stage cost. In that setting, the unknown model is described by a quadruple (A, B, s, r ) ∈ M × S ,where M denotes the set of admissible dynamical models and S the set of admissible cost parameters. The controller must then learn both the true plant dynamics and the cost online in order to stabilize the system. 

(2) Throughout the manuscript, we relied on model dependent history variables z(A,B ) for each (A, B ) ∈ M . This parameterization becomes prohibitive when one moves from a finite model set M to a continuum of models. In that case, one must instead seek history variables whose definition is independent of (A, B ), in the same spirit as the covariance-matrix parameterization in [32]. A natural direction for future work is therefore to extend the framework to richer model classes, including continua of dynamical models. 

(3) Another important direction for future work is to solve the exact Bellman equation and thereby characterize the truly optimal dual controller, including its optimal exploration strategy. 

(4) Another possible follow-up work would be to devise better approaches for solving (5.7) ,which is needed to synthesize the adaptive policy. This should lead to tighter certified cost and gain bounds, similar to the work in [15], which addressed the quadratic setting instead. 

(5) Another interesting direction is to pursue application driven extensions supported by the positive system classes considered here, and to assess the practical implications of the proposed dual controllers in representative case studies. For instance, one may develop a minimax adaptive dual control framework for stochastic shortest-path problems modeled by the class in [30]; see Section 2.3. Another promising direction is the study of compartmental models, as in [14]. 

# Acknowledgments 

The authors would like to thank Tomas Meijer for his constructive feedback, which led to a significant improvement in the clarity and exposition of the paper. The authors are affiliated with the ELLIIT Strategic Research Area at Lund University. This project received funding from the European Research Council (ERC) under grant agreement No. 834142 (ScalableControl), and was partially supported by the Wallenberg AI, Autonomous Systems and Software Program (WASP), funded by the Knut and Alice Wallenberg Foundation. 

# A Auxiliary Lemmata and their proofs 

Lemma 1: Let c, c 1, c 2 be nonnegative real numbers. Then, the following holds: (L1) If λ, γ ≥ 0, then 

max 

> v≥0
> (

λv − γ|c − v|) =

> 

λc, γ ≥ λ, 

+∞, γ < λ. (L1) 22 (L2) If c1, c 2 ≥ 0, α > 0, and λ ≥ 0, then 

max 

> v≥0
> (

λv − α(|c1 − v| + |c2 − v|)) =

> 

λ max {c1, c 2} − α|c1 − c2|, 0 ≤ λ ≤ 2α, 

+∞, λ > 2α. 

(L2) 

Proof of (L1). Consider the scalar problem 

max 

> v≥0
> (

λv − γ|c − v|).

If v ≤ c, then |c − v| = c − v and 

λv − γ|c − v| = λv − γ(c − v) = ( λ + γ)v − γc, 

which is affine in v with slope λ + γ ≥ 0, so on [0 , c ] it is maximized at v = c. If v ≥ c, then 

|c − v| = v − c and 

λv − γ|c − v| = λv − γ(v − c) = ( λ − γ)v + γc. 

We therefore distinguish three cases depending on (γ, λ )

(1) If γ > λ , then λ − γ < 0, so on [c, ∞) the expression is decreasing in v and is again maximized at v = c.

(2) If γ = λ, then the expression is constant on [c, ∞), equal to λc .

(3) If γ < λ , then λ−γ > 0, so on [c, ∞) the expression grows linearly with v and the supremum is +∞.Hence 

max 

> v≥0
> (

λv − γ|c − v|) =

> 

λc, γ ≥ λ, 

+∞, γ < λ. 

This proves (L1). 

Proof of (L2) . Without loss of generality, assume c1 ≤ c2; the case c2 < c 1 is symmetric. Consider 

max 

> v≥0
> (

λv − α(|c1 − v| + |c2 − v|))

.

We analyze three regions. 

(1) Region 1: 0 ≤ v ≤ c1. Then |c1 − v| = c1 − v and |c2 − v| = c2 − v, so 

λv − α(|c1 − v| + |c2 − v|) = λv − α(c1 + c2 − 2v) = ( λ + 2 α)v − α(c1 + c2).

This is affine in v with slope λ + 2 α > 0, so on [0 , c 1] it is maximized at v = c1.

(2) Region 2: c1 ≤ v ≤ c2. Then |c1 − v| = v − c1 and |c2 − v| = c2 − v, so 

λv − α(|c1 − v| + |c2 − v|) = λv − α(c2 − c1).

This is affine in v with slope λ ≥ 0, so on [c1, c 2] it is maximized at v = c2.

(3) Region 3: v ≥ c2. Then |c1 − v| = v − c1 and |c2 − v| = v − c2, so 

λv − α(|c1 − v| + |c2 − v|) = λv − α(2 v − (c1 + c2)) = ( λ − 2α)v + α(c1 + c2).

If λ < 2α, this is decreasing on [c2, ∞), so it is maximized there at v = c2. If λ = 2 α, it is constant on [c2, ∞). If λ > 2α, it is increasing on [c2, ∞), so the supremum is +∞.23 Therefore two cases arise: 

(1) If 0 ≤ λ ≤ 2α, the maximum is attained at v = c2, and since c2 = max {c1, c 2} and 

c2 − c1 = |c1 − c2|,

max 

> v≥0

(λv − α(|c1 − v| + |c2 − v|)) = λc 2 − α(c2 − c1) = λ max {c1, c 2} − α|c1 − c2|.

(2) If λ > 2α, the maximum is +∞.Hence 

max 

> v≥0

(λv − α(|c1 − v| + |c2 − v|)) =



λ max {c1, c 2} − α|c1 − c2|, 0 ≤ λ ≤ 2α, 

+∞, λ > 2α. 

This proves (L2) and completes the proof of the lemma. 

Lemma 2 (Vector version of Lemma 1): Let c, c 1, c 2 be vectors in Rm 

> +

. The maximum is taken over all nonnegative vectors v ∈ Rm 

> +

. The following holds. (VL1) Let λ, γ ∈ Rm 

> +

. Then, 

max 

> v≥0

(

λ⊤v − γ⊤|c − v|

)

=



λ⊤c, γ ≥ λ, 

+∞, otherwise . (VL1) 

(VL2) Let c1, c 2 ∈ Rm 

> +

, α ∈ Rm 

> +

, and λ ∈ Rm 

> +

. Then 

max 

> v≥0

(

λ⊤v − α⊤(|c1 − v| + |c2 − v|))

=



λ⊤ max {c1, c 2} − α⊤|c1 − c2|, 0 ≤ λ ≤ 2α, 

+∞, otherwise .

(VL2) 

Proof. The objectives separate across coordinates. Indeed, 

λ⊤v − γ⊤|c − v| =

> m

∑

> i=1

(λivi − γi|ci − vi|),

and 

λ⊤v − α⊤(|c1 − v| + |c2 − v|) =

> m

∑

> i=1

(λivi − αi(|c1,i − vi| + |c2,i − vi|)).

Therefore the maximization over v ∈ Rm 

> +

reduces to independent scalar maximization problems in the coordinates v1, . . . , v m. Applying Lemma 1 coordinatewise yields the stated formulas. The finiteness conditions are also coordinatewise, namely γi ≥ λi for all i in (VL1) and 0 ≤ λi ≤ 2αi

for all i in (VL2), which is exactly γ ≥ λ and 0 ≤ λ ≤ 2α componentwise. 

A.1 Minimax dynamic programming 

We define a general discrete-time, infinite-horizon, minimax optimal control problem with continuous cost function and constraints as 

inf  

> μ

max 

> w
> ∞

∑

> t=0

g(x(t), u (t), w (t)) 

s.t. x(t + 1) = f (x(t), u (t), w (t)) , x(0) = x0,x(t) ∈ X ; x(0) = x0; u(t) = μ(x(t)) ,u(t) ∈ U (x(t)); w(t) ∈ W (x(t)) ,

(A.1) 24 where f : X × U × W → X represents the vector of n-dimensional state variables, u the 

m-dimensional control variable and w the q-dimensional disturbance. 

Lemma 3: Suppose 

max  

> w∈W (x)

g(x, u, w ) ≥ 0, for all x ∈ X , for all u ∈ U (x).

Then, the following statements are equivalent. (i) The general optimal control problem in (B.1) has a finite value for every x0 ∈ Rn

> +

.(ii) The recursive sequence {Jk}∞ 

> k=0

with J0 = 0 and 

Jk(x) = min  

> u

max 

> w
> [

g(x, u, w ) + Jk−1(f (x, u, w )) ]. (A.2) 

has a finite limit for all x ∈ X .(iii) The Bellman equation 

J∗(x) = min  

> u

max 

> w
> [

g(x, u, w ) + J∗(f (x, u, w )) ] (A.3) 

has nonnegative solution J∗(x), for all x ∈ X .Proof. The proof is presented in [24, Appendix]. 

# B Model-based minimax dynamic games for positive linear systems 

Understanding the model-based solution is an important step toward the model-free setting. Accordingly, this section shows that the model-based solution of Problem 1 coincides with the solution derived in [23], even when disturbances are allowed to be negative, provided that state positivity is preserved. When the system matrices (A, B ) are known, Problem 1 reduces to an 

H∞-type optimal control problem for positive systems. Equivalently, it can be formulated as a zero-sum dynamic game in which the controller is the minimizing player and the disturbance is the maximizing player [8]. We establish this equivalence in the next section. 

B.1 Model-based solution via dynamic programming 

When the parameters (A, B ) are known and under Assumption 3, Problem 1 reduces to 

inf  

> μ

sup  

> w≥ − (Ax +Bu )
> ∞∑
> t=0
> (

s⊤xt + r⊤ut − γ⊤|wt|)

s.t. xt+1 = Ax t + Bu t + wt, x0 ∈ Rn

> +

,ut = μt(xt),

|ut| ≤ Ex t.

(B.1) The following theorem characterizes the solution to B.1. 

Theorem 4 (Solution to problem B.1): Let A ∈ Rn×n 

> +

, B = [ B1 · · · Bm] ∈ Rn×m, E =[E⊤ 

> 1

· · · E⊤

> m

]⊤ ∈ Rm×n 

> +

, s ∈ Rn

> +

, r ∈ Rm, and γ ∈ Rn

> +

. Suppose that A ≥ | B|E and s ≥ E⊤|r|.Then, the following statements are equivalent: (i) Problem (B.1) has finite value for every x0 ∈ Rn

> +

.

25 (ii) The recursive sequence {pk}∞

> k=0

, with p0 = 0 and 

pk = s + A⊤pk−1 − E⊤∣∣r + B⊤pk−1

> ∣∣

, k ≥ 1,

has a finite limit p ∈ Rn 

> +

satisfying p ≤ γ.(iii) There exists p ∈ Rn 

> +

such that 

p = s + A⊤p − E⊤∣∣r + B⊤p∣∣, γ ≥ p. 

If these conditions hold, then problem (B.1) has the optimal value p⊤x0, where p is the limit of the sequence in (ii) that solves the algebraic equation in (iii). Moreover, an optimal control law is ut = −Kx t, where 

K =

> 

sign( r1 + p⊤B1)E1

...

sign( rm + p⊤Bm)Em 

> 

.

For the sake of brevity, we omit the detailed proof, as it can be readily adapted from [23]. We provide only a brief proof sketch below. 

Proof sketch. The proof is based on Lemma 3. First, we reduce the general problem setup in Lemma 3 to our setting by letting 

f (x, u, w ) := Ax + Bu + w, g(x, u, w ) := s⊤x + r⊤u − γ⊤|w|.

The assumptions s ≥ E⊤|r| and A ≥ | B|E, together with the constraint |u| ≤ Ex , guarantee that 

sup 

> w≥− (Ax +Bu )

g(x, u, w ) ≥ 0.

Next, we use induction on Jk(x) := p⊤ 

> k

x and J∗(x) := p⊤x for all x, to prove the equivalence between the recursive sequence {pk}∞ 

> k=0

and the recursion A.2 in Lemma 3 and between the Bellman equation for Problem (B.1) and the fixed point equation A.3 in Lemma 3. We use these equivalences to deduce the bound for the disturbance penalty and prove the implication “ ⇒”. The implication “ ⇐” is direct by applying the disturbance penalty condition to J∗(x). Finally, the expression for the optimal control policy is given by u(t) = μ(x(t)) , where 

μ(x) = arg min  

> |u|≤ Ex

sup 

> w≥− (Ax +Bu )
> {

s⊤x + r⊤u − γ⊤|w| + p⊤(Ax + Bu + w)

> }

.

Equivalently, after removing the terms independent of u,

μ(x) = arg min 

> |u|≤ Ex m∑
> i=1
> (

ri + p⊤Bi

> )

ui.

Since, for all i = 1 , . . . , m , the inequality |u| ≤ Ex restricts ui to the interval [−Eix, E ix], the minimum is attained when (ri + p⊤Bi) and ui have opposite signs. Thus, 

ui = − sign 

> (

ri + p⊤Bi

> )

Eix, i = 1 , . . . , m. 

26 B.2 Related minimax dynamic game and ℓ1-gain 

In this section, we interpret the problem from a robustness perspective. Specifically, we show that a solution to the minimax problem certifies robustness guarantees formulated in terms of an 

ℓ1-gain minimization problem. This highlights that, whenever such a solution is found, it enjoys robustness guarantees. For mathematical convenience, in this subsection we restrict the disturbance penalty vector to be a scalar multiple of the all-ones vector; that is, instead of a general nonnegative vector ¯γ ∈ Rn

> +

,we consider ¯γ = γ1, where γ ≥ 0 and 1 ∈ Rn denotes the all-ones vector. Define the functional 

Lγ (μ, w ) := 

> ∞

∑

> t=0

(s⊤xt + r⊤ut − γ 1⊤|wt|),

where ut = μt(xt), and consider the minimax two-player dynamic game 

inf  

> μ

sup 

> w≥− (Ax +Bu )

Lγ (μ, w )

s.t. xt+1 = Ax t + Bu t + wt,ut = μt(xt), |ut| ≤ Ex t, ∀t ∈ N.

From Theorem B.1, if feasible, the game value is 

inf  

> μ

sup 

> w≥− (Ax +Bu )

Lγ (μ, w ) = p⊤x0.

Let u⋆ and w⋆ denote optimal policies of the minimizing and maximizing players, respectively. For any disturbance sequence w satisfying wt ≥ − (Ax t + Bu ⋆t ), ∀t ∈ N, we have 

> ∞

∑

> t=0

(s⊤xt + r⊤u⋆t

) − γ

> ∞

∑

> t=0

1⊤|wt| = Lγ (u⋆, w ) ≤ Lγ (u⋆, w ⋆) = p⊤x0.

It follows that, for every positivity preserving disturbance sequence w,

> ∞

∑

> t=0

(s⊤xt + r⊤u⋆t

) ≤ p⊤x0 + γ

> ∞

∑

> t=0

1⊤|wt| = p⊤x0 + γ∥w∥1.

In particular, if x0 = 0 , then, by positivity of the stage cost, defining yt := s⊤xt + r⊤u⋆t ≥ 0, we obtain 

∥y∥1 =

> ∞

∑

> t=0

|yt| =

> ∞

∑

> t=0

(s⊤xt + r⊤u⋆t

) ≤ γ∥w∥1.

Equivalently, defining the induced ℓ1-gain from w to y under the positivity-preserving disturbance constraint by 

∥Gγ ∥ℓ1→ℓ1 := sup 

> w≥− (Ax +Bu ⋆)

∑∞ 

> t=0

|yt|

∑∞ 

> t=0

∥wt∥1

,

we obtain ∥Gγ ∥ℓ1→ℓ1 ≤ γ. That is, the induced ℓ1-gain from w to y is upper bounded by γ.We define γ⋆ as the smallest γ ≥ 0 for which there exist p ∈ Rn 

> +

and a static state feedback law 

u = Kx , with K ∈ K (E), such that 

γ⋆ = inf  

> γ, p, K ∈K (E)

γ

subject to γ ≥ 0,p = s + ( A + BK )⊤p + K⊤r, 0 ≤ p ≤ γ 1.

The value γ⋆ is the tightest upper bound on the induced ℓ1-gain from the disturbance sequence 

w to the performance output yt = s⊤xt + r⊤ut when x0 = 0 .27 C Proofs of Main Results 

C.1 Proof of Theorem 1 

Recall that, for each (A, B ) ∈ M , the model-specific history update is 

z(A,B )+ = z(A,B ) + γ⊤|v − Ax − Bu |, x+ = v, v ≥ 0, (C.1) We collect all model-specific histories in the data collection Z. Recall also the dynamic program-ming operators 

FuV (x, Z ) = max 

> v≥0

{

s⊤x + r⊤u + V (v, Z +)

}

, FV (x, Z ) = min  

> u∈U (x)

FuV (x, Z ),

with initialization and update 

V0(x, Z ) := − min  

> (A,B )∈M

z(A,B ), Vk+1 = FVk, k ≥ 0. (C.2) 

Proof of (i)-(ii). We start by showing that the sequence V0, V 1, V 2, . . . is monotonically non-decreasing. By definition, 

V1(x, Z ) = min  

> u∈U (x)

max 

> v≥0

{

s⊤x + r⊤u + V0(v, Z +)

}

= min  

> u∈U (x)

max 

> v≥0

{

s⊤x + r⊤u − min 

> (A,B )∈M

(z(A,B ) + γ⊤|v − Ax − Bu |)}

.

Positivity of the stage cost follows from Assumption 2, i.e. s⊤x + r⊤u ≥ 0. Therefore, 

s⊤x + r⊤u − min 

> (A,B )∈M

(z(A,B ) + γ⊤|v − Ax − Bu |) ≥ − min 

> (A,B )∈M

(z(A,B ) + γ⊤|v − Ax − Bu |).

It follows that, for any (x, Z ),

V1(x, Z ) ≥ − max  

> u∈U (x)

min  

> (A,B )∈M

min 

> v≥0

(z(A,B ) + γ⊤|v − Ax − Bu |) = − min  

> (A,B )∈M

z(A,B ) = V0(x, Z ),

and so V1 ≥ V0. Since F is order-preserving in V , it follows by induction that 

Vk+1 = FVk ≥ FVk−1 = Vk, for all k ≥ 0.

We conclude that the sequence V0, V 1, V 2, . . . is monotonically non-decreasing. Fix N ≥ 0. Consider the N -stage truncated original minimax problem 

inf  

> μ

sup  

> (A,B )∈M
> w≥ − (Ax +Bu )
> N

∑

> t=0

(

s⊤xt + r⊤ut − γ⊤|wt|

)

s.t. xt+1 = Ax t + Bu t + wt, x0 ∈ Rn

> +

.

(C.3) The value of Problem 1 is bounded below by the expression (C.3) . The value of (C.3) grows monotonically with N , and the value of Problem 1 is obtained in the limit as N → ∞ . Introducing the change of variables vt := xt+1 and using the model-specific histories from (C.1) renders (C.3) equivalent to 

inf  

> μ

sup   

> v≥0
> N

∑

> t=0

(s⊤xt + r⊤ut

) − min  

> (A,B )∈M

z(A,B )  

> N+1

,

s.t. xt+1 = vt, x0 ∈ Rn

> +

,z(A,B ) 

> t+1

= z(A,B ) 

> t

+ γ⊤|vt − Ax t − Bu t|, z(A,B )0 = 0 , (A, B ) ∈ M .Zt := {z(A,B ) 

> t

}(A,B )∈M , Z0 := {0}(A,B )∈M ,ut = η(xt, Z t) ∈ U (xt), ∀ t = 0 , . . . , N + 1 .

(C.4) 28 Standard minimax dynamic programming argument with terminal cost and recursion given in (C.2) , shows that the value of (C.4) is VN +1 (x0, 0) . Thus, Problem 1 has finite value J∗(x0)

if and only if the nondecreasing sequence {Vk(x0, 0) }k≥0 is upper bounded. The limit V∗(x0, 0) 

is equal to the value of Problem 1. It holds also that Vk(x, Z ) ≤ Vk(x, 0) for every nonnegative history collection Z. This shows that the limit exists for every such Z as well. If problem 2 is finite, then Vk(x0, 0) is bounded above by the value of (2) , so also V∗(x0, 0) = lim k→∞ Vk(x0, 0) 

is finite. This concludes proving items (i) -(ii) . Next, we prove item (iii) of the theorem. 

Proof of (iii). Suppose we could find a value function V (x, Z ) satisfying V0 ≤ V ≤ ∞ and 

Fη(x, Z ) V ≤ V , we may then define the sequence W0, W 1, W 2, . . . recursively by 

W0 := V0, Wk+1 (x, Z ) := Fη(x, Z )Wk(x, Z ).

By dynamic programming, 

WN (x, 0) = max 

> v≥0
> N

∑

> t=0

(s⊤xt + r⊤ut

) − min  

> (A,B )∈M

z(A,B )  

> N+1

,

where xt+1 = vt, ut = η(xt, Z t), and Z is generated by the model-specific historical data update in (C.1) . Therefore, Problem (2) is bounded above by lim k→∞ Wk(x0, 0) . The definitions of Vk

and Wk give by induction V ≥ Wk ≥ Vk for all k, so 

V∗(x, Z ) ≤ lim  

> k→∞

Wk(x, Z ) ≤ F η(x, Z )V (x, Z ) ≤ V (x, Z ).

This proves that V∗(x0, 0) ≤ Jμ(x0) ≤ V (x0, 0) when the control law μ is defined by ut = η(xt, Z t).In particular, the control law is optimal if the equality V (x, Z ) = V∗(x, Z ) is attained. 

C.2 Proof of Theorem 2 

This is a specialization of the more general Theorem 3 to the two model family M :=

{(A, B ), (A, −B)}. Theorem 3 is proven in appendix C.4. We verify that the conditions in (5.7) of Theorem 3 reduce to conditions (i) –(iii) in the statement of Theorem 2. Set 

p++ = p+, p−− = p−, p+− = p−+ = h. 

The conditions in (5.7) are covered by considering the following three cases. 

(i) Case i = j = σ. Here Ai − Aj = 0 and Bi − Bj = 0 , so the absolute-value term in (5.7) vanishes, while max {Aix + BiKσx, A j x + Bj Kσx} = ( A + σBK σ)x. Since pjk = pij = pσ,condition (5.7) becomes 

p⊤ 

> σ

x ≥ s⊤x + r⊤Kσx + p⊤ 

> σ

(A + σBK σ)x. 

Since this must hold for all x ∈ Rn

> +

, it is equivalent to having (i) .

(ii) Case i = j = σ̸ = k. Again Ai − Aj = 0 and Bi − Bj = 0 , so the absolute value term vanishes, and max {Aix + BiKkx, A j x + Bj Kkx} = ( A + σBK k)x. Since pjk = h and 

pij = pσ, condition (5.7) becomes 

h⊤x ≥ s⊤x + r⊤Kkx + p⊤ 

> σ

(A + σBK k)x. 

Since this holds for all x ∈ Rn

> +

, it is equivalent to (ii) . Such an h also satisfies h ≥

max {p+, p −}, since 

h⊤x ≥ s⊤x + r⊤Kkx + p⊤ 

> σ

(A + σBK k)x ≥ s⊤x + r⊤Kσx + p⊤ 

> σ

(A + σBK σ)x = p⊤ 

> σ

x. 

i.e., applying the wrong controller leads to an increase in the certified cost. 29 (iii) Case i̸ = j. Now Ai = Aj = A and Bi − Bj = ±2B. Therefore 

max {Aix + BiKkx, A j x + Bj Kkx} = max {(A + BK k)x, (A − BK k)x} = Ax + |BK kx|,

where the last identity holds componentwise. Moreover, 

12 γ⊤∣∣ (Ai − Aj )x + ( Bi − Bj )Kkx ∣∣ = γ⊤|BK kx|.

Since pjk = pij = h, substituting into (5.7) yields (iii) , i.e. 

h⊤x ≥ s⊤x + r⊤Kkx + h⊤(Ax + |BK kx|) − γ⊤|BK kx|.

Assuming that (i) –(iii) is true, from Theorem 3 it follows that the Bellman equality holds 

F V (x, Z ) ≤ V (x, Z ) for the choice 

V ij (x, z (+) , z (−)) := p⊤ 

> ij

x − 12

> (

z(i) + z(j))

, V (x, z (+) , z (−)) := max  

> i,j ∈{ +,−}

V ij (x, z (+) , z (−)).

The stated performance bound follows directly from Theorem 3, i.e. Jμ(x0) ≤ max i,j p⊤ 

> ij

x0 =

h⊤x0. Finally, assume in addition that m = 1 hence Kk can take two possible values Kk = ±E.Then Kkx = ±Ex is a scalar. Since E ∈ R1×n 

> +

and x ∈ Rn

> +

, we have Ex ≥ 0, and hence 

|BK kx| = |B| | Kkx| = |B| Ex = ( |B|E)x. Substituting this identity into (iii) gives 

h⊤x ≥ s⊤x + r⊤Kkx + h⊤Ax + ( h − γ)⊤(|B|E)x, ∀x ∈ Rn

> +

.

Since this inequality must hold for all x ∈ Rn

> +

, it is equivalent to (iii’) .

C.3 Proof of Corollary 1 

To prove Corollary 1, we seek the smallest value of γ for which the conditions in Theorem 2 hold, since this yields the best gain and cost guarantee achieved by the CE policy proposed in Theorem 2. First, observe that conditions (i) –(iii) in Theorem 2, under the choices n = m = 1 ,

A = a > 0, B = b > 0, s = 1 , r = 0 , and E = ab reduce to the simpler feasibility conditions 

p ≥ 1, h ≥ 1 + 2 ap, h ≥ 1 + 2 ah − γa, 

with p, h ∈ [0 , γ ]. Next, we find the smallest γ for which these three scalar inequalities hold. Since p ≥ 1, we necessarily have h ≥ 1 + 2 a, and hence every feasible γ satisfies γ ≥ h ≥ 1 + 2 a. 

The last inequality can be equivalently written as h(1 − 2a) ≥ 1 − γa. We now distinguish two cases. 

(1) Case 0 ≤ a ≤ 12 . Choose p = 1 and h = γ = 1 + 2 a. Then the first two inequalities hold with equality. Moreover, 

h(1 − 2a) − (1 − γa ) = (1 + 2 a)(1 − 2a) − (1 − a(1 + 2 a)) = a(1 − 2a) ≥ 0.

Thus γ = 1 + 2 a is feasible. Since no feasible γ can be smaller than 1 + 2 a, this value is minimal. 

(2) Case a ≥ 12 . Since 1 − 2a ≤ 0, the inequalities h ≥ 1 + 2 a and h(1 − 2a) ≥ 1 − γa imply 

1 − γa ≤ h(1 − 2a) ≤ (1 + 2 a)(1 − 2a) = 1 − 4a2,

and therefore γ ≥ 4a. Conversely, choose p = 1 , h = 1 + 2 a, and γ = 4 a. Then h ≤ γ

because a ≥ 12 , and the last inequality holds with equality: h(1 − 2a) = 1 − 4a2 = 1 − γa .Hence γ = 4 a is feasible and minimal. Therefore, γCE (a) = 1 + 2 a for 0 ≤ a ≤ 12 , and γCE (a) = 4 a for a ≥ 12 , with minimizing values 

p = 1 and h = 1 + 2 a. Moreover, Theorem 2 implies that the Bellman inequality holds and certifies the cost bound Jμ(x0) ≤ hx 0 = (1 + 2 a)x0. This proves the claim of the corollary. 30 C.4 Proof of Theorem 3 

For M = {(A1, B 1), . . . , (AM , B M )}, let z(i) denote the scalar cumulative history associated with model (Ai, B i), evolving according to 

z(i) 

> t+1

= z(i) 

> t

+ γ⊤∣∣ vt − Aixt − Biut

∣∣, z(i)0 = 0 .

Collect these into Z := (z(1) , . . . , z (M )). For i, j ∈ { 1, . . . , M }, define 

V ij (x, Z ) := p⊤ 

> ij

x − 12

(z(i) + z(j)).

Next, define V := max i,j V ij . In proving this Theorem, we rely on item (iii) of theorem 1, that is we show that V fulfills the sufficient conditions proposed there. First, note that V ≥ V0 = − min i z(i),indeed, it holds that 

max 

> i,j

(

p⊤ 

> ij

x − 12

(z(i) + z(j)))

≥ max 

> i,j

(

− 12

(z(i) + z(j)))

= − min 

> i,j
> 12

(z(i) + z(j)) ≥ − min  

> i

z(i).

The first inequality follows from p⊤ 

> ij

x ≥ 0, while the last follows by choosing j = i, which gives 

− 12

(z(i) + z(i)) = −z(i). Hence, V ≥ V0 as required by item (iii) in Theorem 1. It remains to show that 

Fη V (x, Z ) ≤ V (x, Z ) for all (x, Z ) ∈ Rn 

> +

× R|M| ,

where FηV (x, Z ) := FuV (x, Z )∣∣u=η(x,Z ). We let ¯η(x, Z ) = Kkx, where k := arg min i∈{ 1,...,M } z(i).and recall FuV (x, Z ) := max v≥0

{s⊤x + r⊤u + V (v, Z +)}. We distinguish two cases: 

(i) If i = j = k, then 

FKix V ii (x, Z ) = max 

> v≥0



s⊤x + r⊤Kix + V ii 

v, 



z(1) 

...

z(M )

 +



γ⊤|v − A1x − B1Kix|

...

γ⊤|v − AM x − BM Kix|



= max 

> v≥0

{

p⊤ 

> ii

v − z(i) + s⊤x + r⊤Kix − γ⊤|v − Aix − BiKix|

}

= −z(i) + s⊤x + r⊤Kix + max 

> v≥0

{

p⊤ 

> ii

v − γ⊤|v − (Aix + BiKix)|

}

.

Since 0 ≤ pii ≤ γ, from Lemma 2 (VL1), it holds that 

−z(i) + s⊤x + r⊤Kix + max 

> v≥0

{

p⊤ 

> ii

v − γ⊤|v − (Aix + BiKix)|

}

= −z(i) + s⊤x + p⊤ 

> ii

Aix + (r⊤ + p⊤ 

> ii

Bi

)Kix. 

Since p⊤ 

> ii

x ≥ s⊤x + p⊤ 

> ii

(Ai + BiKi)x + r⊤Kix, it follows that 

FKi V ii (x, Z ) ≤ − z(i) + p⊤ 

> ii

x = V ii (x, Z ).

(ii) We now consider all remaining index configurations, that is, all triples (i, j, k ) for which at least two of the indices are distinct. With control Kkx used, we obtain 

FKk xV ij (x, Z ) = max 

> v≥0



s⊤x + r⊤Kkx + V ij 

v, 



z(1) 

...

z(M )

 +



γ⊤|v − A1x − B1Kkx|

...

γ⊤|v − AM x − BM Kkx|



= max 

> v≥0

{

p⊤ 

> ij

v− 12

(z(i) +z(j))+s⊤x+r⊤Kkx− 12 γ⊤|v−Aix−BiKkx|− 12 γ⊤|v−Aj x−Bj Kkx|

}

= − 12

(z(i)+z(j))+s⊤x+r⊤Kkx+max 

> v≥0

{

p⊤ 

> ij

v− 12 γ⊤|v−Aix−BiKkx|− 12 γ⊤|v−Aj x−Bj Kkx|

}

.

31 Let yi := Aix + BiKkx, yj := Aj x + Bj Kkx, and recall that yi ≥ 0, yj ≥ 0 and 0 ≤ pij ≤ γ.We are then solving the optimization problem 

max 

> v≥0

{

p⊤ 

> ij

v − 12 γ⊤∣∣v − yi

∣∣ − 12 γ⊤∣∣v − yj

∣∣ }

.

It follows from Lemma 2 (VL2) that 

v∗ = max {yi, y j } = max {Aix + BiKkx, A j x + Bj Kkx}, (C.5) and the optimal value equals p⊤ 

> ij

max {yi, y j } − 12 γ⊤∣∣ yi − yj

∣∣. Plugging this in gives 

FKk xV ij (x, Z ) = − 12

(z(i) + z(j)) + s⊤x + r⊤Kkx

+ p⊤ 

> ij

max {Aix + BiKkx, A j x + Bj Kkx} − 12 γ⊤∣∣Aix + BiKkx − Aj x − Bj Kkx∣∣.

The assumption in (5.7) can be written succinctly as 

max {p⊤

> ik

x, p ⊤

> jk

x} ≥ s⊤x + r⊤Kkx

+ p⊤ 

> ij

max {Aix + BiKkx, A j x + Bj Kkx} − 12 γ⊤∣∣ Aix + BiKkx − Aj x − Bj Kkx ∣∣,

where 0 ≤ pij = pji ≤ γ. Here, the max on the left excludes the case i̸ = j = k, consistent with the assumption in the theorem. It then follows that 

FKk V ij (x, Z ) ≤ − 12

(z(i) + z(j)) + max {p⊤

> ik

x, p ⊤

> jk

x}≤ max 

{

p⊤

> ik

x − 12

(z(i) + z(k)), p ⊤

> jk

x − 12

(z(j) + z(k))}

,

where the last step uses k = arg min r z(r), so z(k) ≤ z(i) and z(k) ≤ z(j).Since by definition V ij (x, Z ) = p⊤ 

> ij

x − 12

(z(i) + z(j)), we obtain 

FKk V ij (x, Z ) ≤ max {V ik (x, Z ), V jk (x, Z )}.

Consequently, 

FV (x, Z ) ≤ F Kk V (x, Z ) = max  

> i,j

FKk V ij (x, Z ) ≤ max  

> i,j

max {V ik (x, Z ), V jk (x, Z )}≤ max  

> i,j

V ij (x, Z ) = V (x, Z ).

This establishes the Bellman inequality FV (x, Z ) ≤ V (x, Z ). Moreover, the corresponding certainty–equivalence control law certifying the Bellman inequality is 

ut = Kkt xt, kt = arg min   

> i∈{ 1,...,M }

z(i) 

> t

.

This concludes the proof. 

# References 

[1] A. Rantzer. “Explicit solutions to Bellman equation for positive systems with linear cost”. In: 61st IEEE Conf. Decis. Control . 2022, pp. 6154–6155. [2] A. A. Al Makdah and F. Pasqualetti. Model-based and Data-based Dynamic Output Feedback for Externally Positive Systems . arXiv preprint arXiv:2305.02472. 2023. [3] A. M. Annaswamy. “Adaptive control and intersections with reinforcement learning”. In: 

Annu. Rev. Control Robot. Auton. Syst. 6.1 (2023), pp. 65–93. 32 [4] A. M. Annaswamy and A. L. Fradkov. “A historical perspective of adaptive control and learning”. In: Annu. Rev. Control 52 (2021), pp. 18–41. [5] H. Arneson, N. Dousse, and C. Langbort. “A Linear Programming Approach to Routing Control in Networks of Constrained Nonlinear Positive Systems with Concave Flow Rates”. In: Automatica 68 (2016), pp. 357–368. [6] K. J. Astrom and B. Wittenmark. Adaptive control . Addison-Wesley Longman Publishing Co., Inc., 1994. [7] K. J. Åström and B. Wittenmark. “On self tuning regulators”. In: Automatica 9.2 (1973), pp. 185–199. [8] T. Başar and P. Bernhard. H-infinity optimal control and related minimax design problems: a dynamic game approach . Springer Sci. & Bus. Media, 2008. [9] F. Bencherki and A. Rantzer. “Adaptive Control of Positive Systems with Application to Learning SSP”. In: Proc. 7th Annu. Learn. Dyn. Control Conf. PMLR. 2025, pp. 660–672. [10] F. Bencherki and A. Rantzer. “Data-Driven Adaptive Dispatching Policies for Processing Networks”. In: vol. 8. IEEE, 2024, pp. 2841–2846. [11] F. Bencherki and A. Rantzer. “Robust simultaneous stabilization via minimax adaptive control”. In: Proc. 2023 IEEE 62nd Conf. Decis. Control (CDC) . IEEE. 2023, pp. 2503– 2508. [12] A. Berman and R. J. Plemmons. Nonnegative matrices in the mathematical sciences . SIAM, 1994. [13] D. Bertsimas and C. W. Kim. “Optimal control of multiclass fluid queueing networks: A machine learning approach”. In: arXiv preprint arXiv:2307.12405 (2023). [14] F. Blanchini, P. Bolzern, P. Colaneri, G. De Nicolao, and G. Giordano. “Optimal control of compartmental models: The exact solution”. In: Automatica 147 (2023), p. 110680. [15] D. Cederberg, A. Hansson, and A. Rantzer. “Synthesis of minimax adaptive controller for a finite set of linear systems”. In: Proc. 2022 IEEE 61st Conf. Decis. Control (CDC) . IEEE. 2022, pp. 1380–1384. [16] S. Coogan and M. Arcak. “A Compartmental Model for Traffic Networks and Its Dynamical Behavior”. In: IEEE Trans. Autom. Control 60.10 (2015), pp. 2698–2703. doi : 10.1109/ TAC.2015.2411916 .[17] G. Didinsky and T. Basar. “Minimax adaptive control of uncertain plants”. In: Proc. 1994 33rd IEEE Conf. Decis. Control . Vol. 3. IEEE. 1994, pp. 2839–2844. [18] L. Farina and S. Rinaldi. Positive linear systems: theory and applications . John Wiley & Sons, 2011. [19] G. C. Goodwin, P. J. Ramadge, and P. E. Caines. “Discrete time stochastic adaptive control”. In: SIAM J. Control Optim. 19.6 (1981), pp. 829–853. [20] G. C. Goodwin and K. S. Sin. Adaptive filtering prediction and control . Courier Corporation, 2014. [21] L. Guo. “Convergence and logarithm laws of self-tuning regulators”. In: Automatica 31.3 (1995), pp. 435–450. [22] A. Gurpegui, M. Jeeninga, E. Tegling, and A. Rantzer. “Minimax linear regulator problems for positive systems”. In: IEEE Trans. Autom. Control (2026). [23] A. Gurpegui, E. Tegling, and A. Rantzer. “A minimax optimal controller for positive systems”. In: arXiv preprint arXiv:2502.01180 (2025). [24] A. Gurpegui, E. Tegling, and A. Rantzer. “Minimax linear optimal control of positive systems”. In: IEEE Control Syst. Lett. 7 (2023), pp. 3920–3925. [25] T. Iwata, S. -i. Azuma, R. Ariizumi, and T. Asai. “Data Informativity for Distributed Positive Stabilization”. In: IEEE Control Syst. Lett. 8 (2024), pp. 826–831. [26] Y. Li and A. Rantzer. “Exact dynamic programming for positive systems with linear optimal cost”. In: IEEE Trans. Autom. Control 69.12 (2024), pp. 8738–8750. 33 [27] N. Matni, A. Proutiere, A. Rantzer, and S. Tu. “From self-tuning regulators to reinforcement learning and back again”. In: Proc. 2019 IEEE 58th Conf. Decis. Control (CDC) . IEEE. 2019, pp. 3724–3740. [28] J. Miller, T. Dai, M. Sznaier, and B. Shafai. “Data-Driven Control of Positive Linear Systems using Linear Programming”. In: Proc. 2023 62nd IEEE Conf. Decis. Control (CDC) . IEEE. 2023, pp. 1588–1594. [29] K. S. Narendra and J. Balakrishnan. “Adaptive control using multiple models”. In: IEEE Trans. Autom. Control 42.2 (1997), pp. 171–187. [30] D. Ohlin, A. Rantzer, and E. Tegling. “Heuristic search for linear positive systems”. In: 

Automatica 192 (2026), p. 113153. [31] D. Ohlin, E. Tegling, and A. Rantzer. “Optimal control of linear cost networks”. In: Eur. J. Control 80 (2024), p. 101068. [32] A. Rantzer. “Minimax adaptive control for a finite set of linear systems”. In: Proc. Mach. Learn. Res. (L4DC) . PMLR. 2021, pp. 893–904. [33] A. Rantzer. “Scalable Control of Positive Systems”. In: Eur. J. Control 24 (2015), pp. 72–80. [34] A. Rantzer and M. E. Valcher. “Scalable control of positive systems”. In: Annu. Rev. Control Robot. Auton. Syst. 4.1 (2021), pp. 319–341. [35] V. Renganathan, A. Iannelli, and A. Rantzer. “An online learning analysis of minimax adaptive control”. In: Proc. 2023 IEEE 62nd Conf. Decis. Control (CDC) . IEEE. 2023, pp. 1034–1039. [36] S. Sadraddini and C. Belta. “Formal Synthesis of Control Strategies for Positive Monotone Systems”. In: IEEE Trans. Autom. Control 64.2 (2019), pp. 480–495. doi : 10.1109/TAC. 2018.2814631 .[37] B. Shafai, A. Moradmand, and M. Siami. “Data-Driven Positive Stabilization of Linear Systems”. In: Proc. 2022 8th Int. Conf. Control, Decis. Inf. Technol. (CoDIT) . IEEE. 2022, pp. 1031–1036. [38] L. Tassiulas and A. Ephremides. “Throughput Properties of a Queueing Network with Distributed Dynamic Routing and Flow Control”. In: Adv. Appl. Probab. 28.1 (1996), pp. 285–307. doi : 10.2307/1427922 .[39] A. Tsiamis, I. Ziemann, N. Matni, and G. J. Pappas. “Statistical learning theory for control: A finite-sample perspective”. In: IEEE Control Syst. Mag. 43.6 (2023), pp. 67–97. [40] G. Vinnicombe. “Examples and counterexamples in finite ℓ2-gain adaptive control”. In: 

Leuven: Sixteenth International Symposium on Mathematical Theory of Networks and Systems (MTNS2004) . 2004. [41] Y. Wang and B. Shafai. “Data Driven Positive Subspace System Identification”. In: IFAC-PapersOnLine 58.15 (2024), pp. 372–377. [42] Y. Wang and B. Shafai. “Data-Driven Identification and Control of Positive Systems”. In: 

Integrated Systems: Data Driven Engineering . Springer, 2024, pp. 289–307. 34